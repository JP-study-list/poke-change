# CLAUDE.md — 開發規範與專案脈絡（單一檔）

> 本檔為通用規範 + 本專案技術背景的合併檔，放在專案根目錄，Claude Code 啟動時自動載入。
> 新 repo 只需複製本檔（§0~§7 通用部分照抄），第一次對 Claude Code 說「初始化這個專案」，
> 其餘檔案（project-index.md、progress.md）與 §8 專案背景會自動長出來（見 §1 自舉）。
>
> ⚠️ 本檔會 commit 上公開 repo。**嚴禁寫入任何 secret**（見 §4 規則 D 的可寫/不可寫清單）。

---

## 0. 語言與溝通
- 一律使用**繁體中文**回覆。
- 解釋精簡、切中要點，節省 token。

---

## 1. 啟動 SOP（每次新 session 開場）

### 一般啟動
1. 讀 `project-index.md`（全部）——掌握結構與各檔用途，**不重掃全部原始碼**。
2. 讀 `progress.md` **最新 3~5 筆**——掌握上次進度與待辦。
3. 需動到某檔時，才針對性讀該檔。

### 首次進專案 / 使用者說「初始化這個專案」時（自舉）
依序執行，**全程只新增、不刪除、不覆蓋**：
1. **讀**現有全部檔案（唯讀，不動任何一個）。
2. 若 `project-index.md` **不存在** → 自動生成；若已存在 → 更新，**絕不覆蓋既有內容**。
3. 若 `progress.md` **不存在** → 建立空模板；若已存在 → 保留。
4. 訪談使用者補齊 §8 專案技術背景，寫進本檔 §8 區塊。
   - **只問非機密項**（Project ID、collection 結構、部署 branch 等）。
   - 遇到金鑰類（`/exec` URL、API key）→ **不寫進本檔**，改提醒使用者放 `.env`，本檔只記指標。

> **自舉硬規則**：初始化流程明文禁止任何 `rm`、覆寫、`git reset` 等破壞既有檔案的操作。只建立不存在的檔。

---

## 2. 開發流程規範（嚴格執行）

收到任何工具／功能開發請求時，**不得立即寫程式碼**。依序：

### Phase 1：需求確認（必做）
1. 用自己的話覆述需求。
2. 提釐清問題，涵蓋：使用情境（手機／桌機／網頁、頻率）、核心功能（MVP vs. 加分）、UI 偏好（語言預設繁中、深/淺色、版面）、資料處理（需持久化？存哪？）、技術限制（單一 HTML？React？GitHub Pages？）。
3. 一次最多 3~5 個關鍵問題，不洗版。

### Phase 2：提案（必做）
- 功能清單（MVP vs. 未來擴充）
- 技術方案 + 一句話理由
- UI 結構文字描述（免圖）
- 結尾問：「確認後才動工，有要調整的嗎？」

### Phase 3：實作
- 僅在明確說「start／確認／go ahead」後開始寫碼。
- 驗證與 review 嚴謹徹底；交付前自我檢查功能完整性。

### 例外
- 瑣碎請求（一行 CSS、明顯 bug）可跳過，但先說「Simple request, proceeding directly」。

### 中途變更
- 需求中途更動且影響架構 → **先指出影響範圍**再動手。

---

## 3. 交付原則（本機環境）
- 直接讀寫本機檔案，不再提供「完整檔案手動貼回／ZIP」。
- 偏好乾淨美學：衝突時，簡潔 > 附加功能；果斷、直接。
- 期望根因診斷，不要表面修復。

---

## 4. 本機環境安全與協作規則（ABCD）

### A. Commit 規範
- **允許直接 commit**，不需事前確認。
- 訊息格式：`feat:` / `fix:` / `refactor:` / `docs:` / `chore:` 前綴 + 繁中摘要。
  - 例：`fix: 修正背景卡 shiny 切換未同步 Firestore`

### B. 破壞性操作前先確認（最高優先，不因 A 而放寬）
- 以下操作**必須先說明影響範圍並等待明確同意**：
  - 刪除檔案、`rm`
  - `git reset --hard`、`git push -f`、`git rebase`
  - 大範圍重構、跨多檔結構性變更
- Commit 可逆，故 A 放行；上述不可逆，故一律先問。不確定是否具破壞性時，先問。

### C. 本機測試優先於宣稱完成
- 宣稱「完成」前，能本機驗證的先驗證：起 server、看 console、跑既有測試。
- 不憑「讀過碼看起來對」就宣稱完成。

### D. Secrets 不進 git（含本 CLAUDE.md）
本檔會上公開 repo，界線如下：

**可寫進本檔（非機密）：**
- Firebase **Project ID**（本就出現在前端 config，非機密）
- Firestore collection 結構、key 設計原則
- 部署 branch、GitHub Pages 網域
- 使用的 API、資料來源、cron 時間
- 已知地雷

**絕不寫進本檔（外置到 `.env` / `config.local.js`，並列入 `.gitignore`）：**
- Apps Script `/exec` URL（等同後端入口，視為機密）
- 任何 API key / token / 私鑰、service account 憑證

本檔只記**指標**，例：`Apps Script /exec URL 存於 .env 的 APPS_SCRIPT_URL（不進 git）`。

> 註：Firebase 前端 `apiKey` 並非密鑰，本就暴露於客戶端，靠 Firestore Security Rules 保護；重點是 Rules 有沒有寫好，而非藏 key。Apps Script `/exec` URL 則須當機密。

---

## 5. 檔案維護機制

### progress.md — 開發歷史（每次改檔即更新）
- 反向時間序（最新在上）。
- 欄位：
  ```
  ## YYYY-MM-DD
  - 類型：新增 / 修正 / 重構
  - 影響檔案：xxx.html, yyy.js
  - 摘要：做了什麼
  - 原因：為什麼
  - 待辦/已知問題：（可留空）
  ```
- 小改允許精簡：只填「日期 + 類型 + 摘要」。

### project-index.md — 專案檔案索引（首次建立，每次改檔同步）
- 每個檔案的用途、彼此關係、進入點。
- 檔案新增／刪除／職責變動 → 同步更新。

---

## 6. 技術教訓（跨專案通用原則）

> 以下為既往踩坑固化的原則。**若當下發現更優解，先提出與使用者討論，不擅自沿用舊規則、也不默默改掉。**

- **Firestore key 設計**：用穩定 ID（`p###`、costume ID）當 key，seed 變動不破壞既有紀錄；只存狀態，不存顯示資料。
- **Apps Script 部署**：一律**編輯現有 deployment**（鉛筆 → 新版本 → 部署）保留 `/exec` URL，絕不新建 deployment。
- **Base64 圖片**：存 Firestore <700KB；iOS Shortcut Base64 encode 關閉換行。
- **GitHub Actions cron**：避開 UTC 午夜，偏移如 `43 0 * * *`。
- **日期／時區**：Taiwan UTC+8，日期邏輯用 local time 格式化，**不用 `toISOString()`**。
- **Google Maps API**：tile 依 ToS 不可快取；離線資料由 Firestore `persistentLocalCache` 處理。
- **靜態站資料源**：AniList GraphQL 支援 CORS 免金鑰；Nominatim 1 req/sec、免金鑰。
- **iOS Shortcut 分享**：LINE Flex Message 擷取走「截圖 → OCR」最可靠。
- **fast-flights**：用 `FlightQuery` / `create_query`（v3.x）；`FlightData` 為破壞性移除。
- **圖片格式**：背景卡等圖片 jpg/png/webp 混雜，寫死檔名前**先確認格式**。

---

## 7. 環境差異備註（vs. Artifact）
- 本機開發可用 localStorage／IndexedDB 除錯；線上部署 GitHub Pages 時，持久化仍走 Firestore。
- 本機可實跑、可 git、可 build/test —— review 標準相應提高。

---

## 8. 專案技術背景【poke-change / 寶可夢交換所】

### 這個專案在做什麼
Pokémon GO 交換清單製作工具。用圖鑑找出活動裝扮、地區型與背卡寶可夢，
排出「想要」與「可以給」兩份清單，產生分享圖傳給對方。

**差異化在資料庫深度**，不在介面。一般交換工具只收一般型態，
這裡收 1460 個條目，其中 303 個裝扮橫跨一百多個物種，
再加 240 張背卡收在 23 個收納夾裡、1579 個收集格。

背卡的圖與代號抓得到上游，「哪些寶可夢帶得了這張背卡」上游沒有，
靠 Dittobase 補、Serebii 與 Bulbapedia 交叉比對。240 張裡 229 張有清單。

### 只收確認在 GO 裡有的東西（最高原則，2026-09-17 使用者拍板）

**查不到就不要放。** 這個站的概念是只放**確認過 Pokémon GO 裡真的有**的東西，
目標是讓大家把它當圖鑑用。標準一放寬，就會有人配出遊戲裡根本不存在的
寶可夢組合，那是比「少列一張」嚴重得多的問題——圖鑑錯了就不是圖鑑。

這條管的不只是圖鑑收哪些條目（見底下「圖鑑只收 GO 實裝的」），
也管**條件與背卡的組合**：沒有資料證明「這一隻可以帶這張卡配這個條件」時，
一律不列。所以背卡旗標只涵蓋 240 張裡的 12 張、勾了淨化有 194 個條目
一張卡都不剩，**這是刻意接受的代價，不要為了畫面好看而放寬**。
旗標只會長，資料補上來就自動收緊。

**寧可少列，不要多列。** 少列的代價是使用者覺得「怎麼沒有」，
多列的代價是使用者照著做一個換不到的清單。

### 技術棧（一行摘要）
純靜態站：ES modules，無建置流程，無後台，資料存在使用者裝置的 localStorage。

### 沒有 Firebase，也不會有
刻意的決定。核心原則是**壓低營運成本到只剩網域年費**。
沒有帳號、沒有雲端、沒有跨裝置同步。代價是使用者換裝置就沒了，
所以匯出備份必須是一等公民，不能藏在設定裡。

**iOS Safari 會清掉 localStorage**（長期沒互動的網站，大約兩週）。
這擋不掉，只能靠匯出提醒。警語現在住在設定面板的資料區，
跟匯出鈕放在一起（2026-09-12 側欄拆掉時使用者指定只放那裡）。
它不是被弄丟了，不要再搬回畫面上，但也不要刪掉。

### 儲存結構
```
localStorage["poke-change/v1"]     交換清單（key 沒換，版本號從 1 變 2）
  { v:2, active:0, lists:[ list, list, list ], updated }
  list：{ name, want:[...], have:[...] }
  項目：{ id, shiny, xxl, xxs, max, bg }
  同一個 id 在同一欄可以有好幾筆，靠背卡與條件區分
  （想要皮卡丘的鑽石背卡版，也想要珍珠背卡版，就是兩格）

localStorage["poke-change/pref"]   偏好
  { lang, dark, big, names, code, goLang }
  big   大圖示。預設 false，手機一排五隻
  names 格子下方顯示名稱。預設 true
  code  友情碼，只存數字，只印在分享圖上
  goLang 搜尋字串裡關鍵字用哪一國的字。預設 "auto" 跟介面語言走。
        **跟 lang 分開**：那串字是給對方貼進他自己的遊戲的
```

兩個 key 分開存，「清空這份清單」只清前者裡的一份，不會把語言也重設掉。

**三份清單是刻意的上限，不是設定值。** 分頁一排三個剛好，多了手機擠不下。
一次只顯示一份，分享圖也只印目前這一份。

**讀進來的資料一律不信任**，全部過 `store.normalize()`。
localStorage 使用者可以手動改，也可能是舊版寫的。

### 條目 id 設計（三個不可違反的原則）
1. **只存使用者的選擇**，名稱、圖片、屬性一律以 `dex.js` 為準。
   圖鑑更新、改譯名、補裝扮都不影響既有紀錄，不需要資料遷移。
2. **id 一旦發布就不能改**。它是使用者紀錄的鍵。
3. **id 的形狀**：
   ```
   d150                    一般
   d150.fA                 型態變化
   d25.cHALLOWEEN_2017     裝扮
   d25.xREDS_HAT           裝扮，圖片來自外部個人專案
   ```

### 資料來源

| 用途 | 來源 | 風險 |
| --- | --- | --- |
| 遊戲數值、型態清單 | PokeMiners/game_masters | 低 |
| GO 圖示、官方三語名稱 | PokeMiners/pogo_assets | 低 |
| 部分裝扮皮卡丘 | Choggor/Pikachu-costume-tracker | **中，個人專案** |
| 前兩個都沒有的裝扮圖 | 自己的 `img/extra/`，5 張 | 無 |
| 立繪備援 | PokeAPI/sprites | 低 |
| 背卡圖與代號 | PokeMiners/pogo_assets `Images/LocationCards/` | 低 |
| 背卡的寶可夢清單 | Dittobase（一次性離線抽取，含型態） | **中，別人的網站** |
| 背卡的日期與英文名 | Serebii（一次性離線抽取） | **中，別人的網站** |
| 背卡清單的交叉比對 | Bulbapedia（只比對，不進資料） | **中，別人的網站** |
| 可極巨化的名單 | Dittobase（一次性離線抽取），game master 的 `BREAD` 設定交叉比對，三筆官方公告指名 | **中，別人的網站** |
| 可淨化的名單 | Dittobase 的 `isShadow`（一次性離線抽取），game master 的 `shadow` 設定只當佐證 | **中，別人的網站** |
| 背卡與交換條件的對應 | Bulbapedia `Background (GO)` 的逐隻角標（一次性離線抽取） | **中，別人的網站，而且只收 77 張卡** |
| 淨化的符號 | PokeMiners `Images/Rocket/ic_purified.png`（一次性，轉成 alpha 存 `img/purified-mark.png`） | 低 |
| 極巨化的符號 | Bulbapedia `File:GO_Dynamax_icon.png`（一次性，加粗後存成 `img/max-mark.png`） | **中，別人的網站** |
| 背卡的本地備援圖 | 自己的 `img/bg/`，17 張 | 無 |

`tools/build-dex.mjs` 離線解析前兩者產生 `js/godex.js`，
網站執行時不會抓這些檔案。GO 更新後重跑腳本即可。

**圖片原則上不鏡像進 repo**。3426 張約 75 MB，而且是 Niantic 素材，
進了 git 歷史要拿掉得改寫歷史。一律直接連外部 CDN。
例外是背卡的備援圖、`img/extra/` 那 5 張與極巨化符號 `img/max-mark.png`，
來源都沒有 CORS，不收進來就畫不進分享圖。合計不到 2 MB。

### 部署
- 線上網址：`https://jp-study-list.github.io/poke-change/`
- repo：`JP-study-list/poke-change`，公開
- GitHub Pages **Source：`main` 根目錄**（不是 GitHub Actions）
  - 這個專案的圖片不鏡像，沒有建置流程，所以不需要 Actions
  - push 即生效，不用等
- 之後：轉私人 repo + Cloudflare Pages + 自有網域。
  轉私人後免費方案的 Pages 會停掉，所以兩件事要一起做

### 已知地雷（本專案特有）

- **`.g2` 是同一個條目的新版渲染**，不是不同條目。解析圖檔名時要去掉，
  否則每個裝扮都會變成兩筆。

- **裝扮在上游有兩套命名**：`.cCODE` 與 `.fCODE`。同一個活動有時歸型態、
  有時歸裝扮，所以合併重複時不能只看其中一種。

- **`_NOEVOLVE` 是遊戲機制不是外觀**（穿了不能進化）。同一個裝扮可能同時有
  `X` 與 `X_NOEVOLVE` 兩個代碼，圖鑑要合併成一筆，否則會出現兩張一樣的卡。

- **基格爾德的 `COMPLETE_` 前綴也是遊戲機制**（細胞收集滿了可以變身）。
  `COMPLETE_FIFTY_PERCENT` 跟 `FIFTY_PERCENT` 的圖檔逐位元組相同、
  基礎數值也相同，10% 那兩筆同理，跟 `_NOEVOLVE` 同一類，2026-09-12 合併掉。
  併掉的是帶前綴的那兩個 id，留下比較短的。
  規則在 `tools/build-dex.mjs` 的 `SAME_LOOK`，**一筆一筆指名不要規則化**：
  `COMPLETE` 自己是獨立的形態（完全體），寫成「去掉 COMPLETE_ 前綴」會把它也吃掉。
  順帶一提語言檔只給了帶前綴那兩個的中日文，留下來的三個查不到，
  譯名在 `FORM_OVERRIDE` 自己指定，數字沿用上游的全形寫法。

- **極巨化與超極巨化都是交換條件，不是條目**（2026-09-16，使用者要求）。
  兩個都跟異色／XXL／XXS／背卡並列，存成 `item.max` 與 `item.gmax`。

  **超極巨化當過半天的條目，被收回來了。** 當時的理由是它外觀真的變了、
  上游也有自己的圖；收回的理由是使用者看到圖鑑裡多出一排
  「妙蛙花 超極巨化」——一個物種本來就有本體加好幾個裝扮，
  再多一格排擠掉的是別隻寶可夢。**圖鑑的格子是稀缺資源，旗標不是。**

  **外觀沒有因此丟掉**：那 19 種的圖掛到本體的 `gmaxIcon`／`gmaxShinyIcon`
  （20 個條目，顫弦蠑螈與武道熊師各兩型），勾起來就換圖，
  跟異色同一個機制（`iconAttrs(e, shiny, gmax)`）。
  所以「不佔格子」與「看得出外觀」兩件事同時成立，不必二選一。

  超級進化、**無極巨化**（Eternamax，無極汰那那個，不是一般的極巨化）
  與原始回歸仍然整批排除：它們連掛圖的位置都不需要，
  沒有人會想在交換清單上標「這隻可以超級進化」。

- **兩者互斥**。超極巨化本來就蘊含極巨化，兩個都亮沒有意義，
  而且格子右上角只放得下一顆徽章。點一個會關掉另一個，
  `main.js` 的草稿與 `setField` 兩條路都要做，`store.cleanItem` 也收斂一次
  （localStorage 使用者改得到）。

- **符號用官方那張圖，不要自己畫**（`img/max-mark.png`，2026-09-16）。
  PokeMiners **沒有**收那顆符號（搜過 48319 個檔案，只有極巨粒子的道具罐
  `mp_pack.png` 與貼紙包佔位圖），但 **Bulbapedia 有**
  （`File:GO_Dynamax_icon.png`）。
  手工描過一版 SVG path，形狀差很多就是差很多：官方那顆是**空心輪廓**，
  上方分岔成尖角、下方兩條帶子張開、右下還有平行線紋理，照截圖畫不出來。

  **細線條一定要先加粗再縮**。原圖 185×185、線條約 10px，直接縮到畫面上的
  20px 會剩不到 1 像素，抗鋸齒一稀釋就糊成一團淡色（實測 alpha>150 的
  只剩 10%，分享圖上幾乎看不到）。`tools/make-max-mark.mjs` 做形態學膨脹
  再降採樣到 96×96，在資產層解決一次，畫面與 canvas 都受益。

  **收進 repo 的理由跟 img/bg、img/extra 一樣**：來源沒有 CORS 標頭，
  不收進來就畫不進分享圖。2.7 KB。

  第一版做成洋紅圓底配白符號，**跟交換表的刪除鈕撞臉**——那顆是右上角的
  紅圓白叉，兩個擺在同一個角落一眼分不出來。改成無底的符號直接疊在圖上，
  這也才是遊戲裡的樣子：官方只在篩選標籤上用圓底。

- **每一種條件鈕都要有自己的底色**。漏掉不會報錯，會變成**整顆鈕隱形**：
  `.mk[aria-pressed="true"]` 把字轉白、邊框轉透明，沒有底色就是白字畫在
  白面板上。超極巨化剛加進來時就漏了，使用者回報「按下去按鈕會不見」
  才發現——量測與截圖都沒抓到，因為 DOM 裡那顆鈕好好的。
  check 的「2b2. 條件鈕的底色」讀樣式表比對，補這個洞。

- **符號有兩組色，用途不同**。`--max`／`--gmax` 是條件鈕的實心底（配白字），
  `--max-mark`／`--gmax-mark` 是疊在寶可夢圖上那顆符號（粉紅與紫）。
  淡色不能拿來當實心底，白字會看不清；深色也不該拿來畫符號，
  那跟刪除鈕的紅太近。符號一律帶白描邊（`paint-order: stroke`），
  背卡底圖有亮有暗，不描會在某些卡上整個糊掉——跟異色星星同一個理由。

- **能極巨化的名單要另外產生**（`js/maxdata.js`，`tools/build-max.mjs`）。
  **game master 單獨給不出這份名單**，兩個看起來像的欄位都不能全信：
  `BREAD_POKEMON_SCALING_SETTINGS` 只有 58 個物種，那是「視覺縮放要特別調」
  的清單；`pokemonSettings.breadTierGroup` 更糟，2467 筆幾乎全都有，
  那是預先排好的強度分級，跟實裝與否無關。
  所以主來源是 Dittobase（每筆帶 `isDynamax` 與 `isGigantamax`），
  **再跟 game master 的 `BREAD_MODE` 取聯集**——那份不完整，但裡面的是真的，
  Dittobase 漏掉的幾何雪花、投擲猴與毒電嬰就是它補回來的（2026-09-16）。
  **遊戲資料裡 Max Battle 叫 bread、超極巨化叫 sourdough**，搜 dynamax 找不到。

- **報告裡「只有 game master 有的」那幾筆不要當成雜訊**。第一版把它們判成
  還沒實裝而排除，結果其中兩隻出現在 GO Fest 2025 那張 Max Battle 背卡的
  清單裡（74 隻有 68 隻可極巨化，那就是一張 Max Battle 的卡），
  使用者確認三隻都真的能極巨化。**下次多出新的要回頭問過，不要默默排除。**

- **背卡不必為超極巨化做任何事**。它現在是掛在本體上的旗標，
  本體的背卡清單本來就在，「超極巨化的噴火龍配 GO Fest 背卡」
  自然就組得出來。`cardsFor()` 曾經為了條目形態加過一段退回本體的邏輯
  （`GMAX_BASE`），條目收回之後那段已經刪掉，**不要再加回來**。
  順帶一提那次查證留下一個有用的事實：背卡清單來自 Dittobase，
  而它**不收任何特殊型態**，Max Battle 抓到的一律記成本體 id。

- **勾了條件，背卡只留配得上的那幾隻**（2026-09-16 起，2026-09-17 改成逐隻）。
  極巨化只能從 Max Battle 抓到、淨化只能從火箭隊或暗影團戰抓到，
  所以身上那張卡一定是那些場合給的；人孔蓋、職棒那些野生活動的卡
  跟它們互斥，列出來的每一張都是遊戲裡湊不出來的組合。

  **粒度是逐隻，不是整張卡。** 第一版（1.08.03）是卡片層級的白名單
  `MAX_BATTLE_CARDS`，四張 GO Fest 2025，整張放行或整張濾掉。
  那個粒度就是錯的：隊長那三張卡各有一百多筆，其中**只有 6 隻**能極巨化
  （妙蛙種子、小火龍、傑尼龜、敲音猴、炎兔兒、淚眼蜥）。
  整張放行會多出九十幾隻，整張濾掉會少掉那 6 隻——使用者當初回報的
  「妙蛙花勾了極巨化還列出隊長卡」，兩種做法都答不對，
  因為能極巨化的是妙蛙**種子**不是妙蛙花。同一張卡、同一個編號、
  不同條目，答案相反。

  資料在 `js/bgflags.js`（`tools/build-bgflags.mjs` 產生），
  查詢走 `cardAllows(卡, 條目, 種類)`。`MAX_BATTLE_CARDS` 與 `isMaxBattle`
  已經不存在，不要再找。

- **背卡旗標的來源是 Bulbapedia，這是唯一有這份資料的地方**。
  它的 Background (GO) 逐隻標了角標，圖例寫得很白：標了暗影或極巨化
  圖示的那一隻「can be obtained with the corresponding background as a
  Shadow Pokémon or a Dynamax Pokémon」。超極巨化則是獨立的圖
  （`3GMax Venusaur` 那種），有那張圖就表示這張卡配得上。

  **所以「Bulbapedia 只比對不進資料」這條在這一份破例了。** 理由是
  Dittobase 的卡片層級只有 `shiny` 與 `evolve` 兩個旗標，
  game master 完全沒有「背卡與交換條件的關係」這種東西。
  **比例也推不出來**：229 張有清單的卡裡 134 張比例是 100%，
  但那些卡的清單中位長度只有 2 筆，全是御三家與皮卡丘這種熱門物種的
  小卡，100% 是巧合不是訊號。

  **代價：沒標記不等於不行。** Bulbapedia 只收錄 77 張卡，我們有 240 張，
  沒收錄的卡一律沒有旗標，勾了條件就一張都不剩。它連有收錄的卡也不見得
  列全——長崎那張我們有 23 筆，它只列 4 隻。所以這份資料**只用來過濾
  畫面上列出什麼**，絕不拿去洗紀錄。

- **卡片對照是人工的，指名前要逐張比對清單內容**。Bulbapedia 用自己的
  命名（`Wild_Area` 指的是 2024 福岡那張特殊背景，不是 2025 的曠野地帶），
  跟上游檔名與 Dittobase 都接不起來。表在 `tools/build-bgflags.mjs`
  的 `CARD_MANUAL`，目前 12 張。**不要照名字猜**——build-bg 踩過這個坑
  （巴黎兩張的編號是交叉的）。型態後綴同理走 `SUFFIX`，
  `37A` 是阿羅拉六尾、`849A` 是高調顫弦蠑螈，同一個字母兩個意思。

  **雷吉那張（`go-fest-2025`）本來就沒有旗標**：那六隻是 2025 年 6 月
  Ancients Recovered 的五星團戰陣容，背卡來自團戰捕捉。

  **順帶推翻了一個推論**：巴黎那張當初收進白名單的理由是「多了超極巨化
  噴火龍那一路」，那是看我們自己的清單有小火龍一家、而噴火龍能超極巨化
  推出來的。Bulbapedia 標的是超極巨化**千面避役**（GOFESTMAX 兌換碼的
  限時研究），小火龍一家在那張卡上是一般個體。推論輸給資料。

- **旗標的條目必須真的在那張卡的清單裡，而且名單要同意它做得到**。
  兩條 check 盯著。前者不對就是死資料（`cardsFor` 從卡的清單找條目，
  清單裡沒有的永遠查不到）；後者不對表示兩個來源有一邊過期了——
  **1.09.00 就是靠後面那條抓出三隻漏掉的極巨化**，見下面那條。

  **裝扮不掛旗標**，即使它是卡上唯一的同編號條目。隊長那三張卡踩過：
  Bulbapedia 標了尼多力諾可以是暗影，而我們那張卡的清單（來自 Dittobase）
  只有「尼多力諾（2020 新年）」。裝扮一律勾不到這些條件，掛上去是一筆
  永遠查不到的死資料，還會讓下次讀到的人以為裝扮可以勾。

- **過濾是畫面層的事，不洗使用者的紀錄**。`cardsFor(id, { purified, max,
  gmax })` 只影響列出什麼，存進去的 `bg` 一律留著——旗標只會長，
  拿它去清掉人家存好的背卡等於默默改資料，跟 `normalize` 不拿極巨化
  名單洗紀錄同一個道理。真正會動到資料的只有使用者自己按下那顆鈕的
  當下（`setField` 與草稿兩條路都做），那時把配不上的那張退回「不指定」。

  例外是 `opts.keep`：**自己已經選著的那張一律留在選項裡**，即使它不合。
  濾掉的話下拉會顯示「不指定」、格子上卻還畫著那張卡，而且再也點不到、
  改不掉。能看見才能修正，寧可多列一張。
  **1.09.00 之後這個出口更重要了**：旗標只涵蓋 12 張卡，
  在那之前用白名單存下的組合大多會變成不合。

- **交換表那份背卡下拉要逐筆算**。同一隻可以收兩筆，一筆極巨化一筆不是，
  共用一份清單會串味。`cards` 因此從 `editBlock` 搬進 `block()`。

- **一張都不剩時仍然畫出背卡區塊**，只留「不指定」那一列。
  換成「目前沒有活動背卡」會被讀成「這隻寶可夢沒有背卡」，
  但它其實有，只是條件配不上——那句話留給真的沒有背卡的條目。

- **超極巨化御三家的背卡查到了，但補不進去**（2026-09-17）。
  GOFESTMAX 兌換碼的限時研究給的超極巨化御三家（轟擂金剛猩／閃焰王牌／
  千面避役）帶的就是 **Delightful Days** 那張季節背卡，GO Fest 2025
  實體場三張也各帶一隻。Bulbapedia 標得清清楚楚。
  **卡在我們的卡片清單沒有牠們**——那份來自 Dittobase，而它不收超極巨化。
  要補得先把牠們加進 `js/bgevents.js` 的清單，而且**逐隻補不是整張卡**，
  那張季節卡上其他寶可夢是野生的。同一種缺口還有拉普拉斯、卡比獸與
  長毛巨魔，全部列在 `tools/bgflags-report.md` 的「對不到條目」。

- **淨化是交換條件，暗影不是**（2026-09-17，使用者要求）。
  暗影寶可夢在 GO 裡**根本不能交換**，淨化之後才能，所以對一份交換清單
  來說「暗影」這個選項沒有意義。名單（`js/shadowdata.js`，480 筆）
  其實是「有沒有暗影版」——能變成暗影的，淨化之後就是淨化版。

  **三個條件互斥**：淨化、極巨化、超極巨化。超極巨化本來就蘊含極巨化；
  淨化跟那兩個在遊戲裡湊不出來，因為暗影寶可夢不能參加 Max Battle，
  而極巨化只能從那裡抓到。而且格子右上角只放得下一顆徽章。
  名單是 `main.js` 的 `EXCLUSIVE`，三條路都吃它：草稿、`setField`、
  `store.cleanItem`（localStorage 使用者改得到）。

- **淨化名單的主來源是 Dittobase，game master 只當佐證**。
  這跟極巨化那邊相反：`BREAD_MODE` 不完整但裡面的是真的，
  而 `pokemonSettings.shadow` 有 1015 筆、**明顯過度包含**——
  連「2019 秋季妙蛙種子」這種裝扮都有一整組淨化費用，
  而遊戲裡從來沒有出現過暗影裝扮。跟 `breadTierGroup` 同一類。
  所以**不取聯集**，差異一律寫進報告。

  比對兩邊的物種代號**要先正規化**：game master 用底線（`ho_oh`、
  `nidoran_female`），Dittobase 用連字號（`ho-oh`、`nidoran-f`）。
  不正規化的話報告會列出一整排假差異，下次的人得白查一輪才發現
  兩邊講的是同一隻。

- **尼多蘭兩筆要指名**。牠們的英文名帶性別符號（`Nidoran♀`），
  `slugify` 會把符號整個去掉變成 `nidoran`，而 Dittobase 寫成
  `nidoran-f` / `nidoran-m`。走 `build-shadow.mjs` 的 `FORM_ALIAS`——
  改 `slugify` 會牽動 build-bg 與 build-max 那兩份同源的複本。

- **騎拉帝納的淨化掛在型態上不是本體**。圖鑑裡 `d487`（本體）、
  `d487.fALTERED`（別種形態）、`d487.fORIGIN` 三個都在，
  Dittobase 的暗影是 `giratina-altered-shadow`，所以名單收的是
  `d487.fALTERED`。**這跟背卡的慣例一致**——背卡清單也只用
  `d487.fALTERED` 與 `d487.fORIGIN`，本體那筆沒人用。
  全圖鑑只有這一個編號是「本體不在名單、型態在」，不必特別處理。

- **淨化的符號是另一張圖，不是換顏色**（`img/purified-mark.png`）。
  極巨化與超極巨化共用同一張圖只換顏色（遊戲裡也是這樣），
  淨化那顆是青色星芒，形狀完全不同，所以 class 是 `purb` 不是 `maxb`。
  寫錯 class 的話 CSS 那條 mask 對不上，畫面上會變成一個看不見的方塊。
  check 有一條盯著。

  **來源是 PokeMiners 的 `Images/Rocket/ic_purified.png`**，
  **不要拿旁邊的 `ic_purified_filter.png`**——那是篩選標籤用的圓底版，
  跟交換表的刪除鈕撞臉，極巨化那顆已經踩過這個坑。

  **不膨脹**。跟極巨化那顆不同，這顆是實心星芒不是空心輪廓：
  縮到畫面上的 20px 之後 alpha>150 的還有 33.5%，極巨化那顆膨脹完
  才 28.5%。再加粗只會把中間的縫糊掉。輸出 64×64 等於原尺寸，
  放大只會變糊。

- **官方那個青色不能直接拿來用**。`ic_purified.png` 是
  rgb(180, 237, 240)，當條件鈕的實心底時白字只有 1.3:1，整顆鈕會糊掉。
  所以畫面上是另外挑的兩個值：`--pur` #0f7d8c（白字 4.85:1，
  對照 `--max` 的 4.79）、`--pur-mark` #1aa8bd（2.85 / 6.69，
  對照 `--max-mark` 的 2.77 / 6.86）。深色一樣不覆寫，
  跟 `--max` 同一個道理。`js/share.js` 的 `LIGHT`／`DARK` 兩邊都要有。

- **三隻漏掉的極巨化，是背卡旗標抓出來的**（2026-09-17）。
  壺壺、勾魂眼與吼吼鯨：Dittobase 連 `shuckle-dynamax` 這種條目都沒有，
  game master 的 BREAD 縮放清單（70 個物種）也沒有，**兩邊一起漏**，
  所以名單少了它們。是 check 的「旗標跟條件名單一致」失敗才發現的——
  Bulbapedia 標了 Dark Skies 那張卡上這三隻可以極巨化。
  **pokemon.com 的 Dark Skies 陣容表寫得明明白白**：8/18 有
  Dynamax Shuckle 與 Dynamax Sableye，8/21–22 有 Wailmer。
  補在 `build-max.mjs` 的 `MANUAL_MAX`，一筆一筆指名，之後上游補上就能刪。

  **這是同一個坑的第二次**。上一輪是幾何雪花、投擲猴與毒電嬰，
  第一版判成還沒實裝而排除，後來發現它們就在 Max Battle 背卡的清單裡。
  那時留下的規矩是「下次多出新的要回頭問過，不要默默排除」——
  這次就是靠那條規矩抓到的，**下次還是一樣**。

- **PNG 編解碼抽在 `tools/png.mjs`**，`make-max-mark` 與
  `make-purified-mark` 共用。只處理 8-bit RGBA，不是通用函式庫。
  抽出來的時候**重跑 make-max-mark 驗過產出的圖位元組相同**
  （md5 `bff07a18…` 不變）——動到既有資產的重構一定要這樣驗一次。

- **名單是物種層級，裝扮一律不給勾**。Dittobase 那 142 筆只有 5 筆帶型態
  （達摩狒狒、顫弦蠑螈兩型、武道熊師兩型），一個裝扮都沒有——
  Max Battle 抓到的不會是裝扮版。`entryKeyMap()` 跳過 `kind === "costume"`，
  check 有一條盯著。

- **超極巨化不需要自己的 CP**。它不是條目，顯示的一直是本體那組數字，
  而這是對的：極巨化在 GO 裡加的是血量倍率與招式，不動基礎數值。
  當條目的那半天曾經在 `STATS_SAME_AS` 塞了 13 筆指名，收回時一起刪了。

- **超極巨化的名單就是「圖鑑裡有 `gmaxIcon` 的條目」**（20 個）。
  這樣名單與「勾了會換圖」永遠一致——勾得到卻沒有任何反應是最難解釋的
  狀態，比少一隻還糟。`gmaxIcon` 由 build-dex 掛上，條件是
  game master 的 `allowedSourdoughPokemon` 說這個型態可以、而且上游真的有圖。

- **上游的寶可夢圖有兩個目錄，超極巨化的進度不一樣**。
  `Images/Pokemon/Addressable Assets` 只有 13 種，
  **`Images/Pokemon - 256x256/Addressable Assets` 有 19 種**——
  皮卡丘、喵喵、灰塵山、長毛巨魔與武道熊師只在後者。
  第一次查「那四隻沒圖」是只看了主目錄，結論錯了。
  兩個目錄的差別是裁切：主目錄緊貼主體（佔畫布 98%），256 那份是固定畫布、
  四周有留白（83~100%），所以用 256 的條目要帶 `gmax256` 與逐張量的
  `gmaxFill`／`gmaxOffX`／`gmaxOffY`，不修正會比旁邊的小一號還偏位。
  數字在 `tools/build-dex.mjs` 的 `GMAX_256`。

- **武道熊師的超極巨化上游叫 `fBREAD_DOUGH_MODE`，不是 `GIGANTAMAX`**
  （`_2` 是連擊流，對照 game master 的 `allowedSourdoughPokemon`）。
  只認 GIGANTAMAX 的話它會靜默消失，所以那兩筆在 `GMAX_256` 一筆一筆指名。

- **掛超極巨化的圖要看型態，不能「同編號就掛」**。喵喵有阿羅拉與伽勒爾兩個
  地區型，它們不能超極巨化，掛了就會長出一顆不該有的鈕；顫弦蠑螈相反，
  高調與低調都能，所以也不能只掛本體。粒度看 game master 的
  `allowedSourdoughPokemon`，它給的正好是型態層級。check 有兩條盯著。

- **`item.max` 與 `item.gmax` 沒有升儲存版本號**。多兩個布林欄位對舊資料
  是相容的，`cleanItem` 讀到舊紀錄補 `false`；升 `v` 反而會讓舊版整包讀不到。
  代價是新版存的那兩個給舊版讀會被洗掉，跟 v1 到 v2 一樣單向，
  但只丟旗標。check 有一條驗「舊紀錄沒有 max 欄位時補 false」。

- **`normalize` 不拿名單去洗紀錄**。名單會隨 GO 更新縮水，
  拿它去清掉使用者已經勾好的 `max`，等於默默改掉人家存的東西。
  勾選框出不出現是畫面層的事（`canMax`），存進去的值一律留著。

- **符號在右上角，刪除鈕也在**。所以 hover 與編輯模式時符號 `opacity: 0`
  讓位（`.want-cell:hover .maxb`）——那一角一次只給一個東西，
  而刪除是暫時的操作狀態，讓掉不影響平常看得到。
  符號不放進格子下方那行尺寸文字：「極巨化」三個字比 XXL 長太多，
  手機 68px 的格子排不下。

- **`--max` 深色刻意不覆寫**。`#da0d86` 是 GO 自己的洋紅，實心填色配白字
  4.8:1，兩套底色都過 AA；調亮到 `#e83fa0` 白字只剩 3.7:1。
  跟 `--x-bg` 同一個道理，填色不必跟著文字調亮。`js/share.js` 的
  `LIGHT`／`DARK` 兩邊都要有這個值，canvas 吃不到 CSS 變數。

- **極巨化不做批次**。多選那條路只有異色能整批帶，理由跟 XXL／XXS 一樣：
  不會一批十隻都要極巨化。所以 `addMany` 的重複判斷比的是 `!x.max`，
  拿實際會寫進去的值比，這條踩過一次了。

- **上游大小寫會不一致**（出現過 `fMay_2023` 與 `fMAY_2023`），
  代碼一律轉大寫再比對。

- **型態與裝扮可以並存**（例如南瓜精四種尺寸各有萬聖節版），
  組名稱時兩個標籤都要留，否則四筆會變成一模一樣的「2022 萬聖節」。

- **異色是否實裝，game master 沒有旗標**。靠「有沒有異色圖檔」判斷。
  實測熊徒弟、武道熊師、眷戀雲確實沒有異色圖，與已知情況相符。

- **超級進化、極巨化與原始回歸不是可交換條目**，是戰鬥中的暫時狀態，
  產生時整批排除。原始回歸是 2026-09-12 才補進這條的，它跟超級進化同一類，
  而且 game master 連基礎數值都沒給它。
  超極巨化 2026-09-16 放行過半天又收回來了，見上面極巨化那幾條——
  它的圖有用到，但用法是掛在本體上，不是變成條目。

- **屬性索引要收得到含物種前綴的型態代碼**。條目的 form 是從圖檔名抽的，
  上游有時寫 `WORMADAM_TRASH` 有時寫 `HISUIAN`。索引只收去前綴那種的話，
  含前綴的會查不到而**靜默退回本體**，屬性看起來很正常其實是別的型態的。
  結草貴婦三種蓑衣曾經全掛本體的蟲加草，實際上砂土是蟲加地面、垃圾是蟲加鋼。
  旁邊的 CP 索引早就兩種寫法都收，所以那時只有屬性錯、CP 是對的——
  改動一個索引時記得另一個也要跟上。

- **上游有圖不等於 GO 實裝了**。圖檔會比遊戲早幾個月出現，抽圖檔名產生的
  條目就會混進還不能取得的東西，屬性退回本體、CP 算不出來。
  單一型態用 `tools/build-dex.mjs` 的 `HIDDEN_FORMS`（key 是 `編號|型態`），
  整隻沒實裝的用 `HIDDEN_DEX`。兩個是不同的東西：前者本體要留著。

- **阿爾宙斯暫時隱藏**（2026-09-12，使用者要求）。GO 還沒實裝，上游只有圖，
  18 個屬性型態的數值一模一樣、多半沒有官方譯名，畫面上是一整排
  「Bug-Type form」。名單在 `tools/build-dex.mjs` 的 `HIDDEN_DEX`，
  實裝之後刪掉編號重跑就會回來，id 不會變。

- **洗翠黏美兒與黏美龍暫時隱藏**（2026-09-12）。game master 完全沒有這兩個
  型態，14 個洗翠型態就少它們兩個，所以屬性退回本體的龍、CP 算不出來。
  **2026-11 的 GO Wild Area 才首次登場**（仙台與墨西哥城 11/6–11/8，
  隔週全球）。名單在 `HIDDEN_FORMS`，實裝後刪掉重跑，屬性會變成鋼加龍、
  CP 也會有，id 不變。check 有兩條盯著，實裝後那兩條要改成驗屬性。

- **裝扮沒有官方名稱**。遊戲內只顯示物種名，語言檔查不到。
  譯名全部在 `js/costumes.js` 自己維護，原則是看圖命名、看得出是哪一個，
  不必去猜代碼原本的含意。`fS` 那類沒有譯名的型態同理，
  改 `tools/build-dex.mjs` 的 `FORM_OVERRIDE`。

- **捷拉奧拉與纏紅鶴上游沒有 GO 圖示**（`pm807` 與 `pm973` 的 icon 都 404），
  在 `js/extra.js` 手動補，用官方立繪。纏紅鶴是背卡帶出來的：
  嘉年華那兩張的清單只有牠一隻，圖鑑沒收那兩張就是空的。

- **外部圖沒有 CORS 標頭就不能畫進分享圖**。畫面上的 `<img>` 不受影響，
  但 canvas 的 `crossOrigin="anonymous"` 會載入失敗而退回備援。
  Dittobase 的 CDN 就是這種，所以那 5 張改成鏡像進 `img/extra/`。
  Choggor 與 PokeMiners 走 raw.githubusercontent，有 `access-control-allow-origin: *`，沒這個問題。

- **`js/extra.js` 補的條目會跟 godex 搶同一個裝扮**。上游補上之後
  兩邊都有，圖鑑就會出現兩張一樣的卡，而且是兩個不同的 id。
  `extraEntries()` 會擋掉，比對規則是代碼轉小寫、底線換連字號、去掉
  `_NOEVOLVE`，命名不同的走 `ALIAS`。`check.mjs` 會提醒哪幾筆可以刪了。

- **世代靠圖鑑編號的區間判斷**，game master 沒有這個欄位。
  標籤走地區名不走世代編號。注意這跟種類裡的「地區型」是兩回事，
  後者指的是阿羅拉的樣子那種型態，容易混淆。

- **IV100 的 CP 不能退回本體的數值**。`build-dex.mjs` 查屬性時查不到會退回
  本體（`dex|null`），那對屬性沒問題；CP 照做會出事：原始回歸與洗翠黏美龍在
  game master 裡沒有自己的基礎數值，退回本體會得到一個看起來很像真的、
  其實是別隻的數字。所以 CP 另外建一份索引，型態一定要有自己的數值，
  例外走 `STATS_SAME_AS` 一筆一筆指名。**目前 1460 筆全部都有 CP**，
  原始回歸整批排除，洗翠黏美兒與黏美龍暫時隱藏。
  check 有一條盯著這件事，之後又冒出沒有 CP 的就是混進了沒有遊戲資料的型態。

- **CP 的等級倍率取自 game master**（`PLAYER_LEVEL_SETTINGS`），不要自己抄一份。
  LV20 是 0.5974、LV25 是 0.667934、LV50 是 0.8403。天氣加成的 +5 級也是
  遊戲資料寫的（`WEATHER_BONUS_SETTINGS` 的 `raidEncounterCpBaseLevelBonus`）。

- **舊專案手寫的 CP 有五筆是錯的**。露奈雅拉、毒貝比、四顎針龍與熊徒弟兩筆，
  跟現在的 game master 對不上。2026-09-12 用本機快取、PokeMiners 線上最新與
  pogoapi.net 三邊比對過，三邊一致。要 CP 一律用算的，不要從舊站抄。

- **圖鑑只收 GO 實裝的**（2026-09-12，使用者確認）。上游有圖不等於遊戲裡有，
  game master 有設定也不等於。判斷一律看 `Images/Pokemon/Addressable Assets/`
  有沒有圖，那是這個專案一貫的做法，已知例外只有捷拉奧拉與纏紅鶴兩筆，
  在 `js/extra.js` 手動補。
  **`pogoapi.net` 的 `released_pokemon` 不可靠**，霏歐納、瑪納霏、毒電嬰、
  顫弦蠑螈與來悲茶明明有圖也早就實裝，它標成未實裝。拿它當佐證可以，
  不要拿它當移除的依據。
  2026-09-12 全面查過一次：game master 標了稀有度而圖鑑沒收的有 25 種，
  24 種上游完全沒有圖，剩下那種是刻意隱藏的阿爾宙斯。沒有漏。

- **稀有度在畫面上只有一類，叫「傳說」**（2026-09-12，使用者要求）。
  傳說、神話與究極異獸在 GO 裡的交換規則是同一套，都要特殊交換，
  分成三個選項對「這隻換不換得動」沒有幫助。
  **合併做在畫面層**：`dex.js` 的 `FILTER_GROUPS.rarity` 只剩一個選項，
  `ui.js` 的 `CLS_LABEL` 三個都指向 `clsLegendary`。
  條目的 `cls` 仍然保留 game master 的原貌，之後想分開不必重跑腳本。

- **篩選的計數要扣掉自己那一組**。算「火」有幾筆的時候，
  其他組的條件要照套，屬性這一組要放掉，否則同一組的選項
  加起來不會等於這一組全不選的結果，數字看起來就是錯的。

- **篩選收在漏斗裡，但已選條件不能一起收起來**。36 個選項攤在畫面上
  太吵，收進面板是對的；可是只在漏斗上留一個數字，使用者就看不出
  自己篩掉了什麼——2026-09-11 曾經因為這個理由把面板改成常駐 chip，
  隔天又改回漏斗。兩次都對，差別在已選的那幾顆有沒有留在外面。
  搜尋列右邊那排 `.fsel` 就是這個代價的補償，不要為了更乾淨而拿掉。

- **設定裡三個顯示選項是兩張預覽縮圖二選一**（2026-09-14，使用者要求，
  參考他自己另一個站的設定面板）。方框勾與圓形圖示開關都是過去式。
  三項講的都是畫面會長什麼樣，畫出來比寫出來好懂。縮圖在 `js/ui.js` 的
  `DISP_THUMBS`，`thumbCells()` 產生格子、`themeThumb()` 產生外觀那兩張。
  **是選值不是 toggle**：`data-val` 帶 0／1，點已經選中的那張不該把它關掉，
  否則會變成兩張都沒選。
  **外觀那兩張的顏色寫死**，不吃 CSS 變數——它們預覽的正是兩套配色本身，
  跟著當前主題走的話兩張會長得一模一樣。改配色時這裡要跟著改。
  **名稱那一項的兩張格子位置必須一樣**，只差有沒有那條線；照高度置中算
  會讓有線的那張整排上移，看起來像在比別的東西。
  **六張卡的框線一律 1px**，選中的只換顏色（`--ink` 對 `--line`），不加粗也不改
  `border-width`（2026-09-16，使用者要求）：粗細不同一眼就看得出來，而顏色
  已經把「誰被選中」講清楚了。

- **設定是置中的彈窗，而且放在 `<header>` 外面**（2026-09-12，使用者要求）。
  原本貼著齒輪往下展開，現在跟右欄的詳情同一種形態：遮罩、置中的殼、
  捲動區，右上角一顆紅色的 X。**一定要放在 header 外面**：`.topbar` 是
  sticky 帶 z-index，會開一個 stacking context，關在裡面的話 fixed 再高
  也蓋不過右欄那片 z-index 40 的遮罩。
  X 走 `data-closepop`，不跟右欄的 `data-close` 共用，那一顆會連詳情一起收掉。
  遮罩在 DOM 上就是 `#settings` 自己，所以「點在面板裡面就不關」這個判斷
  擋不掉它，`main.js` 另外認 `ev.target.id === "settings"`。
  **篩選面板沒有跟著改**，它仍然貼著漏斗浮出，見下一條。

- **篩選面板放在 `.searchbar` 裡面靠 absolute 浮著**，不是排在它下面。
  進了流排就會把搜尋框推窄，而且沒篩選時也佔著高度。
  900 以下同一段 DOM 改 fixed 貼底變抽屜，跟右欄同一個手法。

- **沒有側欄了**（2026-09-12 拆掉）。導覽在頂部列、900 以下貼底成 bar，
  顯示與資料兩區搬進右上角齒輪的設定面板。`.sidebar`、`.scrim`、
  漢堡鈕與 `setSidebar()` 全部不存在，不要再找。

- **浮出來的面板一次只開一個**。`state.pop` 是字串不是兩個布林，
  值只有 `null`、`"filter"`、`"settings"`。兩個各自一個開關的話，
  漏斗與齒輪可以同時打開，兩片面板在桌機會疊在一起。

- **手機的底部 bar 要留內容區的內距**。`.content` 在 900 以下補
  `calc(var(--tab-h) + env(safe-area-inset-bottom) + 24px)`，
  不然最後一排格子被 bar 蓋住，而且那是看不出原因的，因為捲到底也還在。

- **金色不能直接當文字**。`#b8860b` 對白底只有 3.25:1，低於 AA 的 4.5:1。
  文字與小元件用 `--gold-ink`（淺色 `#8a6508`、深色 `#cf9a1a`），
  `--gold` 只做大面積填色。`--dim` 同理，`#7d7870` 也不合格，已改成 `#6f6a62`。

- **深色是另一套值，不是把淺色反過來**。原本 `#16150f` 偏暖褐，
  寶可夢圖彩度高，暖底會跟圖打架，改成中性近黑 `#101010`。
  兩套要各自驗對比，不能從其中一套推另一套。

- **背卡底圖不套濾鏡，改用縮小寶可夢讓出空間**（2026-09-12，使用者要求）。
  原本壓到 `opacity: 0.5`（深色 0.42）好讓寶可夢站得出來，代價是背卡認不出來，
  而背卡是這個專案的差異化，看不清等於沒有。現在底圖原樣顯示，
  有背卡的格子裡寶可夢縮到 64%（`.want-bg + img` 的 18% 內距），
  **沒有背卡的格子維持原本大小**，那些格子沒有要讓給誰。
  分享圖同一套：`js/share.js` 拿掉 `globalAlpha`，`drawContain` 的比例
  改成有背卡 0.64、沒有 0.78，數字要跟 CSS 那邊對得上。
  異色星星在兩邊都有描邊，亮背卡與暗背卡上都還看得見。

- **交換表格子的 `.want-tile` 不能加回 `overflow: hidden`**。刪除鈕是紅圓白叉，
  圓心壓在圖框右上角、一半露在框外（2026-09-13，使用者要求），加回去就被裁掉。
  背卡底圖的圓角原本靠它裁，現在底圖自己帶一份 `border-radius`。
  紅色用 `--x-bg`，跟彈窗那顆關閉鈕同一個，全站只有一種紅圓白叉。
  **溢出只取一半**：格子間距只有 6~8px，更多就跨進隔壁格子，
  而且交換表的格子刻意不畫框，沒背卡的格子上那顆會懸在空白裡。

- **帶倍率的圖要 `pointer-events: none`**。extra 那批外部圖靠 CSS 變數做
  `transform: scale()`，實測 2.46 倍，元素框向上溢出 61px、向左 35px。
  溢出的那一圈是透明留白，看不見卻接得到點擊，會搶走隔壁格子的 hit-test。
  圖本身沒有互動，點擊一律由格子委派，所以整個關掉。規則寫在共用的
  `img[style*="--iz"]`，圖鑑的格子從來沒有裁切，那邊也是同一個坑。

- **手機的刪除鈕收在鉛筆底下**（2026-09-14，使用者要求；在那之前是整顆不放）。
  常駐兩百顆紅點不行，那跟「格子不畫框」是同一個理由；但完全沒有也不行，
  詳情面板那顆「移除」找不到。現在每一欄標題右邊一顆鉛筆，按下去那一欄的
  格子牆帶 `editing`，叉才出現，可以連著刪。
  **只切換自己那一欄**，兩欄各一個布林在 `state.edit`。
  **鉛筆只在 `@media (hover: none)` 顯示**：桌機滑鼠移過去就有叉了。
  **空的那一欄不畫鉛筆**，按了沒反應比沒有那顆鈕更難解釋。
  編輯模式下點格子照樣開詳情，只有叉是刪除——誤觸的代價差太多。

- **交換表格子裡的圖一定要 `position: absolute`**。`<img>` 是 inline 的替換元素，
  用 margin 撐留白會壓在行盒基線上，margin 越大整塊沉得越多：
  背卡那批是 18%、沒背卡的是 8%，差出 7px，同一排看起來一高一低，
  背卡像浮在空中（2026-09-14 修）。改用 `inset` 之後**元素框仍然等於圖框**，
  所以 extra 那批靠 translate 修偏移的圖不受影響，那個百分比是相對元素算的。

- **`hover` 這個媒體特性用 CDP 模擬不動**。`Emulation.setEmulatedMedia` 的
  `features` 設了 `hover: none` 也沒用，`matchMedia` 仍然回 false，
  而畫面看起來會「對」——因為沒送滑鼠事件時 `opacity` 本來就是 0。
  要真的驗 `@media (hover: none)` 得用啟動參數
  `--blink-settings=primaryHoverType=1,availableHoverTypes=1`，
  再用 `getComputedStyle` 量，不要只看截圖。

- **改了配色要同步 `js/share.js`**。canvas 吃不到 CSS 變數，
  `LIGHT` 與 `DARK` 是抄一份的，順序與命名刻意跟 `:root` 對齊，方便逐行比。

- **三個檢視的右欄一律彈出，沒有常駐形態了**（背卡 2026-09-14、
  圖鑑與交換表 2026-09-12，都是使用者要求）。常駐的話點一下就把內容區
  推窄：圖鑑的格子牆從 14 欄掉成 12 欄整片重排，背卡從 6 欄掉成 4 欄。
  **`body.rail-pop`、`rail-off`、`railHasContent()` 與 1200 那整段
  常駐樣式都不存在了，不要再找**。右欄平常 `display: none`，
  靠 `ui.openSheet()` 加 `is-open` 打開，三個檢視同一條路。
  背卡原本常駐是因為它沒點卡片時擺目前清單的摘要（`renderRailSummary`），
  那塊連同分享鈕一起拿掉了——分享圖的入口本來就在交換表裡。

- **背卡詳情的卡面圖要限高**（2026-09-16，使用者要求）。彈窗比它取代的
  常駐右欄寬（520 對 340），等比放大之後一張方形卡面就吃掉大半個視窗，
  收集格全被推到摺線以下。`.card-art` 限 38vh。
  **寬度是 `auto` 不是 `100%`**：寫 100% 再靠 `object-fit` 收的話留白留在
  元素框裡，圓角會跟圖錯開一圈。上游卡面實測一律 512×512。

- **彈出形態是置中的視窗，不是貼右緣的抽屜**（2026-09-12，使用者要求）。
  靠右的話左右留白不對稱，而且會蓋住頂部列右上角的語言與齒輪。
  置中之後兩邊留白一樣寬，頂部列完整露在遮罩底下。
  視覺沿用篩選與設定那個殼，一樣的圓角、框線與陰影。
  **900 以下也置中**（2026-09-12，使用者要求），不再從底部升起，
  只把左右留白從 48 縮到 28，小螢幕上留太多會吃掉可用寬度。
  代價是高度從整個視窗變成 84vh（手機 86vh），背卡多的條目要捲一下
  才看得到兩顆加入鈕。

- **彈窗的關閉鈕是右上角的紅色圓形 X**（2026-09-12，使用者要求）。
  `.rail-inner` 是不捲的殼、`#panel`（`.rail-scroll`）才是捲動區，
  X 疊在殼上所以捲動時一直留在原地。X 寫在 `index.html` 的骨架裡，
  三個面板自動都有，繪製函式一個字都不必改。
  **底下那顆文字關閉鈕（`.btn-close`）已經沒有了**，它是常駐形態專用的，
  在那裡「關閉」的意思是收掉這筆詳情回到摘要；現在關掉就是關掉。
  紅色用自己的 `--x-bg`，不共用 `--danger`：那個是給文字用的，
  深色底上被調亮成 `#e8837c`，拿來當實心圓的底色白叉會看不清楚。

- **右欄不擺交換表摘要**（圖鑑 2026-09-12、背卡 2026-09-14，都是使用者指定）。
  圖鑑就是圖鑑，旁邊不掛半張交換表；背卡那一份 2026-09-14 跟著常駐形態
  一起拿掉。交換表本身就是清單，更不必擺。
  不要為了「桌機右欄空著是浪費」再把它加回去——右欄現在沒點東西就不存在。

- **斷點只有一個**：900px（導覽掉到貼底的 bar、欄數講死）。
  1200 那個是右欄常駐用的，2026-09-14 拿掉常駐形態時一起刪了。

- **手機的欄數是講死的，不是算出來的**。`minmax` 推出來的欄數會跟著
  螢幕寬度飄，360px 的手機會掉成四欄。而且圖鑑沒有面板內距、交換表有，
  同樣寬度會算出不同結果。900px 以下直接 `repeat(var(--cell-cols), 1fr)`。

- **固定欄數要寫 `minmax(0, 1fr)`**。`1fr` 的下限是 `auto`，
  格子裡的名稱長一點就把整排撐開捲出容器。選寶可夢面板踩過這個。

- **v2 是單向的**。2026-09-11 之前的 v1 是單獨一份，want／have 掛在最外層，
  讀到那種形狀會整個包成第一份，另外兩份留空，使用者不必做任何事。
  反過來不行：寫成 v2 之後舊版程式讀這個 key 只會看到空清單。
  只有真的改動才會寫回，光是打開網站不會升級。

- **匯出是三份包成一個檔**。匯入時整包就整包換掉，
  舊版匯出的單份只蓋掉目前在看的那一份，不能讓一個舊檔案把另外兩份清掉。

- **友情碼存偏好不存清單**。它是使用者的身分不是清單的屬性，
  換一份清單不該要重打一次，清空全部也不該把它洗掉。

- **選寶可夢面板有自己的一份篩選，跟圖鑑那份分開**（2026-09-13，使用者要求）。
  在圖鑑篩了只看傳說，按加號看到一片空白是找不出原因的。
  狀態在 `state.pickFilter` 與 `state.pickMulti`，**刻意放在 `state.pick` 外面**：
  那個物件關一次面板就沒了，記憶會跟著消失。跟圖鑑的篩選一樣不寫進偏好，
  重新整理回到預設。
  **兩份篩選的 dataset 名字不能一樣**：都叫 `data-group` 的話 `main.js` 的
  委派兩邊都接到，在加號的面板裡篩一下圖鑑的格子牆也跟著變。
  面板用 `data-pgroup` / `data-popt` / `data-pdrop`，產生 HTML 的
  `pickedChips` 與 `filterGroups` 共用，屬性名由呼叫端給。

- **改篩選與打字不清掉多選的選取，但差額要講出來**（2026-09-16，使用者拍板）。
  先篩火選兩隻、再篩水選三隻、一次加完，是這個面板本來就該支援的用法；
  一批只畫 `PICK_STEP` 200 筆，本來就得靠篩選縮小範圍才找得到東西，
  「改篩選就清掉選取」等於逼使用者一次只能選同一類。
  代價是那幾隻的勾跟著格子從畫面上消失，底部卻仍然算在數字裡——
  **不交代就是「只看到兩個勾卻加進來五筆」，而且沒有任何線索**。
  所以底部補 `t("pickHidden", n)`：`ui.js` 的 `countHidden(sel, shown)` 算差額，
  匯出的 `pickHidden(pick)` 給 `main.js` 那兩條只重畫底部一列的路用
  （手上沒有 `shown`，在那裡重算一次，那兩個時機都不常按）。
  **「看不到」包含還沒接出來的那幾批**，不只被篩選濾掉的，
  對使用者來說是同一件事：格子不在畫面上。所以 `pickHidden` 要吃
  `pick.shown`（目前接到第幾筆），拿 `PICK_STEP` 去算的話，
  接出來之後那句話會賴著不走。
  **英文那句自己帶前導空格**，中日文靠全形括號自帶間隔。
  手機上這句會讓底部從一行變兩行（66→107px），但只在真的有藏起來的
  選取時才長高，平常不佔空間。

- **選寶可夢面板是分段畫的，捲到底接下一批**（2026-09-18，使用者要求）。
  一批 `PICK_STEP` 200 筆，接到全部 1460 為止。在那之前是**硬上限 150 筆**，
  後面的除非打字或篩選否則永遠叫不出來。

  **那個上限站不住，量過才發現**：桌機畫全部 1460 筆是 17ms（150 筆 3.9ms），
  而且**圖鑑檢視本來就一次全畫、沒有任何上限**，同一個站自己打臉。
  真正的問題是捲動高度 28711px（約 96 個手機螢幕）。分段兩件事一起解決：
  開啟只付第一批的錢，後面的叫得出來。

  **接一定要 append，不能重畫**。`#panel` 是捲動容器，換掉 innerHTML 會把
  scrollTop 歸零——而使用者正在往下捲，這是最糟的時機。所以 `growPicker`
  只做三件事：新的幾格 `insertAdjacentHTML` 到格子牆後面、換掉哨兵那一行、
  更新底部的數字。這跟「多選點一格不能重畫整片」是同一個坑。

  **哨兵是底部那行「還有 N 筆」**（`data-pickrest`）。它不在就表示接完了，
  main.js 靠元素在不在判斷要不要再接，**不自己算**——那一行本來就由
  `growPicker` 維護，兩邊各算一次遲早會講不同的話。

  **scroll 不冒泡**，所以監聽用 capture 掛在 `document` 上，再認
  `ev.target.id === "panel"`。這樣仍然是單一委派，不必替面板綁一顆自己的
  監聽、也不必在關面板時解掉。距底 400px 就接，不等真的碰到底——
  看到空白才開始長就已經晚了。

  **整片重畫一定回到第一批**。打字、改篩選、切多選走的都是 `drawDetail()`，
  而那時 scrollTop 本來就歸零，留著長出來的筆數只是在看不到的地方畫格子。
  重置寫在 `drawDetail()` 裡而不是每個呼叫端各寫一次，那有五條路，會漏。

  **`growPicker` 的回傳值要夾到命中數**。一批是 200，但這次搜尋可能只有
  105 筆；不夾的話已經畫完時會回一個比實際格子還大的數字，
  寫進 `shown` 之後「N 隻在篩選外」就算錯。

- **那個面板的篩選表排在流排裡展開，不浮出來**。`#panel` 本身是捲動容器，
  浮層會被裁掉；而且那是直向清單，展開只是把格子牆往下推，
  不像搜尋列那樣會擠窄誰。也**不走 `state.pop`**，那個字串管的是頂部列的
  漏斗與齒輪，混用會變成「在這裡點篩選就把設定關掉」。

- **多選點一格不能重畫整片**。`#panel` 換掉 innerHTML 會把 scrollTop 歸零，
  選到第七排點一下就彈回最上面，而多選正是要一路往下選。
  只改那一格的 class 與底部的數字（`ui.renderPickFoot`）。
  **底部動作列在捲動區外面**（`index.html` 的 `#pickFoot`），所以切到別的面板
  不清就會留著——清的責任在 `drawDetail()` 第一行，不是每個繪製函式各自記得。

- **多選時沒選的格子不畫勾圈**。150 格全掛一個灰圈就是 150 個雜訊，
  跟手機不放刪除鈕同一個理由。只有選起來的才有勾，桌機 hover 淡淡提示。
  **多選時面板放寬到 720**（`body.pick-wide`），**只在桌機**。
  手機按下多選版面一個像素都不該動（2026-09-13，使用者要求），只有那顆鈕變色：
  面板本來就吃滿寬度、格子牆本來就吃 `--cell-cols`（小圖示五欄、大圖示三欄），
  沒有理由換一種排法。所以那兩條規則關在 `@media (min-width: 901px)` 裡面，
  **不能只靠手機那段覆寫**——`body.pick-wide .rail-inner` 的特異性高過
  手機的 `.rail-inner`，寫在外面反而會把手機的左右留白從 28 撐回 48。

- **選寶可夢面板的欄數吃設定，兩種寬度各一組值**（2026-09-16，使用者要求）。
  520 的單選走 `--cell-cols`（小 5 大 3），720 的多選走 `--pick-wide-cols`
  （小 7 大 5），桌機手機同一套。原本桌機兩段都寫死（3 與 5），
  設定選了小圖示還是一排三隻，520 除出來每格 152px、中間擺一張 54px 的圖，
  看起來比圖鑑的大圖示還大——使用者是從這裡看出不對的。
  **兩組值不要寫成「`--cell-cols` 加二」**：那是算出來的，而它們講的是
  兩種寬度各自排得好看的欄數，沒有固定差距。
  連動發生在 CSS 變數層，所以面板開著時去設定切大小，格子牆立刻跟著變，
  不必重繪，`renderPicker` 一個字都不用改。

- **多選只有異色能整批帶**（2026-09-13，使用者選的）。底部動作列那顆
  `data-pickshiny` 開著就整批寫 `shiny: true`，狀態在 `state.pickShiny`，
  跟 `pickFilter`／`pickMulti` 一樣放在 `state.pick` 外面，**切回單選也不清**。
  XXL／XXS 不做批次：那是個體大小，不會一批十隻都要。
  **沒有實裝異色的那 50 筆照一般色加進去**，不略過，toast 多一句講明幾隻。
  **重複判斷要拿實際會寫進去的值比**，不是拿開關的值——那幾筆開著也只會寫
  `false`，拿 `true` 去比會判成不重複，同一筆加兩次。
  按這顆跟點格子一樣**只重畫底部那一列**，走 `drawDetail()` 會把捲動歸零。

- **背卡的批次入口在背卡檢視，不在加號的面板**（2026-09-13，使用者選的）。
  點開一張卡，清單右上角一顆「多選」，底部兩顆「加進想要／加進可以給」，
  加進去的自動帶那張卡。**兩顆不是一顆**：從交換表按加號是從某一欄進來的，
  這裡沒有那個脈絡，而且同一張卡常常是想要幾隻、可以給幾隻。
  加號那邊刻意不放背卡下拉：要列「這批全部都帶得了的卡」，十隻一選交集
  常常是空的，空下拉無從解釋。
  **加完留在原地**（`addMany` 的 `stay`），並把 `#panel.scrollTop` 放回去——
  103 格那張卡加完彈回最上面就要重找看到哪裡。
  **選取跟著卡片走、模式記著**：換卡清 `state.bgSel`，`state.bgMulti` 留著。
  異色開關跟加號那邊共用 `state.pickShiny`，委派認到 `data-pickshiny`
  之後看自己是哪一邊，重畫自己那一條動作列。
  **底部窄的時候換行**：`.pick-foot` 靠 `flex-wrap: wrap` 讓兩顆鈕自己
  佔一行，不要為了擠成一行而把字縮掉。（那條規則是右欄還常駐、只有 340 寬時
  加的，現在彈窗 520 寬放得下一行，但手機仍然需要。）

- **從背卡點一隻進詳情，草稿要先配好那張卡**（2026-09-13，使用者確認）。
  `newDraft(bg)` 收一個背卡 id，點的時候把 `state.openCard` 傳進去。
  使用者就是在那張卡的清單裡點的，再叫他從下拉挑一次同一張沒有道理。

- **多選的重複判斷要連背卡一起比**。`addMany` 原本寫死 `!x.bg`，
  帶背卡加進去之後再加一次會判成不重複，同一筆進兩次。

- **詳情面板上方那張圖跟著草稿的異色走**（2026-09-13，使用者要求）。
  勾了異色就換成異色圖，不必先加進清單也看得出要的是哪一種。
  異色鈕只在 `hasShiny(e)` 時才畫，所以草稿為真必定有 `shinyIcon`。
  **四個條件一律預設不勾**，包括異色——常見情形是一般色，
  而且圖會跟著變，一打開就是異色會看錯是哪一隻。

- **從加號進來的詳情只給那一欄的加入鈕**（2026-09-18，使用者回報後拍板）。
  使用者說「按下可以給的加號，加進來的卻出現在想要」。**資料流沒有錯**，
  四條入口（單選、多選、搜尋後、返回再選）實測加進去的都在 `have`。
  錯的是介面：底部擺兩顆同等權重的鈕，橘色的「加入想要」還排在左邊第一顆，
  而面板標題明明寫著「加入可以給」——加號已經指定過欄了，再問一次就是
  給人按錯的機會，而且 `addItem` 加完會 `closePanels()` 直接跳回交換表，
  一眼就看到東西落在另一欄。
  `renderDetail` 的第七個參數因此從 `back`（布林）改成 `from`（欄位字串），
  兩顆鈕都走 `addBtn(col, from, t)`。**樣式不必分兩套**：
  `.d-actions button` 是 `flex: 1`，剩一顆就自己撐滿整列。
  **圖鑑點進來維持兩顆**，那裡沒有欄的脈絡。代價是從加號進來想改欄要
  退出去重按，使用者接受。check 有兩條盯著。

- **詳情面板的加入鈕不是開關**。同一隻可以配不同背卡各收一筆，
  「再按一次就移除」在這種情況下沒有意義，移除走每一筆自己的鈕。
  四個條件（異色、XXL、XXS、背卡）完全一樣才算重複，那時不新增只閃一下。
  只比對背卡會擋掉「同一張背卡，一筆要異色一筆不要」這種合法需求。

- **版本號有兩個檔，真相在 `js/version.js`**（2026-09-13 建立）。
  畫面只認那個模組，`VERSION.md` 是給使用者看的紀錄，JS 讀不到 md。
  兩邊寫岔的話網站會顯示一個紀錄裡查不到的號碼，所以 `check.mjs` 釘住
  「md 最上面那筆的號碼與日期等於模組的值」。升版要同時改兩個檔。
  號碼是「大改版.新功能.修正」，**後兩段補兩位**（`1.02.03` 不是 `1.2.3`）。
  **這跟儲存格式的 `v: 2` 是兩件事**，那個是資料結構的版本，
  只在格式真的變了才動，不要讓兩個號碼互相牽動。

- **不做自由備註欄**。備註只有自己看得到，對方收到的是分享圖，
  讀不到任何文字欄位。要讓對方知道的條件必須畫進圖裡，
  也就是異色、XXL、XXS、背卡這四個有視覺表示的維度。
  2026-09-10 已把 `note` 從儲存結構移除。

- **背卡的圖與代號有資料源，對應關係沒有**。game master 的 `LC_` 樣板給代號，
  `Images/LocationCards/` 給圖，`sb_` 是特殊背景、`lc_` 是地點卡。
  但「哪些寶可夢帶得了這張背卡」上游完全沒有，那一份仍是手工，也還是護城河。

- **上游的背卡圖不一定是玩家看到的卡面**。game master 標了 `vfxAddress` 的
  那 31 張（全部是 `sb_` 特殊背景），上游那個 PNG 只是底層，實際卡面是它
  再疊一層特效。GO Tour 2026 鑽石那張上游只有一片漸層天空，實際卡面還有
  石柱與星軌。`lc_` 地點卡沒有這個問題，實測五張與本地圖逐像素相同。
  所以有本地圖的優先用本地那張，沒有的只能顯示底層。

- **背卡的 game master 代號與圖檔名大小寫不一致**（`lc_citysafari2025_amsterdam`
  對 `lc_CitySafari2025_amsterdam`），跟裝扮同一個坑，一律轉小寫再比對。

- **Serebii 的背卡表是三列一組**（圖、日期、可取得寶可夢），
  平面攤開對會錯位，一定要照列分組。它的代號不帶年份，
  跟上游檔名要用去數字加年份的方式配。

- **Serebii 會漏掉進化取得的寶可夢**。City Safari 那批它只列伊布，
  Dittobase 與 Bulbapedia 都列了八個進化型。實測 193 張比對，
  119 張是這個原因，所以它只當墊底不當主來源。

- **Dittobase 的頁面裡嵌了結構化 JSON**，比解 DOM 穩，
  而且帶物種編號、型態代號與異色、暗影、超級進化、極巨化的旗標。
  代號組成是「編號-英文名-型態代號」，跟條目 id 對得起來。

- **上游與 Dittobase 對同一張卡的叫法可能完全不同**。上游寫球場
  （`lc_2026_NPB_kyocera`），Dittobase 寫球隊（`lc-nbp-orix-buffaloes`）；
  上游叫都內（`lc_TokMun_koto`），Dittobase 叫蓋章拉力賽。還有上游把
  wimpole 拼成 whimpole、Dittobase 把 npb 拼成 nbp。這種靠規則接不起來，
  只能在 `tools/build-bg.mjs` 的 `DB_MANUAL` 一張一張指名。
  Serebii 那邊同樣的坑（30 週年上游叫 tpc30th、它叫 PokéXciting），
  用 `SEREBII_MANUAL`。

- **比對卡面圖是唯一可靠的查證方式**。名稱推論會錯得很難看：巴黎兩張的
  編號是交叉的，上游 01 對 Dittobase 的 `-2`；`lc_CR_2026_001` 其實是
  紐約時代廣場。兩邊的圖縮成 32×32 逐像素比，同一張差 0～0.4，
  不同張差 25 以上，中間沒有灰區。**但 `vfx: true` 的不能這樣比**，
  上游那張只是底層，跟實際卡面本來就不一樣。

- **比對只防「一張卡對到多筆」，不防「多張卡對到同一筆」**。
  `norm` 會去掉數字也去掉 `lc_`／`sb_` 前綴，所以 `lc_MLB_tampaBayRays`
  與 `…Rays2`、`lc_GOWA_fukuoka` 與 `sb_GOWA_fukuoka` 會撞在一起，
  後面那張默默拿到前面那張的清單。`resolveMatches` 改成撞到就兩張一起
  退回沒有資料，要哪一張得在人工對照裡指名。曾經因此有三張卡掛著
  別張卡的清單，其中 GO Fest 2025 那張少了 68 隻。

- **對照條目時要處理四件事**：重音（`Flabébé` 要先去重音）、
  `_NOEVOLVE` 後綴（上游清單不帶）、只有型態沒有本體的
  （酋雷姆用 `fNORMAL` 當本體）、以及叫法不同的裝扮（人工補別名）。

- **背卡的寶可夢一律寫到型態層級**（`d128.fPALDEA_COMBAT`），
  不要用「本體加註記」那種舊寫法。註記留給真的需要補一句說明的情況，
  型態與裝扮的名稱由圖鑑提供，不必自己維護。

- **背卡 id 的年份要斷開**。上游把年份黏在活動名後面，
  `lc_CitySafari2024_tainan` 要變成 `city-safari-2024-tainan`。
  少數檔名年份寫了兩次，重複的去掉。規則在 `tools/build-bg.mjs` 的 `makeId`。

- **GO 圖示非正方形**且各不相同。畫面一律 `object-fit: contain`、
  canvas 等比縮放，不可假設正方形。

- **`extra.js` 那批圖四周有大量透明留白，而且主體不在正中央**
  （2026-09-13 修）。PokeMiners 的圖是緊貼裁切的，主體佔畫布 96~99%；
  Choggor 與 Dittobase 一律畫在 256×256 的畫布上，主體只佔 37~43%、
  而且偏下約 19%。`contain` 依畫布縮放，所以那 26 筆在圖鑑裡
  只有別人的四成大。
  修法是逐張記 `fill`（主體佔畫布多少）與 `offX`／`offY`（中心偏多少），
  畫面用 `transform: scale() translate()`、canvas 在 `drawContain` 裡
  換算，兩邊吃 `dex.js` 的同一組函式。
  **一定要逐張**：每張的留白都不一樣，乘同一個數字仍然會差 15 個百分點。
  **平移不能省**：只放大的話偏移跟著放大，腳會壓到格子底下的名稱。
  **`scale` 要寫在 `translate` 前面**，位移才會跟著放大。
  **translate 的百分比是相對元素算的**，所以帶倍率的圖元素必須等於圖框：
  圖鑑格子把它收成正方形，交換表的留白改用 margin 而不是 padding。
  **換備援要清掉**：`__imgfb` 會移除那三個變數，canvas 那邊只在拿到主圖時
  才掛倍率——官方立繪是滿版的，沿用倍率會整隻爆出格子。
  數字由 `tools/measure-icons.mjs` 量，**工具不自動改檔**，自己貼進去。

- **搜尋字串帶得了逐隻條件，靠分配律**（2026-09-17 改寫）。
  **先推翻一條寫錯的**：原本這裡寫「逐隻條件寫不出單一字串」，理由是 GO
  沒有括號、`,`（或）綁得比 `&`（且）緊。**前提對，結論錯。**
  GO 的字串結構是「與的或」（`A,B&C,D` = `(A或B) 且 (C或D)`），
  我們要的是「或的與」，兩者靠分配律互轉，不需要括號：

      4 ∨ 19 ∨ (異色 ∧ 7)  =  (異色 ∨ 4 ∨ 19) ∧ (7 ∨ 4 ∨ 19)
                            =  `異色,4,19&7,4,19`

  **「做不到」也是一個結論，也要查證過才寫進文件**，跟外部資料同一條規矩。

- **每一串搜尋字串固定以 `!交換&` 起頭**（2026-09-17，使用者要求）。
  **GO 裡交換過的寶可夢不能再交換**，列在字串裡對方也給不了，
  而他要點下去才知道。**兩欄都加**：「想要」那欄一樣是對方拿去翻
  自己的箱子，他交換來的那隻同樣給不出來。
  關鍵字 `交換`／`こうかん`／`traded` 出自官方 FAQ 1486 的
  「條件與分類」表（不是社群補的那一節）。`!` 要緊貼關鍵字，
  中間不能有空格。

  **開頭要先從長度預算裡扣掉**，不是組完才檢查：`build()` 收的是
  `budget = SEARCH_MAX - head.length`。組完才發現爆掉的話，
  降級會少放掉一組條件，產出一串超過上限的字。

  **開頭永遠不進降級的候選。** 放掉條件是安全的（對方多翻幾隻），
  放掉開頭不是——那會列出他根本換不了的寶可夢。check 的
  「交換來的一律搜不到」這一條因此**不因降級而放寬**。

  **空欄要早退。** 在這之前空欄自然組出空字串，加了固定開頭之後
  會變成只有 `!交換&` 的一串，複製了也搜不出東西，
  而 `openCopy` 的 `if (!res.str) return` 擋不掉它。

- **一定只有一行，寧可放掉條件也不換行**（2026-09-17，使用者否決了多行版）。
  GO 的搜尋框是**單行輸入**：多行貼進去會被黏成一串或只吃第一行，
  兩種都會變成搜不到東西的字串，而且**對方看不出哪裡不對**，他只會以為自己沒有。
  所以 `searchString` 算出來超過 `SEARCH_MAX` 時，**從隻數最少的那一組條件
  開始放掉**，那幾隻併回純編號再算一次，最壞退回純編號（就是 1.10.00 的行為）。
  放寬的方向永遠安全：對方多翻幾隻不會怎樣，漏掉才是災難。
  toast 用 `copyDropped` 講明幾隻是這樣來的。

- **同一組條件的那幾隻要共用一個子句**，不是一隻一個。
  10 隻裡 3 隻要異色，共用是 `異色,base&4,7,39,base` 51 字元，
  一隻一個是 93——接近翻倍，而且子句數是各桶選項數的**乘積**，
  差別會一路放大。不做也不會算錯，只會爆得快。

- **字串長度隨「隻數 × 子句數」成長，所以隻數多的時候條件會先被放掉**。
  每個子句都要把沒帶條件的編號整份重抄一遍，實測 36 格帶六種條件時
  base 自己就 143 字元，任何條件都塞不下，整批降級成純編號。
  十幾隻的常態清單不會碰到，這是刻意接受的。

- **正確性靠對拍，不靠讀**。轉出來的字串長得跟原清單完全不像，
  肉眼看不出對錯。check 的 4b **枚舉每一種可能的寶可夢狀態**
  （編號 × 條件的所有子集），拿字串的真值跟清單原意逐一比對。
  沒降級時要求完全等價，降級時要求**只准多不准少**。改這支的演算法一定要跑它。

- **搜尋字串的關鍵字表放 `gostring.js`，不放 i18n**。那是**遊戲的字**，
  不是我們的介面文字；混進 i18n 會讓「三語 key 一致」那條檢查管到不該管的東西。
  六個條件的三語全部出自官方 FAQ 1486 原文，一個都沒有照字面推。

- **背卡在字串裡只能寫「有背卡」**。`紀念背卡`（locationbackground）只涵蓋
  `lc_` 地點卡，我們還有 `sb_` 特殊背景，而 `specialbackground` 的中日文查不到。
  一律用寬的 `背卡`。**這跟圖鑑「寧可少列」的方向相反是刻意的**：
  圖鑑多列會讓人配出遊戲裡不存在的組合，搜尋少列會讓人以為自己沒有。

- **`極巨化` 這個字的語意跟我們的名單不是同一件事**。它是 `dynamax1-` 的捷徑，
  找的是「這隻解鎖了幾個極巨招式」，不完全等於「這隻能極巨化」。
  2026-09-17 使用者拍板照放（是官方關鍵字），**但真值還沒在遊戲裡實測**，
  可能會漏掉對方剛抓到還沒解招式的那隻。

- **裝扮的 `特殊` 刻意不做**。官方有這個關鍵字（costume），語意也對，
  但 1460 筆裡 303 筆是裝扮，加進來幾乎每份清單都會多一組桶、
  把降級的機率主動拉高，換到的只是少翻幾隻。等 `SEARCH_MAX` 實測出真值再說。

- **搜尋字串裡同一隻只會出現一次**。裝扮、型態、背卡、條件通通塌回
  本體的圖鑑編號，皮卡丘收了六格也只有一個 `25`。這是對的：搜尋本來就
  指定不了是哪一個裝扮，而對方要做的事就是翻自己所有的皮卡丘。
  編號一律跟 `dex.js` 要（`gostring.js` 的 `dexNumbers`），**不從 id 字串拆**
  ——「名稱、圖片、屬性一律以圖鑑為準」這條也涵蓋編號。

- **`SEARCH_MAX` 是還沒實測的值**（`js/gostring.js`，先填 200 字元）。
  搜尋框確實有上限，社群的清箱工具會自動分段，但 Niantic 沒公開數字。
  所以第一版**只多講一句提醒，不切字串**：拿猜的數字去切，切錯了是靜默
  少一半；讓使用者自己看一眼，錯了至少看得見。一欄可以收 200 格，
  但去重後常態是十幾個編號，碰得到這條線的是極少數。

- **搜尋字串沒有語言問題**。純數字三語通用，所以複製鈕不需要語言選擇器，
  只有鈕的文字與 toast 跟著介面語言走。之後真要加條件（`異色&…`）才會
  需要分語言，對照表在 `docs/go-search-syntax.md`。

- **按複製鈕先開確認彈窗，不直接寫剪貼簿**（2026-09-17，使用者要求）。
  那串字長得跟原清單完全不像（`異色,4,19&7,4,19`），複製完只有一句 toast
  的話，使用者無從判斷自己拿到的是不是對的東西。現在先把字串攤在
  `#sheet` 裡（`ui.renderCopy`），窗裡那顆 `data-docopy` 按下去才寫，
  **右上角的 X 就是放棄，什麼都不複製**。

  **`copyDropped` 與 `copyLong` 跟著從 toast 搬進彈窗，放在字串上方**：
  「有 24 隻的條件放不進去」在複製後才講已經來不及了，複製前看得到
  才有機會回去減幾隻再來。toast 只留「已複製 N 隻」。

  **複製失敗不關窗**——字串還在畫面上，可以自己選取複製，那是
  舊瀏覽器唯一的出路。`state.copy` 是右欄互斥狀態的第四種，
  三個開右欄的入口都要清掉它。

- **字串框的斷行是 `word-break: break-all`，不是 `overflow-wrap: anywhere`**。
  兩者都擋得掉橫向捲動（那串字裡一個空白都沒有），但 anywhere **只在
  「這一行無論如何放不下」時才啟用任意斷點**，所以它會先斷在開頭
  「異色」之間的 CJK 正常機會上——第一行只剩孤零零一個「異」字，
  第二行才滿。break-all 每個字元後都能斷，排出來才是齊頭的一塊。

- **`closePanels()` 不叫 `closeDetail()`**。寫錯名字擺在 `async` 函式裡，
  ReferenceError 會被吞成未處理的 rejection：**剪貼簿寫進去了、窗沒關、
  toast 也沒出**，而 `node tools/check.mjs` 全過。這條路只有真的按下去
  才會走到，所以「改了介面要實際起 server 用瀏覽器看」不是客套話。

- **複製鈕桌機手機都顯示，鉛筆只在觸控裝置**。兩顆並排在欄標題右邊，
  但存在理由不同：鉛筆是為了補觸控裝置格子上沒有刪除鈕，複製兩邊都用得到。
  **空的那一欄兩顆都不畫**——按了得到空字串跟按了沒反應一樣難解釋。

- **Clipboard API 要安全內容與使用者手勢**，兩個條件都成立（線上是 HTTPS 的
  GitHub Pages、本機是 localhost，而那段跑在 click 裡）。舊 iOS Safari 沒有
  這個 API，退回螢幕外的 textarea 加 `execCommand`。
  **headless 驗剪貼簿要開 `Emulation.setFocusEmulationEnabled`**，
  否則 `readText()` 會丟 "Document is not focused"，那不是程式的問題。

- **本機開發不能用 `file://`**：ES modules 會被 CORS 擋，
  必須 `python3 -m http.server 8000`。

- **headless Chrome 的視埠下限是 500px**。用 `--window-size=430` 截圖
  看起來溢出其實是裁切，不是版面問題。要量真正的手機寬度得用 iframe。

- **`process.exit()` 在 Windows 上會炸**：fetch 的 keep-alive socket 還開著時
  強制結束會 assertion abort。腳本一律設 `process.exitCode`。

### 測試方式

```
node tools/check.mjs        # 不連外網
node tools/check.mjs --net  # 加驗圖片網址，含 240 張背卡逐一驗
node tools/measure-icons.mjs # 驗 extra.js 的 fill/offX/offY 跟圖片對得上
node tools/build-max.mjs --force # 重抓可極巨化名單，報告在 tools/max-report.md
node tools/build-shadow.mjs --force  # 重抓可淨化名單，報告在 tools/shadow-report.md
node tools/build-bgflags.mjs --force # 重抓背卡旗標，報告在 tools/bgflags-report.md
node tools/build-kb.mjs      # 產生知識的靜態頁（改了內文或殼就要重跑）
node tools/build-kb.mjs --check  # 只比對不寫檔，check.mjs 走的就是這條
```

涵蓋：三語 i18n key 一致性、版本號格式與兩個檔沒寫岔、條目欄位完整與
id 不重複、背卡引用與 id 回歸、知識條目的 slug 與欄位（外加 `kb/` 的產出
有沒有過期）、儲存往返、全部繪製函式（含 1478 筆詳情逐一繪製）、HTML 逸出。

DOM stub 在 `tools/check.mjs` 裡面，需要新的元素 id 時加進去就好。

改動介面後，除了 check 還要**實際起 server 用瀏覽器看**。
headless 截圖指令：
```
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless --disable-gpu --window-size=1440,900 \
  --virtual-time-budget=12000 --screenshot=out.png http://localhost:8000/
```

### 檔案結構

```
index.html            骨架（149 行）
css/style.css         全部樣式，token 集中在 :root
js/godex.js           自動產生的圖鑑資料（不要手改）
js/maxdata.js         自動產生的可極巨化名單（不要手改）
js/shadowdata.js      自動產生的可淨化名單（不要手改）
js/bgflags.js         自動產生的背卡條件旗標（不要手改）
js/extra.js           手動補的條目
js/costumes.js        裝扮譯名
js/dex.js             圖鑑單一入口
js/store.js           localStorage 讀寫、匯出匯入
js/backgrounds.js     背卡單一入口（合併骨架與手工）
js/bgdata.js          自動產生的背卡骨架（不要手改）
js/bgevents.js        手工維護的背卡
js/bgseries.js        收納夾譯名
js/imgchain.js        圖片備援鏈（dex 與 backgrounds 共用）
js/types.js           屬性顏色與名稱
js/i18n.js            繁中／日／英字典
js/kbdata.js          知識條目的 metadata（手動維護，目前是空的）
js/version.js         版本號（畫面唯一認的值）
js/ui.js              全部繪製函式
js/main.js            進入點、狀態、事件
js/share.js           雙欄分享圖（canvas）
js/gostring.js        把一欄變成 GO 的搜尋字串
img/bg/               背卡的本地備援圖（17 張，進 git）
img/extra/            上游沒有的裝扮圖（5 張，進 git）
img/purified-mark.png 淨化的官方符號（進 git）
kb/_src/              知識的內文片段（**底線開頭，Jekyll 才不會公開**）
kb/_src/lucky-trinket.html  第一則：亮晶晶首飾
kb/img/              知識的內文插圖（進 git，規格在該目錄的 README）
kb/<slug>/index.html  產生的知識頁（不要手改）
tools/build-dex.mjs   產生圖鑑資料
tools/build-max.mjs   產生可極巨化名單
tools/build-shadow.mjs   產生可淨化名單
tools/build-bgflags.mjs  產生背卡條件旗標
tools/build-bg.mjs    產生背卡骨架
tools/make-purified-mark.mjs  產生淨化符號
tools/png.mjs         PNG 讀寫（兩支 make-*-mark 共用）
tools/build-kb.mjs    產生知識的靜態頁
tools/check.mjs       自我檢查
tools/measure-icons.mjs  量 extra.js 那批圖的留白
docs/go-search-syntax.md  GO 搜尋語法抄錄（參考用，程式沒用到）
project-index.md      檔案索引與依賴關係
progress.md           開發歷史（給我看的，逐次改動）
VERSION.md            版本紀錄（給使用者看的，一版一筆）
```

**四個檢視**（桌機在頂部列，手機在貼底的 bar）：圖鑑、交換表、背卡、知識。
**知識那一顆在 `js/kbdata.js` 是空的時候不會畫**，所以目前畫面上只有三顆。

### 目前規模

| 項目 | 數量 |
| --- | --- |
| 圖鑑條目 | 1460 |
| 　一般 | 893 |
| 　型態變化（含地區型 53） | 264 |
| 　裝扮 | 303 |
| 已實裝異色 | 1410 |
| 可極巨化的條目 | 151（其中 20 個可超極巨化） |
| 可淨化的條目 | 480 |
| 有背卡條件旗標的卡 | 12（107 個標記） |
| 收納夾 / 背卡 / 收集格 | 23 / 240 / 1579 |
| 介面文字 | 三語各 149 個 key |
| 有 IV100 CP 的條目 | 1460（全部） |

### 知識檢視【地基 2026-09-18 做完（A-2），內容還沒進】

> **地基在了，內容是空的。** `js/kbdata.js` 現在是空陣列，
> 所以**第四顆檢視鈕與頁尾那條連結都不會畫，`kb/` 底下也沒有任何產出**——
> 這是刻意的，沒有內容就沒有入口。第一則進 kbdata 就整套長出來。
> 寫內容前先讀這一節。

**是什麼**：第四個檢視，一面格子牆，一格一則寶可夢知識。
**點格子不是開右欄**，是連到那一則自己的網頁 `kb/<slug>/`。

**為什麼要獨立的頁**（使用者指定，理由是 SEO）：`index.html` 只有 180 行
骨架，內容全部是 JS 執行後才填的，爬蟲與分享預覽拿到的是空殼。
知識頁要被搜尋得到，內容就必須**直接寫在 HTML 裡**。
GitHub Pages 根目錄本來就能放任意多個 .html，架構上不衝突：
SPA 照舊，靜態頁是新長出來的一層。

**內容分兩半存**：
- `js/kbdata.js` —— metadata（slug、標題、摘要、分類、更新日、來源清單）。
  **格子牆只讀這個**，畫格子不必載入內文。
- `kb/_src/<slug>.html` —— 內文，**純 HTML 片段**，不套殼。
- `tools/build-kb.mjs` 讀兩邊套上共用的殼，輸出 `kb/<slug>/index.html`
  與索引頁 `kb/index.html`。

**寫一則的四個動作**：`js/kbdata.js` 加一筆 → 寫 `kb/_src/<slug>.html` →
`node tools/build-kb.mjs` → `node tools/check.mjs`。

#### 做地基時定下來的幾件事

- **內文源的目錄是 `_src` 不是 `src`**。GitHub Pages 會把 repo 裡每一個檔案
  都當網頁送出去，裸片段被索引到就是一頁沒有 title、沒有樣式、內容卻跟
  正規頁一樣的重複內容。**Pages 預設跑 Jekyll，而 Jekyll 不發布底線開頭的
  目錄**，改個名就擋掉了。**所以不要在這個 repo 加 `.nojekyll`**，
  那會關掉 Jekyll，`_src` 立刻全部公開。

- **爬蟲要有一條真的路走進來**。格子牆是 JS 畫的，爬蟲讀不到，
  那些靜態頁就沒有任何連結指過去。所以是三段：`index.html` 頁尾一條
  `<a href="./kb/">`（`#kbFoot`，寫死繁中讓爬蟲直接讀得到，
  `renderChrome` 再依語言覆蓋）→ `kb/index.html` 索引頁 →
  每一則。每一則的麵包屑再指回去。**這三段少一段就等於沒做 SEO。**

- **格子是 `<a>` 不是 `<button>`**。中鍵開新分頁、右鍵複製連結都要能用，
  而且它指向的是一頁真的 HTML。check 有一條盯著。

- **一則都沒有就不產出任何檔案**。產一頁寫著「還沒有內容」的索引頁上線，
  等於給搜尋引擎一頁空的。

- **`canonical`、`og:url` 與 `og:image` 目前不寫**（2026-09-18 使用者拍板）。
  三個都要絕對網址，而網域還沒買（待辦 C）。填現在的 GitHub Pages 子路徑
  等於之後全部要改一次。`tools/build-kb.mjs` 頂端的 `SITE` 常數填了就長出來，
  改一行重跑。其餘連結一律相對路徑，搬家不用改。

- **深色那段 inline script 放在 `<body>` 之後的第一行**，不是 `<head>` 裡：
  head 裡 `document.body` 還不存在，而 style.css 那兩千行深色全部掛在
  `body.dark` 底下。同步 script 會擋住繪製，所以不會先閃一下白的。

- **靜態頁吃同一份 `css/style.css`**，不開 `kb.css`。為的是拿到 `:root`
  那組 token 與深色，另開一個檔等於深色要同步兩處。知識的樣式整段
  （含自己的 `@media`）放在 style.css 檔尾自成一區。

- **`.kb-note` 的底色用 `--card` 不是 `--sunk`**。深色的 `--sunk`（#0b0b0b）
  比畫布（#101010）**更暗**，整段會變成頁面上一個黑洞。

- **行內代碼一定要 `<code>`**。搜尋語法那一則整篇都是 `,` 與 `&`，
  不給它自己的樣子就跟旁邊的標點混在一起——實測截圖裡真的看不出來。

- **寬表格自己包一層 `<div class="kb-scroll">`**。內文是手寫的，
  CSS 沒辦法替它加容器，不包的話手機上整頁會被撐出橫向捲動。

- **插圖一定要 `<figcaption>`**，而且 `<img>` 要寫死 `width` 與 `height`。
  圖說是給看不到圖的人唯一的內容；沒寫尺寸的話圖載進來整頁會往下跳（CLS）。
  圖放 `kb/img/`（**進 git**，跟 `img/bg` 同一類例外），規格在
  `kb/img/README.md`：720px 寬、單張 200KB 以內、命名 `<slug>-<序號>`。

- **分類目前只有兩個**（`trade` 交換、`search` 搜尋），在 `js/kbdata.js`
  的 `KB_CATS`，譯名在 `js/i18n.js` 的 `kbCat*`，對照表在 `js/ui.js` 的
  `KB_CAT_KEY`。三處要一致，check 有一條盯著。
  **`data-cat` 目前不上色**——兩個分類多兩個顏色 token 換不到辨識度，
  屬性先留著。

- **check 會驗產出有沒有過期**（`build-kb.mjs --check`）。改了內文卻忘記
  重跑的話，線上那頁就一直是舊的，而那是**看不出來的**：頁面好好地在，
  只是內容過期。

- **中文段落一行寫到底，不要在句中換行**（2026-09-18 寫 A-3 時實測抓到）。
  HTML 把 CJK 字元之間的換行渲染成**一個空格**，畫面上就冒出
  「其中一名朋友 變成」這種縫。**原始碼看起來完全正常**，而且是整篇零星
  幾處，肉眼很容易漏——第一則寫完量到 7 處。標籤之間換行沒問題
  （行首行尾的空白會被丟掉），所以縮排照常。
  check 有一條盯著，**註解裡的換行不算**（那根本不會渲染，
  不跳過的話範本那份寫滿說明的註解會整片假報）。

- **索引頁的引言用 `kbLead` 不是 `kbFooter`**。後者是 SPA 頁尾那條連結的
  文字，自帶「知識：」前綴，擺在 h1「知識」底下會變成同一句講兩次。

- **社群數字對不起來的時候，就寫「沒有共識」，不要挑一個**
  （2026-09-18 使用者拍板，A-4 的亮晶晶好朋友機率）。那個數字社群測出來
  從 1% 到 5% 都有人講，連整理得最完整的英文社群都直說精確機率沒有人
  找到過。**挑一個寫等於幫讀者算一個假的期望值**，而講明它沒有共識
  本身就是有用的資訊。這跟圖鑑「查不到就不要放」同一個方向。

- **沒讀到原文的來源不列進 `sources`**。A-4 的「每天第一次互動才判定」
  在 Fandom 與 sportskeeda 都交叉比對過，說法一致，但那兩個站擋爬蟲
  （402／405）拿不到原文，所以來源只列讀得到的 Pokémon GO Hub。
  **交叉比對過是一回事，列成出處是另一回事。**

- **知識頁之間可以互相連結**，寫 `<a href="../<slug>/">`
  （從 `kb/<slug>/index.html` 看出去正好是隔壁那一則）。
  check 有一條驗「指到的那一則真的存在」——slug 打錯就是一條死連結，
  **畫面上看起來完全正常**，點下去才 404，而寫的人多半不會去點
  自己剛寫的連結。

- **對照表的第一欄要用 `<th scope="row">`**，不是 `<td>`。
  樣式有一條 `.kb-body th[scope="row"]` 把它轉回一般內文的樣子，
  不然那一欄會變成一排小灰字，比右邊的值還難讀。

**刻意不做 Markdown 解析器**。這個站沒有 npm，得自己刻一個，那是 bug 溫床
（逸出、表格、巢狀清單）。寫內文時多打幾個 `<p>` 的代價遠比維護一個
半吊子解析器低。改殼、改樣式重跑一次腳本就好，不必手改 N 個檔——
這也是不手寫一頁一檔的理由。

**先只做繁中**（使用者拍板）。SEO 主要吃中文搜尋，而內容要自己查證，
三語就是三倍的量。**但介面文字照舊三語**：檢視名稱「知識」要補進 `i18n.js`，
三語 key 一致那條檢查管的是介面，不管知識內容。

**術語一律用官方繁中，不用玩家俗稱、更不要自己翻譯**。
2026-09-18 討論時把 Lucky 全猜成「幸運」，官方其實是**「亮晶晶」**：

| 官方繁中 | 英文 |
| --- | --- |
| 亮晶晶好朋友 | Lucky Friends |
| 亮晶晶首飾 | Lucky Trinket |
| 亮晶晶寶可夢 | Lucky Pokémon |

**友誼等級是六個里程碑**（2026-09-18 查 FAQ 2847 逐字確認，做 A-3 時發現的）：

| 官方繁中 | 朋友點數 |
| --- | --- |
| 朋友 | 加了好友就是 |
| 好朋友 | 1 |
| 給力好朋友 | 7 |
| 麻吉好朋友 | 30 |
| 正港好朋友 | 90 |
| 正港好朋友＋ | 180 |

**這張表推翻了兩件事**：這一節原本只列「給力好朋友 / 正港好朋友」，
而我寫 A-3 時差點把中間那級寫成**「超級好朋友」——那個詞不存在**，
官方是「麻吉好朋友」。最後那級「正港好朋友＋」也是原本不知道的。
**沒查就寫的話，一則講友誼等級的知識頁會有一半的等級名稱是錯的。**

**「亮晶晶好朋友」不在這六級裡面**，它是疊在友誼等級上的一個暫時狀態，
交換完就解除。寫內容時不要把它當成第七級。

**遠距交換要「正港好朋友＋」**（180 點），而且每次用掉要再累積 90 點，
同時只能有 1 次機會。**2026-09-18 做 A-6 時逐字查證過，全部正確**，
原句在 FAQ 2847：「與每位『正港好朋友＋』僅能同時擁有1次遠距交換的機會。」

#### 知識頁的文句規矩（2026-09-18 使用者回饋）

**知識頁不要寫成 CLAUDE.md 的語氣。** 這份檔案本身是密集強調、破折號轉折、
斷言句——那適合給自己看的工程筆記，寫給玩家看就變成「像在說服人」。
使用者的原話是「轉折語氣太用力」與「句子本身不自然」。三條：

1. **破折號 `——` 不拿來當轉折**，拆成兩句。（五則原本共 14 處，現在 0）
2. **不用「其實／真正／正是／這才是」**這種先貶後褒的套路。
3. **「不是 X 而是 Y」直接講 Y**，除非兩者真的需要對比。

**粗體不在這條規矩裡。** 第一次改的時候順手把粗體從 13 砍到 2，
使用者指出他沒要求這件事——**沒被要求的改動不要夾帶**，
即使自己覺得比較好看。改完粗體密度要跟改之前一樣。

**還有一條：改了畫面就把圖送出去。** 使用者說「我還看不到現在的樣子」，
在那之前我只用文字描述差異。文句這種東西量給他看沒有用，要看畫面。

#### 搜尋語法的兩條，方向相反（2026-09-18 做 A-5 時確認）

- **`|` 是「且」不是「或」，這是官方寫的。** FAQ 1486 繁中原文把
  `&` 與 `|` 放在同一組（「複數條件搜尋」），`,` `:` `;` 才是「或」
  （「多條件搜尋」）。多數程式語言剛好相反，**這是最容易搞反的一條**。
  `js/gostring.js` 目前只用 `&` 與 `,`，沒有用到 `|`。

- **「`,` 綁得比 `&` 緊」官方三語都沒有寫**，是社群整理的。
  但 `gostring.js` 的分配律整個靠它，check 4b 的對拍也建立在這個前提上。
  **頁面上標明了這件事並建議讀者自己先試一個短的**。
  真要動 `gostring.js` 的演算法，這個前提本身也該進遊戲實測一次。

- **`docs/go-search-syntax.md` 是內部完整參考，不等於那一頁的內容。**
  212 行整份搬上去只會讓讀者淹在裡面。頁面只收運算符、優先順序、
  交換用得到的關鍵字、一串字的拆解與陷阱。底稿留著不動。

#### 社群來源的規矩（2026-09-18 使用者指定）

**巴哈姆特 Pokémon GO 哈啦板是這個主題的社群主來源**，每一則都該去翻一輪。
官方 FAQ 只講機制不講數字，而使用者最想知道的常常正是那些數字
（個體值下限、機率、上限），那些只有社群有。

四條規矩：

1. **官方仍然是主來源**，社群只補官方沒講的部分。兩邊衝突時以官方為準，
   並在頁面上把衝突講出來。
2. **社群數字要兩個以上獨立來源才寫**。A-3 的 12/12/12 與沙子減半是
   巴哈、英文 Fandom wiki 與幾個攻略站三邊對得起來才寫進去的。
   只有一個來源的一律不寫——跟外部資料交叉比對那條同一個道理。
3. **頁面上一律標成社群說法**：`sources` 那筆 `official: false`
   （會顯示「社群說法，非官方」的標籤），內文那一段用 `.kb-note` 再講一次
   「這個數字沒有官方出處」。
4. **不整段抄，自己寫**。事實可以引用，表達方式要自己的。
   社群來源的 `label` 要帶**發文或更新日期**，那些數字會隨版本改。

#### 已經查到、還沒用到的數字（給 A-4 與 A-7）

巴哈那篇（2026-07-13）與英文社群整理的，**都還沒有做第二來源交叉比對**，
寫進頁面前要再查一輪：

- ~~一般交換變成亮晶晶的機率約 **5%**，用陳年寶可夢會提高，
  **最高 20%**（距今 2~3 年以上達上限）~~ —— **2026-09-18 A-7 查證過，
  寫進頁面了，但前提是壞的**：只有巴哈那一篇給數字，而 PoGO Alley
  （2026-08）明講網路上的機率表幾乎都追溯到 The Silph Road 的取樣，
  那個組織 2023 年就結束了、數字從未被官方證實、之後沒有人重做。
  **這不是兩個來源互相印證，是一份舊資料被抄很多遍。**
  使用者拍板照寫，所以頁面上把這個來歷一起寫出來。
  **下次碰到社群數字，要問的不是「有幾個來源」而是「這幾個來源是不是
  同一個源頭」**——兩個網站抄同一份取樣，不算兩個來源。
- ~~交換可以獲得的亮晶晶寶可夢**數量上限從 35 提高到 45**~~ ——
  **2026-09-18 追到官方繁中原句了**，在 pokemongo.com 的「新年 2026」
  活動公告。連帶查到 2018 年首次公布的那則也有繁中版，而且把上限講得更死：
  「兩位訓練家過去獲得的亮晶晶寶可夢數量都未滿10隻」——**是雙方一起算**，
  2026 那則只換數字，沒有取消這個條件。
- 正港好朋友之後，每天互動變成亮晶晶好朋友的機率約 **1%**，
  70 等之後約 **2%**（**還沒查證**，A-4 已經決定機率整段不寫）

**來源標準比照 `js/gostring.js` 的關鍵字表**：主來源是 Niantic 說明中心的
繁中版（搜尋關鍵字那份出自 FAQ 1486，亮晶晶好朋友是 1485、亮晶晶首飾是 4945，
就在隔壁）。**查不到官方出處的要在頁面上明講是社群說法**——
這跟圖鑑「查不到就不要放」不衝突，差別在知識頁寫得出「這條沒有官方依據」，
圖鑑只能不列。

#### A-6 查證時順帶挖到的（2026-09-18，還沒用到）

- **官方說明頁是 server-rendered 的，`curl` 抓得到全文。** Helpshift 那些
  `niantic.helpshift.com/hc/zh-hant/...` 的頁面內容就寫在 HTML 裡，
  抽 `class="faq-body js-faq-body"` 那一段就是原文。**WebFetch 的摘要模型
  會拒絕逐字引用**，而這一則需要的正是官方原句（例如「僅能同時擁有1次」），
  所以下次直接 curl 再自己剖，不要跟摘要模型繞。

- **同一份 FAQ 的不同語言版本內容會不一樣，而且繁中常常是舊翻譯。**
  FAQ 96 的交換等級門檻，英文寫 10 級、繁中寫 12 級。
  **查到關鍵數字時多開一次英文版對一下**，這是零成本的交叉比對。

- **英文版 FAQ 96 的特殊交換清單有一行繁中版沒有：**
  `Pokémon with unique or special features (like Location Cards)`——
  **帶背卡的寶可夢算特殊交換**。這條直接打到本站的主軸（背卡是差異化），
  而繁中版完全沒有。還沒寫進任何一頁。

- **遠距交換排除「過去 30 天內捉到的寶可夢」。** 搜尋字串目前只排掉
  交換過的（`!交換&`）。如果之後要做「這份清單是要遠距交換的」那種模式，
  還得排掉剛抓到的，關鍵字要另外查（FAQ 1486 那份裡有沒有對應的詞還沒確認）。

- **官方從來沒有公布過交換的星星沙子數字**，只給了折扣的相對關係
  （略為／適中／更加減少）。**但那個相對關係有用**：它裁定了社群費用表的
  等級對應——第一次出現折扣是「給力好朋友」，所以把折扣掛在「好朋友」的
  那份社群表是錯一格的。**定性的官方描述可以用來驗證量化的社群數字。**

#### A-7 立下的一條規矩（2026-09-18）

- **數官方出處要連「數字在哪裡」一起想。** 說明中心（helpshift）只講機制，
  **具體數字多半在 pokemongo.com 的活動公告裡**。A-6 與 A-7 連續兩次把
  「查不到官方數字」寫成待辦，兩次都是因為只翻了說明中心。
  兩個地方都要翻，而且公告有繁中版（`pokemongo.com/zh-Hant/news/<slug>`，
  頁面是 server-rendered，curl 抓得到）。

- **社群來源要問的是「是不是同一個源頭」，不是「有幾個」。**
  亮晶晶機率有一堆網站在寫，但它們幾乎都抄同一份 The Silph Road 取樣，
  而那個組織 2023 年就結束運作了。**兩個網站抄同一份資料，不算兩個來源。**
  CLAUDE.md 原本那條「社群數字要兩個以上獨立來源」要這樣讀。

#### 交換機制的數字，第一個該翻的是 game master（2026-09-18）

- **`tools/.cache/game_master.json` 裡有交換機制的權威設定，不必上網查。**
  做「交換的星星沙子」那一則時才發現的，兩個模板一次解決很多問題：

  | 模板 | 給了什麼 |
  | --- | --- |
  | `FRIENDSHIP_LEVEL_0`~`5` | `tradingDiscount`（0、0、0.2、0.92、0.96、0.96）、`minPointsToReach`（1／7／30／90／+90）、`unlockedTrading`（哪一級能換哪些）、`attackBonusPercentage`、`raidBallBonus` |
  | `REMOTE_TRADE_SETTINGS` | `requestedPokemonCount` 3、`pokemonUntradableDays` 30、`maxRemoteTradesPerDay` 1、`timeLimitMinutes` 2880（＝48 小時）、`taggingUnlockPointThreshold` 85（對 90，就是官方說的「差 5 點」） |

  **`REMOTE_TRADE_SETTINGS` 把 A-6 整頁逐項對上了。** 下次寫交換相關的
  知識頁，先 grep 這個檔再去翻說明中心。

- **但底價（100／20,000／1,000,000）不在 game master**，那是 client 端常數。
  完整格子只能對照 Bulbapedia 的 `Trade (GO)`——**Bulbapedia 在這裡又破例
  當資料來源**，跟 bgflags 同一個理由：官方只給定性描述，沒有別的地方有。

- **1.16.00 的費用表是錯的，錯因是「推算」。** 那時拿 52poke 的折扣
  （0.92／0.96）去乘底價 100，推出麻吉好朋友 8、正港好朋友 4，
  **實際上「一般且雙方都已登錄」六個等級全部固定 100、不吃折扣**。
  折扣只作用在 20,000 與 1,000,000 那兩個底價上。
  使用者拿兩張社群費用表來對才抓到，Bulbapedia 與英文社群也一致。
  **教訓：自己乘出來的數字是待驗證的假設，不是結論**——
  這跟「IV100 的 CP 不能退回本體的數值」是同一類錯，
  推出來的東西看起來都很像真的。

**MVP 五則**（都是使用者點名的，`search-syntax` 已有底稿）：

| slug | 內容 | 來源狀況 |
| --- | --- | --- |
| `lucky-friends` | 亮晶晶好朋友是什麼、怎麼變成、交換後解除 | 官方 FAQ 1485 |
| `lucky-trinket` | 亮晶晶首飾的使用條件與限制 | 官方 FAQ 4945 |
| ~~`guaranteed-lucky`~~ | 必定變亮晶晶的條件 | 已上線，官方公告 + FAQ 38／1485 |
| ~~`remote-trade`~~ | 遠距離交換的條件、流程與限制 | 已上線，官方 FAQ 5312／2847／96 |
| ~~`search-syntax`~~ | 搜尋術語規則 | 已上線，官方 FAQ 1486 ＋ 兩條社群 |

已經查到、動工時可以直接用的（官方 FAQ 4945）：亮晶晶首飾是一次性道具、
對方友誼至少要「給力好朋友」、已經是亮晶晶好朋友就不能用、
**放在背包裡有時限**、**不會改變交換距離**，而且**對方可以在
設定→朋友裡關掉，拒絕別人對他使用**。

**每一頁要有的 SEO 件**：`<title>`、`meta description`、`canonical`、`og:*`、
`lang="zh-Hant"`、麵包屑回主站、底部逐條列出處。

**深淺色要自己處理**。靜態頁吃同一份 `css/style.css`，但深色是存在
`poke-change/pref` 的偏好，頁面開頭要放一小段 inline script 讀它套上
`body.dark`，不然從深色模式點進去會先閃一下白的。

**三件先知道的代價**：
1. ~~**手機底部 bar 從 3 格變 4 格**排不排得下要量過~~ —— **2026-09-18 量過了，
   排得下，版面一個字都不必改**。三語 × 320/360/375/390/430 五種寬度全量：
   **零裁切、零換行、零溢出**，bar 高度維持 57px（`--tab-h` 56 + 1px 上框線）。
   最緊的是 **320px 的英文**，每格 80.3px，最長的 `Backgrounds` 佔 68.5px，
   `Trade List` 與它的字距剩 20px——截圖看過，還不到黏在一起。
   觸控目標 80.3×56 遠超過 44×44。選中那條金線寫死 26px，四格照樣置中。
   **桌機也順手量了**：901px 最窄時英文四格右緣 728.5，離右邊還有 170px
   給語言與齒輪，零換行。（1280px 有 11px 的文件橫向溢出，但**三格時就有**，
   不是第四顆造成的，是既有的小毛病，另外查。）
   英文標籤不受版面限制：`Guide` 30.7、`Tips` 22.3、`Knowledge` 57.9、
   `Know-how` 55.3，四個候選在 320px 都放得下，挑哪個純粹看語意。
2. **SEO 的價值要等自有網域才兌現**。`jp-study-list.github.io/poke-change/`
   這種路徑吃不到什麼權重。現在做不算白做，URL 走相對路徑，搬家不用改。
3. **成本主要在查證不在程式**。程式大概一天，五則內容逐條找官方出處、
   標明哪些查不到，那才是重頭。知識頁寫錯比圖鑑多列一隻更糟。

---

### 待辦

**每一項待辦冠一個字母，細節與衍伸項目寫成 `A-1`、`A-2`**（2026-09-18，
使用者拍板，**跨 session 一律照這套，不要重新發明**）。三條規則：

1. **字母是主題的身分，不是優先序。** 配了就不動，做完退休**不回收**，
   新的主題拿下一個沒用過的字母。這樣跨 session 講「做 B-1」永遠指同一件事。
2. **狀態分組不冠字母。** `/start` 印的「現在就動得了／要你先決定／卡住」
   只是當下的分類，同一項會在組間移動，**移動時編號不變**。
   分組要是也叫 A、B、C，`A-2` 就會分不出是「A 組第二項」還是「A 的第二個細節」。
3. **這一節是唯一的待辦來源。** 別處看起來像待辦的（progress 的「待辦/已知問題」、
   程式碼註解）是歷史紀錄，不要撈進表裡。

---

**A 知識檢視**（第四個檢視 + `/kb/` 靜態頁）。規格已定案，見上面「知識檢視」
那一節，還沒動工。**用頁面拆：一則知識就是一個計劃**，做完一則就能上線一則，
不必等五則齊。A-3 到 A-7 每一則的工作都一樣：查官方繁中出處 → 使用者審 →
寫 `kb/_src/<slug>.html` → 重跑 build-kb。**成本在查證不在程式。**

- ~~**A-1** 量手機底部 bar 3 格變 4 格排不排得下~~（2026-09-18 量完，字母退休不回收）。
  **結論：排得下，A-2 不必為版面做任何事**，數字見上面「三件先知道的代價」第 1 點
- ~~**A-2** 地基~~（2026-09-18 做完，字母退休不回收）。`tools/build-kb.mjs`、
  共用的殼、`js/kbdata.js`、第四個檢視、i18n 與 check 都在了。
  **canonical、`og:url` 與 `og:image` 刻意沒做**，等 C；
  其餘 SEO 件（`<title>`、description、`og:title`/`og:description`/`og:type`、
  麵包屑、爬蟲入口、深色 inline script）寫在殼裡，五則共用。
  細節見上面「知識檢視」那一節
- ~~**A-3** `lucky-trinket` 亮晶晶首飾~~（2026-09-18 上線，1.14.00，字母退休不回收）。
  四條來源全是官方（4945／1485／2847／38）。**插圖還沒有**，
  使用者之後補（規格在 `kb/img/README.md`）
- ~~**A-4** `lucky-friends` 亮晶晶好朋友~~（2026-09-18 上線，1.15.00，字母退休不回收）。
  三條官方（1485／2847／4945）＋ 一條社群。**機率刻意不寫**，見下面那條
- ~~**A-5** `search-syntax` 搜尋術語規則~~（2026-09-18 上線，1.19.00，字母退休不回收）。
  **MVP 五則到此全部完成。** 底稿那 14 條「未確認」的處理方式：先重抓官方繁中
  FAQ 1486 全文確認（**8 條繁中未確認的官方一條都沒收**，社群也沒有繁中字串；
  另外 4 條只缺日文，而知識頁只做繁中，不影響），然後**一條都不列繁中寫法**，
  改用一段文字交代「有這些東西、但繁中沒有可靠來源」。
  **底稿 212 行沒有整份搬過去**，頁面只收「讀得懂本站產生的字串」需要的部分
- ~~**A-6** `remote-trade` 遠距離交換~~（2026-09-18 上線，1.16.00，字母退休不回收）。
  **出處比預期硬**：整個機制四條官方講完（FAQ 5312／2847／96 繁中與英文），
  只有沙子費用表與 100 公尺是社群的。**「出處待查」這個判斷是錯的**——
  上一輪只搜了「距離」與「費用」這兩個詞，而官方根本沒用這兩個詞寫，
  它有一整頁專講遠距交換（5312）。**下次先找主題頁，不要先找數字。**
- ~~**A-7** `guaranteed-lucky` 必定變亮晶晶~~（2026-09-18 上線，1.17.00，字母退休不回收）。
  **「上限數字只有社群有」這個判斷也是錯的**——35→45 的官方繁中原句就在
  pokemongo.com 的「新年 2026」活動公告裡，2018 年首次公布保證機制的那則
  也有繁中版。**說明中心只講機制，數字在活動公告裡**，這是找官方數字的
  第二個地方。真正沒有官方依據的只有機率，見下面那條

（順序是按出處確定度排的，不是按重要性。）

**B 背卡清單補缺**。補完重跑 `build-bgflags`，旗標就會自己長出來。

- **B-1** 超極巨化御三家補進 `js/bgevents.js` 的卡片清單，**逐隻補不是整張卡**：
  Delightful Days 帶轟擂金剛猩／閃焰王牌／千面避役，GO Fest 2025 實體場
  三張各帶一隻
- **B-2** 同一種缺口的拉普拉斯、卡比獸與長毛巨魔，見 `tools/bgflags-report.md`

**C 轉私人 repo + Cloudflare Pages + 自有網域**。**C-1 與 C-2 必須一起做**，
轉私人之後免費方案的 GitHub Pages 會停掉。**A 的 SEO 要等這一步才真的兌現**，
兩件事有先後但不互相卡。

- **C-1** 買網域
- **C-2** 轉私人 repo 並切到 Cloudflare Pages

**D 等上游或 GO 更新後重跑**。四支都是等別人動，我們這邊只是重跑。

- **D-1** GO 開放新的寶可夢可以極巨化 → `node tools/build-max.mjs --force`。
  **報告裡多出來的不要默認排除**，這個坑踩過兩次
- **D-2** GO 開放新的寶可夢可以淨化 → `node tools/build-shadow.mjs --force`
- **D-3** Bulbapedia 更新背卡角標 → `node tools/build-bgflags.mjs --force`
  （新的卡要先逐張比對清單內容才補進 `CARD_MANUAL`，不要照名字猜）
- **D-4** 上游把 256x256 那幾種超極巨化補進主目錄 → 重跑 `build-dex`，
  會自動改用主目錄那張（裁切比較好），`GMAX_256` 那幾筆就可以刪了

**E 解除暫時隱藏**。兩筆都是等 GO 實裝，刪掉名單重跑腳本，id 不變。

- **E-1** 阿爾宙斯（刪掉 `HIDDEN_DEX` 裡的 493）
- **E-2** 2026-11 洗翠黏美兒與黏美龍（清掉 `HIDDEN_FORMS`，
  並把 check 那兩條從「不該在圖鑑裡」改成驗屬性是鋼加龍）

**~~F `.DS_Store` 加進 `.gitignore`~~**（2026-09-18 做完，字母退休不回收）。
