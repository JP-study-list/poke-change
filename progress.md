# progress.md — 開發歷史

> 反向時間序，最新在最上面。

---

## 2026-09-16（五）
- 類型：修正
- 影響檔案：js/backgrounds.js, tools/build-max.mjs, js/maxdata.js,
  tools/max-report.md, tools/check.mjs, js/version.js, VERSION.md,
  CLAUDE.md, project-index.md
- 摘要：超極巨化的背卡改成跟著本體，名單補上漏掉的三隻。版本升 1.06.01。
- 原因：使用者截圖指出超極巨化妙蛙花寫「目前沒有活動背卡」，
  問超極巨化或極巨化是不是也該有背卡。
- 查證：
  - **極巨化那一半本來就沒問題**：它是掛在本體條目上的勾選條件，
    本體的背卡清單照舊（妙蛙花 7 張、噴火龍 13 張），
    「本體＋背卡＋極巨化」一直做得出來。
  - 缺的是超極巨化那 13 個新條目。原因是背卡清單來自 Dittobase，
    而它**不收任何特殊型態**（快取裡 gigantamax／dynamax／shadow 都是 0，
    唯一的 6 個 mega 是大竺葵與 Mega Tokyo 那類卡名，不是超級進化）。
    Max Battle 抓到的一律記成本體 id，因為抓到的當下就是本體外觀。
  - **關鍵證據**：`go-fest-2025-eternatus` 這張卡 74 隻裡有 68 隻在可極巨化
    名單裡——那就是一張 Max Battle 的卡。所以在那場活動抓到的超極巨化
    噴火龍，遊戲裡確實帶得了那張背卡。
- 決定（都問過使用者）：
  - **`cardsFor()` 對超極巨化退回本體那一份**。代價是沒有 Max Battle 的
    活動卡也會進下拉，使用者確認接受：要擋得先知道哪幾場活動有
    Max Battle，上游沒有這份資料。
  - **反向不做**。背卡詳情那一面不列超極巨化，否則收集格會憑空多出來，
    而 Dittobase 並沒有說那張卡收得到。收集格仍然是 1579。
  - 顫弦蠑螈沒有本體條目，指名到高調形態，跟 `STATS_SAME_AS` 一致。
- 順帶修掉的一個判斷失誤：
  那張卡剩下的 6 隻裡有**幾何雪花與投擲猴**，正是上一版報告裡
  「只有 game master 有、判定未實裝所以不收」的其中兩隻。
  使用者確認三隻（加毒電嬰）都真的能極巨化，所以 `build-max.mjs`
  改成**兩邊取聯集**，game master 的 `BREAD_MODE` 從佐證升為來源之一。
  名單 145 → 148（d615 幾何雪花、d766 投擲猴、d848 毒電嬰）。
  **教訓**：那份清單不完整（只有需要特調縮放的才在），但裡面的是真的，
  下次報告又列出「只有 game master 有的」不要默默排除，回頭問過。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增 3 條（超極巨化的背卡等於本體、
    本體真的有卡可繼承、背卡清單裡沒有超極巨化）。
  - CDP 實跑（關快取）：`d3.fGIGANTAMAX` 背卡 7 列、`d6.fGIGANTAMAX` 13 列，
    都不再出現「目前沒有活動背卡」；本體仍有極巨化鈕、超極巨化條目沒有
    （它本身就是極巨化，再勾一次是重複）。
  - 截圖確認 Go Fest 2025 Eternatus 排在清單第一個。
- 待辦/已知問題：（無）

---

## 2026-09-16（四）
- 類型：新增
- 影響檔案：tools/build-max.mjs（新增）, js/maxdata.js（新增，自動產生）,
  tools/max-report.md（新增，自動產生）, tools/build-dex.mjs, js/godex.js,
  js/dex.js, js/store.js, js/ui.js, js/main.js, js/share.js, js/i18n.js,
  css/style.css, tools/check.mjs, js/version.js, VERSION.md, CLAUDE.md,
  project-index.md
- 摘要：極巨化做成第五個交換條件，超極巨化 13 隻收進圖鑑。版本升 1.06.00。
- 原因：使用者要求「圖鑑加入極巨化，也要出現在交換列表」。
- 分界（這次最重要的一個決定）：
  **外觀有沒有變**。一般的極巨化在 GO 裡只是個體身上的能力，圖還是同一張，
  所以做成勾選條件；超極巨化那 13 隻上游給了自己的圖與異色圖，
  外觀真的不一樣，所以是條目。使用者選的是勾選框那一半，
  13 隻當條目是提案時一起問的，他選了「順便收」。
  這同時推翻了 CLAUDE.md 原本「極巨化整批排除」那條，已改寫。
- 資料來源的查證（花最多時間的部分）：
  - **game master 給不出「哪些能極巨化」**。兩個看起來像的欄位都不能用：
    `BREAD_POKEMON_SCALING_SETTINGS` 只有 58 個物種，那是視覺縮放表；
    `breadTierGroup` 2467 筆幾乎全有，那是強度分級。
    Max Battle 在遊戲資料裡叫 **bread**、超極巨化叫 **sourdough**，
    搜 dynamax 只會找到一個招式跟一雙手套。
  - 主來源只能是 Dittobase（每筆帶 `isDynamax`／`isGigantamax`），
    145 個條目 id，0 筆對不上。
  - **交叉比對三處差異**：game master 獨有的幾何雪花、毒電嬰、投擲猴
    （未實裝，不收）；Dittobase 說已實裝的超極巨化 17 隻但上游只有 13 張圖
    （皮卡丘、喵喵、灰塵山、積怨番長四隻實測 404，不收成條目，
    但本體仍在可極巨化名單裡）；game master 的 sourdough 31 筆含 14 筆
    還沒實裝的。全部寫進 tools/max-report.md。
  - 抽取踩過一個坑：第一版用「往前找最近的圖片網址」配 slug，
    取成了視窗裡**最前面**那個，整份名單位移一格，
    列出來的超極巨化全是裝扮版。改成解析 `{"slug":...}` 那個物件本身才對。
- 實作決定：
  - **`item.max` 不升儲存版本號**。多一個布林對舊資料相容，
    `cleanItem` 補 false；升 `v` 反而讓舊版整包讀不到。
  - **`normalize` 不拿名單洗紀錄**。名單會隨 GO 更新縮水，
    拿它清掉使用者勾好的值等於默默改掉人家存的東西。
  - **超極巨化的 CP 走 `STATS_SAME_AS` 一筆一筆指名**（13 筆），
    不寫成「查不到退回本體」——那正是原始回歸會拿到蓋歐卡數字的那條路。
    顫弦蠑螈沒有本體，指到高調形態。
  - **徽章在右上角，刪除鈕也在**，兩顆圓差 1.5px 重疊，
    所以 hover 與編輯模式時徽章讓位。
  - **`--max` 深色不覆寫**：`#da0d86` 配白字 4.8:1 兩套底色都過，
    調亮到 `#e83fa0` 只剩 3.7:1。
  - **極巨化不做批次**，跟 XXL／XXS 同一個理由。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增 13 條測試（名單健全性、
    canMax 一致、超極巨化 13 筆與 CP 等於本體、往返保留 max、
    舊紀錄補 false、勾選框只在名單內、徽章只在勾了的那一格）。
  - 13 張超極巨化圖與 13 張異色圖**線上實測全部 200**。
  - CDP 實跑（關快取、資料在載入前注入）：徽章底色 `rgb(218, 13, 134)`、
    白字、14×14、距框右 3 距框上 2；三格只有勾了的那格有徽章；
    噴火龍與妙蛙種子本體有勾選框、裝扮妙蛙種子沒有；
    滑鼠移上去徽章 opacity 1 → 0、刪除鈕 1。
  - **分享圖逐像素驗**：`buildShareImage` 產出的 1320×656 PNG 裡
    洋紅像素 534 個（一顆 r=8 的圓在 2 倍解析度下的合理值），只有一顆。
- 待辦/已知問題：
  - 上游哪天補上皮卡丘、喵喵、灰塵山、積怨番長的超極巨化圖，
    重跑 build-dex 就會長出來，記得在 `STATS_SAME_AS` 補指名。

---

## 2026-09-16（三）
- 類型：修正
- 影響檔案：css/style.css, js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：選寶可夢面板的欄數改成跟著「圖示大小」的設定走。版本升 1.05.03。
- 原因：使用者截圖指出從交換表按加號開出來的面板是大圖示，
  要它連動設定。
- 診斷：圖片本身（`--cell-img`）一直有跟設定走，**寫死的是欄數**：
  桌機單選寫死 3 欄、多選寫死 5 欄。520 寬的彈窗除出來每格 152px，
  中間擺一張 54px 的小圖，看起來就比圖鑑的大圖示還大。
  手機那段早就吃 `--cell-cols`，只有桌機沒跟上。
- 決定：
  - **照設定的欄數講死，不用 auto-fill 推算**（使用者選的，另一個方案是
    跟圖鑑同一條 `minmax(--cell-min, 1fr)` 自己算）。單選走 `--cell-cols`
    （小 5 大 3），桌機手機同一套，大圖示維持原本的三欄。
  - **寬彈窗另記一組 `--pick-wide-cols`**（小 7 大 5）。小圖示單選已經五欄，
    多選再放寬到 720 卻還是五欄的話，格子只是變大到 131px，
    放寬就白費了。大圖示那一格仍是 5 欄，跟改動前一模一樣。
  - **不寫成「`--cell-cols` 加二」**：那是算出來的，而這兩組講的是
    兩種寬度各自排得好看的欄數，沒有固定差距。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - CDP 量 computed `grid-template-columns`（關快取，偏好在載入前注入）：
    桌機 1440 小圖示單選 5 欄／88.4px、多選 7 欄／89.4px；
    大圖示單選 3 欄／151.3px、多選 5 欄／126.8px（後者與改動前相同）。
    88.4 跟圖鑑桌機格子的下限 88px 對得上。
  - **手機 430 一個像素都沒動**：小圖示 5 欄／68px、大圖示 3 欄／116px，
    按下多選欄數與寬度都不變，符合 2026-09-13 訂的那條。
  - 面板開著時去設定點「大」，格子牆當場從 5 欄變 3 欄、圖從 54 變 62，
    設定面板沒被關掉——連動發生在 CSS 變數層，不經過重繪。
  - 截圖看過小圖示單選與多選兩種，名稱沒有撐開格子。
- 待辦/已知問題：（無）

---

## 2026-09-16（二）
- 類型：修正
- 影響檔案：css/style.css, js/version.js, VERSION.md, CLAUDE.md
- 摘要：設定的預覽卡框線統一 1px。版本升 1.05.02。
- 原因：使用者指出選中與未選中的框線粗細不同。
- 決定：選中的那張原本多疊一層 `inset` 陰影，視覺上是 2px，
  跟旁邊擺在一起一眼就看得出兩條線不一樣粗。拿掉陰影，
  **只靠顏色區分**（`--ink` 對 `--line`），那個對比已經夠。
  仍然不改 `border-width`——兩張各差 1px 會讓整組位移。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - CDP 量六張卡：框線一律 `1px`、`box-shadow` 都是 `none`、
    尺寸全部 175.0×131.9 完全一致；選中的邊框色是 `--ink`
    （淺色 `rgb(27,26,23)`、深色 `rgb(233,231,227)`），未選是 `--line`。
    淺色與深色各量過一次。
- 待辦/已知問題：（無）

---

## 2026-09-16
- 類型：修正
- 影響檔案：js/ui.js, css/style.css, js/version.js, VERSION.md,
  CLAUDE.md, project-index.md
- 摘要：背卡詳情的卡面圖限高 38vh。版本升 1.05.01。
- 原因：上一版把右欄換成彈窗之後寬度從 340 變 520，卡面圖等比放大，
  收集格全被推到摺線以下。使用者確認要限高。
- 決定：
  - **寬度用 `auto` 不用 `100%`**。寫 100% 再靠 `object-fit: contain` 收的話，
    留白留在元素框裡，`border-radius` 就跟圖錯開一圈。
    讓元素自己等比縮到最高 38vh，左右留白但圓角貼著圖。
  - inline style 改成 `.card-art`，數字放 CSS 才跟其他版面規則在一起。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - CDP 實跑：桌機 1400×900 彈窗 520×739、卡面圖 342×342（佔彈窗高 46%），
    **八個收集格與多選鈕全在彈窗內**（第一格 top 590、彈窗底 820，
    `scrollHeight - clientHeight` 是 0，完全不必捲）；
    430×860 手機同樣結果（圖 327、佔 47%）。
  - 連點 14 張卡量比例：上游卡面一律 512×512，顯示 342×342，沒有變形。
- 待辦/已知問題：（無）

---

## 2026-09-14（四）
- 類型：重構
- 影響檔案：js/main.js, js/ui.js, css/style.css, tools/check.mjs,
  js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：背卡的右欄改成彈窗，常駐形態整組拿掉。版本升 1.05.00。
- 原因：使用者要求「點下背卡後會跳出彈窗」。
- 決定：
  - **右欄的清單摘要整塊拿掉**（使用者選的，另外兩個方案是「摘要留著、
    只有卡片詳情彈出」與「摘要搬到資訊列」）。分享圖的入口本來就在
    交換表裡，右欄不必替它留第二份。摘要沒了之後常駐形態也沒有存在理由。
  - **連帶刪掉的**：`renderRailSummary()`、`body.rail-pop`、`body.rail-off`、
    `railHasContent()`、1200 那整段常駐樣式、`.rs-*` 摘要樣式、
    `--rail-w` 變數，以及三個繪製函式底下那顆文字關閉鈕 `.btn-close`
    （它是常駐形態專用的，在那裡「關閉」是收掉這筆詳情回到摘要）。
  - 斷點因此只剩 900，1200 那個是常駐右欄專用的。
- 查證：
  - **刪 CSS 時差點刪過頭**。第一次用「右欄摘要那一節到下一節」當範圍，
    結束錨點抓成很後面的「圖鑑格狀」，`.views`、`.opt-*`、篩選 chip
    等 641 行一起被刪掉。`git diff --stat` 立刻看得出來（641 刪除對
    預期的一百多行），還原重做，第二次用行號精準切，只刪 149 行。
    **刪整節之前先看 `git diff --stat` 的數字對不對。**
- 驗證：
  - `node tools/check.mjs` 全部通過；`renderRailSummary` 的三條測試
    連同函式一起移除，其中「分享鈕是灰的」那條改成只驗交換表那一顆。
  - CDP 實跑（1400 與 430 兩種寬度，關快取）：沒點卡片時 `.rail` 是
    `display: none`、內容區寬度不變（1364／394）；點卡片後彈窗
    `position: absolute`、置中偏移 0.0、寬 520／402，**內容區寬度仍然
    是 1364／394**（不再被推窄）；紅 X 在、文字關閉鈕不存在；
    詳情裡的多選鈕還在；關掉之後回到 `display: none`。
  - 截圖：桌機卡片牆從四欄變六欄，彈窗置中浮在上面。
- 待辦/已知問題：
  - 彈窗比原本的右欄寬（520 vs 340），卡面圖跟著變大，收集格要捲一下
    才看得到。要不要給圖限高還沒決定。

---

## 2026-09-14（三）
- 類型：重構
- 影響檔案：js/ui.js, css/style.css, js/i18n.js, js/main.js,
  tools/check.mjs, js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：顯示設定的三項從開關改成兩張預覽縮圖二選一。版本升 1.04.02。
- 原因：使用者拿自己另一個站的設定面板當參考，要求「有質感一點」。
  那張圖的語彙是預覽縮圖加卡片，不是一行一個小圖示。
- 決定：
  - **三項都做縮圖**（使用者選的，另外兩個方案是「只有深淺色做縮圖」
    與「三項都做卡片列加開關」）。這三項講的都是畫面會長什麼樣，
    畫出來比寫出來好懂，並排也看得出彼此是同一類設定。
  - **是選值不是 toggle**。兩張卡各代表一個值，點已經選中的那張
    不該把它關掉——那會變成兩張都沒選。`data-val` 帶 0／1，
    `main.js` 改成設值。
  - **外觀那兩張的顏色寫死**，不吃 CSS 變數：它們預覽的正是兩套配色本身，
    跟著當前主題走的話兩張會長得一模一樣。數字抄自 `:root` 與 `body.dark`，
    跟 `js/share.js` 同一個道理，改配色時這裡要跟著改。
  - **選中靠 inset 陰影加粗邊框，不改 `border-width`**：1px 換 2px 會讓
    兩張各差 0.5px，選一下整組跟著位移。
  - 字串重寫過：`bigIcons`／`showNames`／`theme` 三個 key 換成九個
    （每項一個標題加兩個選項名），舊的三個沒有別處在用，直接移除。
  - 上一版才加的圓形圖示開關（`DISP_ICONS`）整組拿掉，縮圖就是圖。
- 查證：
  - 第一版縮圖用 `viewBox 0 0 100 72`，整個「顯示」區在手機上佔滿一屏。
    壓成 55 高、格子尺寸也縮一號之後，三組加起來大約半屏。
  - 名稱那一項的兩張**格子位置要一樣**，只差有沒有那條線；
    照高度置中算的話有線的那張會整排上移，看起來像在比別的東西。
- 驗證：
  - `node tools/check.mjs` 全部通過，原本那條「顯示選項的圖示」改寫成
    驗六張卡、每項剛好一張選中、`data-val` 兩個值都在、深色開了之後
    選中的那張要跟著換。
  - CDP 實跑（430 寬）：初始 5 欄，點「大」變 3 欄且兩張的 pressed 對調；
    **再點一次「大」仍然是大**（不會兩張都沒選）；點「不顯示」之後
    `body.no-names` 為 true、`.nm` 的 computed display 是 none；
    深色與淺色來回切，`body.dark` 與偏好都跟著走。
  - 淺色與深色各截圖看過，選中的那張是粗框加金色格子。
- 待辦/已知問題：（無）

---

## 2026-09-14（二）
- 類型：修正
- 影響檔案：js/ui.js, css/style.css, tools/check.mjs, js/version.js,
  VERSION.md, CLAUDE.md, project-index.md
- 摘要：設定面板那三個顯示選項改成圖示開關。版本升 1.04.01。
- 原因：使用者要求「有點圖示的效果，不是只有打勾」。
- 決定：
  - **圖示本身就是開關**（使用者選的，另外兩個方案是「圖示加右邊滑桿」
    與「圖示加原本的勾」）。關著是線條加淡框，開著整顆填金、圖示轉白，
    跟全站其他 `aria-pressed` 的鈕同一種語彙。方框勾整個拿掉——
    留著的話一行裡兩個東西都在講狀態。
  - **深色模式的圖跟著狀態換**（太陽↔月亮）。那一項的兩個狀態各自有
    公認的樣子，只換顏色等於浪費了這件事。另外兩項沒有這種對照，
    硬要換只會變成兩個都看不懂的圖。
  - 圖示放在 `js/ui.js` 的 `DISP_ICONS`，跟 `VIEW_ICONS` 同一種寫法。
- 查證：
  - 圓形徽章 28px、圖示 16px。金底配白圖示沿用原本勾框的組合，
    `--gold` 只做填色這條規則沒有被破壞（文字仍是 `--ink`）。
  - **截圖驗證踩到一次假象**：第一次 `--screenshot` 出來整張空白，
    是 probe 頁的 script 跑在 `<body>` 建立前，`appendChild` 沒接上。
    加 `<body>` 就好。空白不代表樣式有問題。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增一條驗三項各有一個 svg、
    包在 `.ic` 徽章裡、深色關著是太陽開著是月亮。
  - 截圖看過四種：淺色（名稱開著是金色、另外兩項線條、太陽）、
    深色全開（三顆金徽章、月亮），以及 3 倍解析度的細節。
- 待辦/已知問題：（無）

---

## 2026-09-14（一）
- 類型：修正 + 新增
- 影響檔案：css/style.css, js/ui.js, js/main.js, js/i18n.js,
  tools/check.mjs, js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：修好背卡格子比旁邊低一截；手機加編輯模式（鉛筆）才顯示刪除鈕。
  版本升 1.04.00。
- 原因：使用者在手機上看到有背卡的那一格「浮在空中」，
  順帶指出手機沒有刪除鈕，要求做成鉛筆切換。
- 查證（浮在空中）：
  - 量到有背卡的格子 tile 在 @373、沒背卡的在 @366，名稱跟著低 7px，
    而 cell 與 tile 的尺寸都一樣。**是 `<img>` 的 inline 基線**：
    圖用 `position: relative` 加 margin 撐留白，inline 的替換元素會壓在
    行盒基線上，margin 越大沉得越多——背卡那批是 18%、沒背卡的是 8%，
    差出來的就是那 7px。
  - 改成 `position: absolute` 加 `inset`，脫離行流就沒有基線可言。
    **元素框仍然等於圖框**（寬高照舊 84%／64%），所以 extra 那批
    靠 translate 修偏移的圖不受影響——那個百分比是相對元素算的。
  - 修完量到五格 tile 全在 @361、名稱全在 @430，格子高度從 109 收到 97
    （那 12px 就是原本讓給基線的空間）。帶倍率的圖另外量過，
    視覺主體在有無背卡的格子裡都是 41px，跟一般圖一致。
- 決定（編輯模式）：
  - **鉛筆放每一欄的標題右邊，只切換自己那一欄**。刪除本來就是欄內的事，
    而且想要有九隻、可以給空著的時候，沒必要兩欄一起變成滿畫面的紅點。
  - **只有觸控裝置看得到鉛筆**（`@media (hover: none)`）。桌機把滑鼠移過去
    就有叉了，再放一顆切換鈕是同一件事的第二條路。
  - **空的那一欄不畫鉛筆**。沒有東西可刪時給一顆鈕只會讓人按了沒反應。
  - **編輯模式下點格子照樣開詳情**，只有叉是刪除。誤觸的代價差太多。
  - 狀態在 `state.edit`（兩欄各一個布林），不寫進偏好，重整回到關著。
- 查證（編輯模式）：
  - **`const edit` 撞名**：click 委派裡早就有一個 `const edit = el(".d-edit[data-col]")`，
    同一個函式再宣告一次是 SyntaxError，整個 `main.js` 不會執行——
    畫面只剩骨架，而 `check.mjs` 不載入 main.js 所以照樣全過。
    改名成 `pencil`。**改 main.js 的委派一定要實跑，check 保證不到那裡。**
  - `hover` 這個媒體特性照例用啟動參數驗（`--blink-settings=primaryHoverType=1,
    availableHoverTypes=1`），CDP 的 `setEmulatedMedia` 對它無效。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增一條驗鉛筆兩顆、只有開著那一欄帶
    `editing`、另一欄不跟著開、空清單不給鉛筆。
  - 觸控模式實跑（430 寬）：`matchMedia("(hover: none)")` 為 true，
    鉛筆兩顆都是 `display: grid`；沒開編輯時叉是 `display: none`，
    開了之後想要那一欄的叉 `display: grid`、`opacity: 1`，
    可以給那一欄仍然 none；叉的圓心正好壓在圖框右上角（偏移 0,0）；
    點一下少一格、localStorage 也少一筆、仍留在編輯模式可以連刪；
    刪光之後那一欄的鉛筆消失、另一欄還在。
  - 桌機（有 hover）同一段流程：鉛筆兩顆都是 `display: none`，
    叉仍然是 hover 才浮出來（`opacity: 0`），行為沒變。
- 待辦/已知問題：
  - 加號面板的篩選改變後，已選但被篩掉的仍留在 `sel` 裡（1.01.00 留下的）。

---

## 2026-09-13（八）
- 類型：新增
- 影響檔案：js/i18n.js, js/ui.js, js/main.js, css/style.css,
  tools/check.mjs, js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：背卡詳情可以多選一批直接加進清單，自動帶那張背卡。版本升 1.03.00。
- 原因：接續上一筆。使用者問多選時要特定背卡怎麼辦，討論後決定
  背卡不做在加號的面板裡，改走背卡檢視。
- 決定：
  - **入口在背卡檢視不在加號面板**（使用者選的）。加號那邊要塞背卡就得
    列出「這批全部都帶得了的卡」，十隻一選交集常常是空的，而空下拉
    無從解釋。從背卡進來反過來：卡是已知的，選的是要收哪幾隻。
  - **底部是兩顆鈕**（加進想要／加進可以給）。從交換表按加號是「從某一欄
    進來的」，這裡沒有那個脈絡；而且同一張卡常常是想要幾隻、可以給幾隻。
  - **加完留在原地**（`stay`），不像加號那條路會跳回交換表。使用者還在
    看這張卡，跳走等於要自己找回來。選取清掉所以整片重畫，
    但把 `#panel.scrollTop` 放回去——103 格的卡加完彈回最上面很難用。
  - **選取跟著卡片走，模式記著**。換一張卡就清 `bgSel`（兩張卡的清單
    根本不是同一批），但 `bgMulti` 留著，一張一張卡收下去不必每張都再按。
  - **異色開關共用 `state.pickShiny`**。兩個地方是同一個概念，
    分成兩個值只會變成「在這裡開了，換個地方又要再開一次」。
    委派認到 `data-pickshiny` 之後看自己是哪一邊，重畫自己那一條。
  - **從背卡點單隻進詳情，草稿預先配好那張卡**（使用者確認）。
    使用者就是在那張卡的清單裡點的，再叫他從下拉挑一次同一張沒有道理。
- 查證：
  - **底部在桌機放不下一行**：背卡的右欄是常駐的，只有 340 寬，
    左半邊加兩顆鈕擠不下。`.pick-foot` 改成 `flex-wrap: wrap`，
    兩顆自己佔一行對半分。實測 340 寬兩顆是 137／150 同一行、沒有溢出，
    英文最長的「Add 0 to Want」在 103 格那張卡上也是 144／143。
  - **加號面板那條沒有被 wrap 影響**：430 寬選 12 隻時仍是單行，高 66。
  - **`renderCardDetail` 單選時要自己收掉底部那條**。原本只在多選時填，
    清的責任在 `drawDetail()` 第一行——但 check 是直接呼叫繪製函式的，
    這樣會把上一條測試留下的動作列算成自己的。跟 `renderPicker` 對齊，
    非多選就 `railFoot("")`，順便讓這個函式自己講得完整。
  - **重複判斷要連背卡一起比**。原本寫死 `!x.bg`，帶背卡加進去之後
    再加一次會判成不重複。改成 `x.bg === bg`。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增一條驗多選時格子認得出來、
    底部兩顆鈕與異色開關都在、沒有清單的卡不給多選鈕、單選要收掉整條。
  - CDP 實跑（1400 寬桌機常駐右欄，關快取）：多選鈕在、勾圈只有選起來的有、
    捲到 200 選三隻位置不動、加入後 localStorage 三筆都帶
    `bg: "gf26-chicago"` 與 `shiny: true`、加完仍在同一張卡且 scrollTop
    還是 200、選取歸零；關掉多選底部整條收掉、格子還在。
  - 430 手機（彈窗形態）同一段流程結果相同，兩顆鈕 168／181 同一行。
  - 「加進可以給」那顆另外跑過一次，資料進的是 have。
  - 單選點一隻，詳情的背卡確實預選成剛才那張卡。
  - 英文與深色各看過一次。
- 待辦/已知問題：
  - 前面那條還在：加號面板篩選改變後，已選但被篩掉的仍留在 `sel` 裡。
    背卡這邊沒有這個問題，它沒有篩選。

---

## 2026-09-13（七）
- 類型：新增
- 影響檔案：js/i18n.js, js/ui.js, js/main.js, css/style.css,
  tools/check.mjs, js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：多選的底部動作列加整批異色開關。版本升 1.02.00。
- 原因：使用者問多選時要特定背卡或異色怎麼辦。討論後拆成兩件事，
  這是第一件；背卡走背卡檢視，另一件另外做。
- 決定：
  - **只有異色做批次**（使用者選的）。一批十隻常常整批都是異色，
    但一批十隻不會整批都是 XXL——XXL／XXS 是個體大小，
    放進來只是把底部那一列擠滿，手機尤其。
  - **沒有實裝異色的那 50 筆照一般色加，不略過**（使用者確認）。
    略過等於默默少了幾隻，使用者回頭數才會發現。照加至少東西在清單裡，
    差別在 toast 多一句「N 隻沒有異色，照一般色加」。
  - **`state.pickShiny` 放在 `state.pick` 外面**，跟 `pickFilter`／`pickMulti`
    同一個理由。而且**切回單選不清掉**：連加兩批異色不該要按第二次，
    單選那條路有自己的草稿，這個值在那裡看不到也用不到。
  - 開關用詳情面板那顆一模一樣的 `.mk.shiny`，全站只有一種「異色」的樣子。
- 查證：
  - **按異色不能重畫整片**，跟點格子同一個坑：`#panel` 是捲動容器，
    而按這顆的時機多半是已經往下選了一段。只重畫底部那一列，
    實測捲到 400 按一下，仍然是 400。
  - **重複判斷要拿實際會寫進去的值去比**，不是拿開關的值。
    沒有異色的那幾隻開著也只會寫 `shiny: false`，
    拿 `true` 去比的話清單裡已經有的那一筆會判成不重複，變成加了兩筆一樣的。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增一條驗開關預設關、開著要標 pressed。
  - CDP 實跑（1400 寬，關快取）：異色鈕預設 `aria-pressed="false"`，
    按下變 `true`、底色 `rgb(201,79,124)`（`--shiny`），`#panel.scrollTop`
    維持 400；選五隻（含妙蛙花的 `fCOPY_2019`，沒有實裝異色）按加入，
    localStorage 存到四筆 `shiny: true` 加一筆 `false`，
    toast 是「加了 5 隻 · 1 隻沒有異色，照一般色加」。
- 待辦/已知問題：
  - 背卡的批次還沒做，接著做（從背卡檢視多選一批，自動帶那張卡）。
  - 前面那條還在：篩選改變後，已選但被篩掉的仍留在 `sel` 裡，按加入會一起加。

---

## 2026-09-13（六）
- 類型：修正
- 影響檔案：css/style.css, js/version.js, VERSION.md, CLAUDE.md
- 摘要：手機按下多選不再重排版面，只有按鈕變色。版本升 1.01.01。
- 原因：使用者在 iPhone 上實際用到，點多選整片格子牆跳掉。
- 查證：
  - 手機單選本來就是一排五隻——`.grid` 在 900 以下吃 `--cell-cols`（5），
    寫在 media 外的 `.pick-grid` 三欄被它蓋掉。多選卻掉回三欄，
    因為 `body.pick-wide .pick-grid` 是寫在手機那段裡面的。
  - 另一個沒被發現的：`body.pick-wide .rail-inner`（0,2,1）特異性高過
    手機的 `.rail-inner`（0,1,0），所以手機多選時左右留白從 28 變成 48，
    面板整個縮一圈。原本的註解寫「手機那段的寬度仍然蓋過這裡」，那是錯的。
- 決定：
  - **兩條 pick-wide 規則一起關進 `@media (min-width: 901px)`**，
    手機完全不套。不是在手機那段各補一條覆寫回去——那正是上面第二點
    出錯的原因，靠特異性打架遲早再錯一次。
  - **桌機的放寬保留**。520 挑二十隻要捲七排，那個理由沒有變。
  - 狀態顯示只剩那顆鈕（`aria-pressed="true"` 填實成黑底白字），
    樣式本來就有，不必新增。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - CDP 量測（430 寬 iframe，headless 視埠下限 500px 量不到真手機寬度）：
    多選前後 `grid-template-columns` 都是五個 68px、`.rail-inner` 都是 402、
    第一格都是 68×90 @33,197.1，逐項相同；只有按鈕底色從
    `rgb(255,255,255)` 變 `rgb(27,26,23)`。無橫向溢出（scrollWidth 430）。
  - 1400 寬同一段量測：520→720、三欄 152.7 → 五欄 128.4，桌機行為沒變。
- 待辦/已知問題：
  - 前一版那條還在：篩選改變後，已選但被篩掉的仍留在 `sel` 裡，按加入會一起加。

---

## 2026-09-13（五）
- 類型：新增
- 影響檔案：index.html, css/style.css, js/ui.js, js/main.js, js/i18n.js,
  tools/check.mjs, js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：選寶可夢面板加漏斗與多選，一次加很多隻。版本升 1.01.00。
- 原因：使用者要求。原本一次只能加一隻，而且那個面板沒有篩選。
- 決定：
  - **單選那條路原封不動**（使用者選的）。點一隻仍然切到詳情、配背卡、
    加進去。多選是另一個模式，靠面板上那顆鈕切換。
  - **多選加入的一律不帶條件**。逐隻配背卡本來就得一隻一隻來，
    那是單選在做的事；多選要的是快。
  - **篩選與圖鑑那份分開**（使用者選的）。在圖鑑篩了只看傳說，
    按加號看到一片空白是找不出原因的。反過來也一樣。
  - **篩選與多選模式記到重新整理為止**（使用者選的），跟圖鑑的篩選同一個規則，
    不寫進偏好。放在 `state.pickFilter` / `state.pickMulti`，不是放在
    `state.pick` 裡面——後者關一次面板就沒了，等於沒記。
  - **多選時面板放寬到 720、一排五隻**。520 是常駐右欄時代的寬度，
    挑二十隻要捲七排，捲動本身就是瓶頸。手機維持三欄，430 的螢幕上
    五欄每格只剩 66px。
  - **不做全選鈕**。一次最多畫 150 筆，一鍵加 150 隻的誤觸代價太大。
- 查證：
  - **點一格不能重畫整片**。第一版沿用 `drawDetail()`，實測發現
    `#panel` 是捲動容器，換掉 innerHTML 會把 scrollTop 歸零——
    選到第七排點一下就彈回最上面，而多選正是要一路往下選。
    改成只改那一格的 class 與底部的數字，`renderPickFoot` 因此獨立出來。
    實測捲到 600 再點一格，位置仍是 600。
  - **沒選的格子不畫勾圈**。第一版每格右上角都有一個灰圈，
    150 格就是 150 個雜訊，跟手機不放刪除鈕同一個理由。
    改成只有選起來的才有，桌機滑鼠移過去淡淡提示一下。
  - **底部動作列要放在捲動區外面**，所以加在 `index.html` 的骨架裡
    （`#pickFoot`）。它不隨內容被換掉，所以清的責任放在 `drawDetail()`
    第一行，不是要每個繪製函式自己記得。
  - **兩份篩選的 dataset 名字不能一樣**。都叫 `data-group` 的話
    `main.js` 的委派兩邊都接到，在加號的面板裡篩一下圖鑑也跟著變。
    面板用 `data-pgroup` / `data-popt` / `data-pdrop`，
    產生 HTML 的兩個函式抽出來共用，屬性名由呼叫端給。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增「套篩選」與「多選」兩條，
    後者連「切回單選底部那條要收掉」一起驗。
  - CDP 實跑（關快取）：漏斗展開五組、選火從 1460 剩 102、chip 與
    漏斗數字都對、圖鑑的篩選沒被動到；關掉面板再開篩選還在；
    切到圖鑑再回來也還在。
  - 多選實跑：選六隻、捲動後再選一隻、取消一隻，底部數字同步；
    按加入之後 localStorage 多六筆且四個條件都是空的，交換表格子數相符。
    同一批再加一次得到「加了 1 隻 · 2 隻重複略過」——那一隻是剛才取消的。
  - 版面：桌機淺色與深色、430 手機都看過。手機三欄、沒有橫向溢出，
    底部動作列在視窗內（733~799，視窗高 860）。
- 待辦/已知問題：
  - 篩選改變後，已經選起來但被篩掉的那些仍留在 `sel` 裡，按加入會一起加。
    底部的「已選 N 隻」看得到總數，所以不算藏起來，但畫面上看不到那幾隻。

---

## 2026-09-13（四）
- 類型：修正
- 影響檔案：js/ui.js, js/main.js, tools/check.mjs, js/version.js,
  VERSION.md, CLAUDE.md
- 摘要：詳情面板上方那張圖改成跟著草稿的異色走，異色預設不勾。版本升 1.00.03。
- 原因：使用者指出點了異色沒有任何反應，而且預設就勾著。
- 查證：
  - 開關本來就有作用（`state.draft.shiny` 有翻，`drawDetail()` 也重畫了），
    問題在 `renderDetail` 的 `<img>` 寫死 `iconAttrs(e, false)`，
    整個面板重畫完仍然是一般色的圖。不是事件沒接上。
  - 預設值在 `main.js` 的 `newDraft()`，原本是 `!!e.shinyIcon`，
    所以 1410 個有異色的條目一打開就勾著。
- 決定：
  - **上方那張圖吃草稿的 `shiny`**。異色鈕只在 `hasShiny(e)` 時才畫，
    所以 `d.shiny` 為真就一定有 `shinyIcon`，不會挑到不存在的圖。
  - **預設改成四個條件都不勾**。原本的註解說「沒有異色可收的就不預設勾」，
    立意是省一次點擊，但常見情形是一般色；而且圖現在會跟著變，
    一打開就是異色反而會看錯是哪一隻。
  - `newDraft()` 不再需要 `id`，順手把參數與兩個呼叫點清掉，
    連帶清掉 `main.js` 兩個已經沒人用的 import（`find` 是這次變成沒用的，
    `fullName` 早就沒用了）。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增一條「勾異色會換成異色圖」，
    驗預設是 `icon`、勾了變 `s.icon`。
  - CDP 實跑（關快取）：超夢預設 `aria-pressed="false"` 顯示 `pm150.icon.png`，
    點異色變 `pm150.s.icon.png` 且 `naturalWidth > 0`（圖真的載到，不是破圖），
    再點一次回到一般色，加進想要之後 localStorage 存的是 `shiny: false`。
- 待辦/已知問題：（無）

---

## 2026-09-13（三）
- 類型：修正
- 影響檔案：css/style.css, js/ui.js, js/version.js, VERSION.md,
  CLAUDE.md, project-index.md
- 摘要：交換表格子的刪除鈕改成紅圓白叉，圓心壓在圖框右上角一半在外。
  手機上整顆不顯示，改走詳情面板的移除。版本升 1.00.02。
- 原因：使用者要求。原本那顆是灰底灰叉蹲在方塊裡，
  背卡底圖改成原樣顯示之後就壓在圖上看不清了。
- 決定：
  - **溢出只取一半**（`-8px` 等於半徑），不是使用者一開始說的 ¾。
    格子間距只有 6~8px，¾ 大約 13px 會整顆跨進隔壁格子；
    而且交換表的格子刻意不畫框，沒背卡的格子上那顆會懸在空白裡。
    使用者看了兩種的示意之後選了一半。
  - **紅色用 `--x-bg` 不用 `--danger`**，跟彈窗右上角那顆關閉鈕同一個。
    全站只有一種「按了會收掉東西」的紅圓白叉。叉也換成同一段 SVG。
  - **手機整顆不放**（使用者選的）。一頁最多兩百隻，常駐就是兩百顆紅點，
    跟「格子不畫框」是同一個理由。原本想做成「點格子才出現」，
    但手機點格子已經是開詳情面板了，沒地方站；
    而詳情面板每一筆本來就有一顆「移除」，路徑等價且不必加狀態。
- 查證：
  - **`.want-tile` 的 `overflow: hidden` 必須拿掉**才溢出得了。
    它原本的用途是裁背卡底圖的圓角，改成底圖自己帶 `border-radius`，
    視覺等價。
  - **拿掉之後量到一個真的問題**：extra 那批外部圖靠 CSS 變數做
    `transform: scale()`，實測倍率 2.46，元素框向上溢出 61px、向左 35px。
    溢出的那一圈全是透明留白，看不見，卻照樣接得到點擊，
    會搶走隔壁格子與上方區域的 hit-test。在那之前是被裁掉的。
    修法是給 `img[style*="--iz"]` 補 `pointer-events: none`，
    圖本身沒有互動，點擊一律由格子委派。
  - **圖鑑的格子一直沒有裁切**，所以同一批圖在那邊本來就溢出，
    這次那一行補在共用規則上，順帶把圖鑑既有的同類問題一起修掉。
  - **紅圓永遠壓在圖上面**：`.want-tile` 沒有 z-index 不建立 stacking
    context，所以紅圓的 `z-index: 2` 是跟隔壁格子的圖直接比，贏。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - CDP 實跑（關快取、載入前種資料）：桌機淺色與深色、430 寬、
    有背卡與沒背卡、異色、XXL、extra 圖各看過。
  - **真的用滑鼠事件點下紅圓中心**：命中的是 SVG 的 `path`，
    格子從 14 掉到 13，localStorage 同步，而且詳情面板沒有被連帶打開
    （`stopPropagation` 仍然有效）。
  - **命中測試**：格子上方 30px 落到 section 不屬於任何格子、
    隔壁格子左緣落到隔壁自己、紅圓中心落到自己那格。修之前上方那一點
    會落到第 0 格的圖。
  - **每列最右邊那格與最靠近欄界那格都看過**，紅圓沒有被面板裁掉，
    也沒有侵入另一欄。
  - **手機那條要用 Blink 啟動參數才驗得到**：
    `Emulation.setEmulatedMedia` 的 `features` 改不動 `hover`，
    `matchMedia('(hover: none)')` 仍然是 false，第一次截圖看起來對
    只是因為沒送滑鼠、`opacity` 本來就是 0。改用
    `--blink-settings=primaryHoverType=1,availableHoverTypes=1` 之後
    才真的量到 `display: none`。
  - 手機路徑實跑：點格子開詳情、面板裡兩顆「移除」（同一隻兩筆）、
    按下去格子從 14 掉到 13，localStorage 同步。
- 待辦/已知問題：（無）

---

## 2026-09-13（二）
- 類型：修正
- 影響檔案：tools/measure-icons.mjs（新）, js/extra.js, js/dex.js,
  js/imgchain.js, js/share.js, css/style.css, tools/check.mjs,
  js/version.js, VERSION.md, CLAUDE.md, project-index.md
- 摘要：26 個外部來源的裝扮圖示過小，改成逐張放大並修正偏移。版本升 1.00.01。
- 原因：使用者指出圖鑑裡有一批特別小。
- 查證：
  - **根因是圖本身有透明留白，不是 CSS 寫錯**。
    抽樣 19 張 PokeMiners 的圖，主體佔畫布 96.4~99.2%，緊貼裁切。
    Choggor 與 Dittobase 的 26 張一律畫在 256×256 的畫布上，
    主體只佔 36.7~43.0%。`object-fit: contain` 依畫布縮放，所以差了一倍半。
  - **主體還偏下約 19%**（offY 0.172~0.215），偏右約 5~8%。
    PokeMiners 那批是 0.00~0.01，幾乎正中央。
- 決定：
  - **逐張量，不乘同一個數字**（使用者選的）。26 張的留白各不相同，
    統一倍率仍會差 15 個百分點，最大的那幾張還會滿出格子。
  - **目標是主體佔圖框 98%**，對齊 PokeMiners 那批量到的 96~99%。
  - **平移不能省**。第一版只放大，結果偏移也跟著放大，
    實測腳壓到格子底下的名稱。加了 translate 之後主體落在 15.5~68.5，
    跟正常圖的 15~69 一致。
  - **元素必須等於圖框**，否則 translate 的百分比基準不對：
    圖鑑格子把帶倍率的圖收成正方形，交換表的留白從 padding 改成 margin
    （視覺等價，方塊是正方形、contain 後圖框一樣大）。
  - **工具不自動改 extra.js**。那是手寫維護的檔，有註解與排版，
    量出來的數字自己貼。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增一節驗每筆外部圖都有三個值、
    倍率算得出來且不超過 3。
  - `node tools/measure-icons.mjs` 29 筆的數字都跟圖片對得上。
  - 四個使用點實跑：圖鑑格子、交換表格子、詳情面板的大圖、分享圖。
    把正常的與修正過的排在一起比，大小一致。
  - **備援測過**：把圖的 src 換成不存在的網址，`__imgfb` 退回官方立繪之後
    style 被清空，不會拿裝扮的倍率去放大一張滿版的立繪。
  - 430 寬與淺色也看過。
- 待辦/已知問題：（無）

---

## 2026-09-13（一）
- 類型：新增
- 影響檔案：js/version.js（新）, VERSION.md（新）, index.html, js/ui.js,
  js/i18n.js, css/style.css, tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：加版本號。設定面板最底下顯示「版本 1.00.00 · 2026-09-13」，
  發布紀錄寫在根目錄的 `VERSION.md`。
- 原因：使用者要求，想在設定裡看得到版本，並且自己有一份 md 記錄。
- 決定（都是使用者選的）：
  - **語義是「大改版.新功能.修正」**，後兩段補兩位（`1.02.03` 不是 `1.2.3`）。
    每次 push 前由我判斷升哪一段，並講一句理由。
  - **起始值 1.00.00**。站已經上線、功能完整，沒理由說自己還沒到 1.0。
  - **顯示版號加日期**，這樣在手機上看得出來是不是最新的。
- 設計：
  - **真相來源在 `js/version.js`，不是 md**。JS 讀不到 md，要讀得 fetch，
    多一次請求又可能失敗。md 是給人看的紀錄。
  - **`check.mjs` 釘住兩邊一致**：md 最上面那筆的號碼與日期必須等於模組的值，
    另外驗號碼是 `x.xx.xx`、日期是 `YYYY-MM-DD`。
    故意把模組改成 1.00.01 跑過一次，確實擋下來。
  - **跟儲存格式的 `v: 2` 無關**，那是資料結構的版本，已在 CLAUDE.md 註明。
  - 版本號是註腳不是設定項，所以不包成 `side-block`，
    只有一條上緣髮絲線隔開，字級用既有的 `--fs-sm`，不新增字級。
  - i18n 只加一個 key（標籤「版本」），號碼與日期三語共用同一個寫法。
    三語各 102 → 103。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - 起 server 實跑三語：版本 1.00.00 · 2026-09-13／
    バージョン 1.00.00 · 2026-09-13／Version 1.00.00 · 2026-09-13。
  - **又踩到快取**：改了 `index.html` 之後瀏覽器拿舊的，`#verLine` 查不到，
    看起來像沒生效。截圖工具加上 `Network.setCacheDisabled` 之後正常。
    這跟前一筆 `js/share.js` 是同一個坑。
- 待辦/已知問題：（無）

---

## 2026-09-12（十七）
- 類型：修正
- 影響檔案：css/style.css, js/share.js, CLAUDE.md
- 摘要：交換表格子的背卡底圖不再套濾鏡，改成把寶可夢縮小讓背卡露出來。
  畫面與分享圖同一套值。
- 原因：使用者要求。底圖壓到半透明之後背卡認不出來，
  而背卡的清單是這個專案的差異化，看不清等於沒有。
- 決定：
  - **只縮有背卡的格子**。選擇器是 `.want-bg + img`（底圖那一層畫在
    `<img>` 前面），沒指定背卡的維持原本大小——那些格子沒有要讓給誰。
  - **18% 內距（圖示 64%）**。分享圖的 `drawContain` 用 0.64 對齊，
    沒有背卡的維持 0.78。
  - 深色也拿掉濾鏡，不留 0.42。留著的話深色仍然看不清背卡，
    等於這件事只做了一半。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - 種了 12 筆（9 筆有背卡，含亮的東京鐵塔、暗的隊長紅與 GO Fest 紫）
    實跑淺色與深色：背卡認得出來，寶可夢縮小後仍然辨識得出來，
    異色星星在亮底與暗底上都看得見。
  - 分享圖淺色與深色各產一張，與畫面一致。
  - **踩到一個坑**：改 `js/share.js` 之後瀏覽器用了快取的舊模組，
    第一張分享圖還是舊的半透明。import 加 query 才看到新的。
    之後驗 canvas 的改動要注意這件事。
- 待辦/已知問題：（無）

---

## 2026-09-12（十六）
- 類型：修正
- 影響檔案：js/main.js, css/style.css, CLAUDE.md, project-index.md
- 摘要：交換表的右欄在桌機也改成置中彈窗，跟圖鑑一樣。
  `rail-pop` 的判斷從「是圖鑑」改成「不是背卡」，CSS 一行都沒改。
- 原因：使用者要求。點加號跳出的選寶可夢面板原本在 1200 以上是常駐右欄，
  會把交換表的兩欄推窄。
- 決定：
  - **條件寫成排除背卡，不是列舉圖鑑與交換表**。形態取決於
    「這個檢視的右欄要不要一直在」，那只有背卡是真的
    （沒點卡片時擺清單摘要）。之後加檢視不必再改這一行。
  - **選寶可夢面板與條目詳情一起改**（使用者選的）。從加號選了一隻會切到
    詳情、按返回鈕回選面板，兩者形態不一致的話按一次返回版面就跳一次。
  - 順手修掉 `css/style.css` 一行過時註解：`.rail-inner` 上面寫
    「900 以下改成貼底升起」，但（十一）那筆已經把手機也改成置中了。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - 1440 寬實跑：加號的面板置中、遮罩在、右上角紅 X，格子牆沒被推窄
    （`.content` 仍是 1440）。點一隻進詳情，形態一樣、返回鈕在左上，
    底下那顆文字關閉鈕自動收起（`.btn-close` 是 `none`、`.btn-x` 是 `grid`）。
  - 返回鈕來回切過：回到面板時搜尋框還在、返回鈕消失，形態不跳。
  - 關掉之後 `body` 是 `rail-pop rail-off`、`#sheet` 沒有 `is-open`，
    `.rail` 預設 `display: none` 所以沒有殘影擋畫面。
  - **背卡沒有受影響**：body 不帶 `rail-pop`、右欄仍是 sticky，
    `.content` 1100 寬（右欄佔 340）。
  - 430 寬實跑：置中彈窗、一排五隻、底部 bar 露在遮罩外，與改動前相同
    （1200 以下本來就是彈出）。
- 待辦/已知問題：
  - 選寶可夢面板沿用同一個殼，寬度 520，桌機一排三隻要捲比較久。
    這是改動前常駐右欄就有的寬度，沒有變差，要放寬得另外談。

---

## 2026-09-12（十五）
- 類型：修正
- 影響檔案：tools/build-dex.mjs, js/godex.js, tools/check.mjs,
  CLAUDE.md, project-index.md
- 摘要：基格爾德從 5 筆併成 3 筆，順帶補齊三個中文型態名。條目 1462 → 1460。
  - `COMPLETE_FIFTY_PERCENT` 與 `COMPLETE_TEN_PERCENT` 併進不帶前綴的那兩筆。
  - 留下來的三個代碼在語言檔查不到中文，譯名補進 `FORM_OVERRIDE`。
- 原因：使用者說留一組就好。那兩組的圖檔逐位元組相同、基礎數值也相同，
  差別只在細胞收集滿了可以變身，是遊戲機制不是外觀。
- 決定：
  - **保留比較短的 id**，也就是 `fFIFTY_PERCENT` 與 `fTEN_PERCENT`，
    沿用 NOEVOLVE 合併既有的「留短的」規則。
  - **`SAME_LOOK` 一筆一筆指名，不寫成去掉前綴的規則**。
    `COMPLETE` 自己是獨立的形態（完全體），規則化會把它也吃掉。
  - **日文沿用上游的全形數字**。第一版我寫成半形，比對時發現那是
    多改的一筆，語言檔本來就給了「５０％フォルム」，改回去跟中文一致。
- 驗證：
  - **逐筆比對重跑前後的 1433 筆**：消失的剛好是那兩個帶前綴的 id，
    欄位有變的剛好是那三筆的 `zhForm`，其餘 1428 筆一個欄位都沒動。
  - 背卡、`extra.js` 都沒有引用被併掉的那兩個 id。
  - `node tools/check.mjs` 全部通過，新增四條：基格爾德是 3 筆、
    兩個舊 id 不該存在、三筆都要有中文型態名。
  - 起 server 實跑三語：
    中文 完全體形態／５０％形態／１０％形態，
    日文 パーフェクトフォルム／５０％フォルム／１０％フォルム，
    英文 Complete Forme／50% Forme／10% Forme。
  - **舊 id 種進清單測過**：`d718.fCOMPLETE_FIFTY_PERCENT` 讀得回來，
    兩筆都還在 localStorage，資料不會被吃掉。那一筆畫不出來，
    但計數已經在（九）那筆處理過，不會出現數字對不上。
- 待辦/已知問題：（無）

---

## 2026-09-12（十四）
- 類型：修正
- 影響檔案：js/dex.js, js/ui.js, js/i18n.js, CLAUDE.md, project-index.md
- 摘要：稀有度三類合成一類「傳說」。另外查證了圖鑑沒有收未實裝的東西。
  - 傳說 87、神話 34、究極異獸 11 併成一個選項，共 132 筆、86 個物種。
  - 合併做在畫面層，`cls` 欄位保留 game master 的原貌。
  - i18n 刪掉四個 key（`filterMythic` `filterUltra` `clsMythic` `clsUltraBeast`），
    三語各 106 → 102。
- 原因：使用者要求。稀有度分三種對「這隻換不換得動」沒有幫助，
  GO 裡這三類都是特殊交換。
- 決定：
  - **究極異獸一起併**（使用者選的）。三類合一，篩選只剩一顆。
  - **名稱用「傳說」**（使用者選的）。日文伝説、英文 Legendary。
  - **合併在畫面層不在資料層**。之後想分開不必重跑腳本，
    而且 `cls` 仍然是上游的原貌，不會變成自己編的資料。
- 查證（使用者問「傳說是不是漏了」）：
  - **沒有漏**。傳說 55 個物種、87 個條目，那 87 是含型態的條目數，
    所以看起來跟物種數對不上。
  - 反過來查：game master 標了稀有度而圖鑑沒收的有 25 種，
    24 種在上游四個圖片目錄（Addressable Assets、Pokemon - 256x256、
    PokedexV2、Unique Pokemon Icons）完全沒有圖，剩下一種是刻意隱藏的
    阿爾宙斯。缺的是雪暴馬、靈幽馬、蕾冠王、四災、故勒頓、密勒頓、
    厄詭椪、太樂巴戈斯這些，全都還沒進 GO。
  - **`pogoapi.net` 的 `released_pokemon` 不可靠**，實測它把霏歐納、瑪納霏、
    毒電嬰、顫弦蠑螈與來悲茶標成未實裝，那五種上游都有圖、也早就實裝了。
    已記進 CLAUDE.md，之後不要拿它當移除的依據。
  - 另外掃過一次「有 form 但 game master 找不到自己設定」的條目，
    修了索引之後只剩哲爾尼亞斯那個活力滿溢的樣子，而它是既有的
    `STATS_SAME_AS` 例外。圖鑑裡沒有其他遊戲資料缺失的東西。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - 起 server 實跑 1440：篩選面板的稀有度只剩「傳說 132」一顆，
    勾選後資訊列 132 項、格子 132 個，夢幻與虛吾伊德都在、妙蛙種子不在。
  - 詳情面板三語都看過：超夢、夢幻、虛吾伊德都顯示傳說／伝説／Legendary。
- 待辦/已知問題：
  - 基格爾德那兩組重複的卡，已於下一筆處理掉。

---

## 2026-09-12（十三）
- 類型：修正
- 影響檔案：index.html, css/style.css, js/main.js, js/ui.js,
  tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：設定也改成置中彈窗，跟右欄的詳情同一種形態。
  - 原本貼著齒輪往下展開、900 以下貼底，現在所有寬度都是置中，
    有遮罩、有右上角的紅色 X。
  - **整段搬出 `<header>`**。`.topbar` 是 sticky 帶 z-index 30，
    會開一個 stacking context，關在裡面的話 fixed 再高也蓋不過右欄
    那片 z-index 40 的遮罩。
  - 三層結構 `.modal` / `.modal-inner` / `.modal-scroll` 跟右欄對齊，
    視覺值也刻意抄同一套。
- 原因：使用者要求。
- 決定：
  - **X 走 `data-closepop`，不共用右欄的 `data-close`**。那一顆管的是右欄，
    按下去會連詳情一起收掉。
  - **遮罩要另外認**。它在 DOM 上就是 `#settings` 自己，
    「點在面板裡面就不關」這個判斷擋不掉，`main.js` 加認 target 的 id。
  - **篩選面板沒有跟著改**。它貼著漏斗浮出是刻意的，調整當下的檢視
    就該貼近觸發點；設定是全站偏好，置中比較合理。使用者也只說了設定。
- 驗證：
  - `node tools/check.mjs` 全部通過。DOM stub 補了 `settingsX`。
  - 起 server 量過 1440 與 390：1440 左右留白各 510、寬 420；
    390 左右各 14、寬 362，都是置中，遮罩都是 rgba(0,0,0,0.45)。
  - 四種關閉路徑逐一試過：點面板內不關（對）、點 X 關、點遮罩關、
    開篩選會把設定關掉（一次只開一個那條規則沒破）。
    `aria-expanded` 跟著回到 false。
  - 內容完整：顯示三個開關、資料三個動作，
    以及那段「資料只存在這台裝置」的警語都還在原位。
- 待辦/已知問題：（無）

---

## 2026-09-12（十二）
- 類型：修正
- 影響檔案：index.html, css/style.css, js/ui.js, tools/check.mjs,
  CLAUDE.md, project-index.md
- 摘要：手機也改成置中彈窗，關閉鈕換成右上角的紅色圓形 X。
  - **900 以下不再從底部升起**，跟桌機一樣置中，左右留白從 48 縮到 28。
  - **`.rail-inner` 改成不捲的殼**，捲動移到新的 `.rail-scroll`（就是 `#panel`）。
    X 疊在殼上，所以內容捲動時它一直留在原地。
  - **X 寫在骨架裡**，三個面板自動都有，繪製函式一個字都沒改。
  - **常駐形態換回文字關閉鈕**，兩顆都在 DOM，靠 CSS 切換。
- 原因：使用者要求。手機從下方彈出跟桌機置中不一致，
  而且底部的文字關閉鈕要捲到最後才按得到。
- 決定：
  - **兩邊都用 X**（使用者選的）。只要是彈窗就有，行為一致。
  - **背卡的常駐右欄不放 X**。那裡「關閉」是收掉詳情回到摘要，
    欄位還在，紅色的 X 會讓人以為整欄要消失。
  - **紅色用自己的 `--x-bg`，不共用 `--danger`**。後者是給文字用的，
    深色底上被調亮成 `#e8837c`，拿來當實心圓的底色白叉只有 2.4:1。
    `--x-bg` 深色用 `#c8352b`，白叉 5.2:1，在近黑上仍然看得出是紅的。
  - **X 用 absolute 不佔流排**。佔流排的話標題要為它讓出一塊，
    而內容區右邊本來就有 22 的內距，實測最長的名稱也不會被壓到。
- 驗證：
  - `node tools/check.mjs` 全部通過。DOM stub 補了 `closeX`。
  - 起 server 量過 1440 與 390：
    1440 左右留白各 460、上下各 72；390 左右各 14、上下各 59，都是置中。
    X 是 30px 的紅圓，`aria-label` 跟著語言走。
    兩個寬度的文字關閉鈕都是 `display: none`。
  - **捲到底後 X 仍在原位**，兩顆加入鈕完整露出，點 X 關得掉。
  - 背卡在 1440 的常駐欄：X 隱藏、摘要還在、點開卡片後文字關閉鈕是 block。
  - **拿最長的名稱測過**（小火馬 伽勒爾的樣子・2021 GO Fest，24 字）：
    1440 與 390 都沒有任何內容跟 X 重疊，型態名會自己換行。
- 待辦/已知問題：（無）

---

## 2026-09-12（十一）
- 類型：修正
- 影響檔案：css/style.css, js/main.js, CLAUDE.md, project-index.md
- 摘要：圖鑑的詳情改成**置中彈窗**，點一隻寶可夢不再讓格子牆重排。
  - 新增 `body.rail-pop`，`drawDetail()` 裡跟著 `state.view === "dex"` 切換。
  - 1200 以上那段查詢全部限定 `body:not(.rail-pop)`。帶著這個 class
    就自然沿用預設的 fixed 彈出形態，不必把 sticky 一條條覆寫回去。
  - 背卡與交換表一行都沒改。
- 原因：使用者說右側抽屜有左右不均的問題。實際量過是 1200 以上右欄
  常駐時會佔掉版面的第三欄，圖鑑點一隻，內容區被推窄，
  格子牆從 14 欄掉成 12 欄整片重排。
- 決定：
  - **只改圖鑑**（使用者指定）。背卡的右欄沒點卡片時擺清單摘要，
    它是常駐才有意義的東西，改成彈出就沒有觸發點，所以維持原樣。
  - **沿用現有的遮罩**（使用者選的）。點暗的那一片就關掉。
  - **用 `:not()` 限定而不是覆寫回去**。sticky 改回 fixed 要寫回六七個屬性，
    漏一個就是難查的版面錯位。
  - **彈出形態改成置中，不是貼右緣的抽屜**。第一版做成從右邊滑出，
    使用者說那不是他要的：靠右仍然左右不對稱，而且蓋住右上角的語言與齒輪。
    置中之後 1440 左右留白各 460、1000 各 240，完全對稱，頂部列整條露著。
    視覺沿用篩選與設定那個殼。900 以下不動，那裡是貼底升起。
  - **`transform` 要在常駐那一段清掉**。它在 `position: static` 底下照樣
    生效，不清的話背卡整欄內容會往左上位移一半。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - 起 server 用 DevTools Protocol 量過 1440 / 1000 / 390，每個寬度都跑
    圖鑑未點開、點開、點外面關掉、背卡、交換表五個狀態：
    1440 圖鑑點開後欄數仍是 14、內容右緣仍是 1440，改動前會變 12 欄；
    背卡仍然是 sticky 常駐 340，內容右緣 1100，摘要還在；
    交換表仍然沒有右欄。
  - 置中之後重量一次：1440 彈窗寬 520、左右留白各 460；1000 留白各 240；
    390 貼底、滿寬、高 726。三個寬度的頂部列都沒有被蓋住。
    遮罩仍是 rgba(0,0,0,0.45) 蓋滿視窗，點外面關得掉。
  - 深色看過：彈窗底 rgb(25,25,25)、框線 rgb(43,43,43)，在暗背景上浮得起來。
- 待辦/已知問題：
  - **背卡多的條目要捲一下才看得到兩顆加入鈕**。置中之後高度從整個視窗
    變成 84vh，妙蛙種子這種有六張背卡的內容高 884、可視 754，超出 130。
    捲到底兩顆鈕完整可用，被切一半本身也是「還有內容」的提示，
    所以沒有另外做固定底欄。要處理的話動作鈕不在最後，
    後面還有已加入的項目與關閉鈕，不能直接 sticky。
  - 交換表按加號開的選寶可夢面板在桌機仍然是常駐欄，一開就把兩欄推窄，
    跟圖鑑原本的毛病一樣。使用者這次只指定圖鑑，沒有動它。

---

## 2026-09-12（十）
- 類型：修正
- 影響檔案：tools/build-dex.mjs, js/godex.js, tools/check.mjs,
  CLAUDE.md, project-index.md
- 摘要：待辦 A-1。查下去發現原本記的根因是錯的，而且底下藏著更大的問題。
  條目 1464 → 1462。
  - **結草貴婦的砂土與垃圾蓑衣屬性是錯的**。兩筆都掛著本體的蟲加草，
    實際上砂土是蟲加地面、垃圾是蟲加鋼。根因是屬性索引只收去掉物種前綴
    的代碼（`413|TRASH`），查詢卻用含前綴的（`413|WORMADAM_TRASH`），
    查不到就靜默退回本體。旁邊的 CP 索引兩種寫法都收，所以垃圾蓑衣的
    CP 一直是對的，只有屬性錯。補 CP 那次修了一邊沒回頭修另一邊。
  - **洗翠黏美兒與黏美龍不是屬性抄錯，是資料根本不存在**。game master
    裡 14 個洗翠型態就少它們兩個。GO 還沒實裝，2026-11 的 GO Wild Area
    才首次登場。加 `HIDDEN_FORMS` 暫時隱藏。
  - 沒有 CP 的條目歸零，1462 筆全部都有。
- 原因：使用者指定處理 A-1。
- 決定：
  - **洗翠那兩筆隱藏而不是手動補屬性**（使用者選的）。GO 沒實裝就換不到，
    放在交換工具裡是誤導；而且沒有官方 GO 數值，補什麼都是猜的。
    只剩兩個月，實裝後刪掉重跑就帶真實資料回來，零維護。
  - **`HIDDEN_FORMS` 跟 `HIDDEN_DEX` 分開**。前者藏單一型態、本體要留著，
    後者藏整個編號。黏美兒與黏美龍的本體都還在。
  - **修索引而不是替結草貴婦寫死屬性**。寫死只治三筆，修索引連未來
    所有含前綴的型態一起治好。
- 驗證：
  - 三個來源交叉比對過洗翠黏美的實裝狀態：PokeMiners 的 game master
    沒有、pogoapi 沒有、官方與媒體說 2026-11 的 GO Wild Area 首次登場
    （仙台與墨西哥城 11/6–11/8，隔週全球）。
  - **逐筆比對重跑前後的 1435 筆**：消失的剛好是那兩筆洗翠，欄位有變的
    剛好是結草貴婦那兩筆，其餘 1431 筆一個欄位都沒動。沒有意外變動。
  - 背卡沒有引用被隱藏的那兩筆，收集格數不受影響。
  - `node tools/check.mjs` 全部通過。新增五條：結草貴婦三種蓑衣各驗屬性、
    洗翠那兩筆不該在圖鑑裡。原本寫死「沒有 CP 的剛好是那 2 筆」那條
    改成「每一筆都有 CP」。
  - 起 server 實跑 1440：圖鑑 1462 項、搜尋「黏美」只剩本體兩筆、
    結草貴婦三筆屬性各不相同，垃圾蓑衣的詳情面板是蟲加鋼、
    CP 910 / 1138 / 1802。
- 待辦/已知問題：
  - 2026-11 洗翠黏美兒與黏美龍實裝後要解除隱藏，已記進 CLAUDE.md 的待辦。

---

## 2026-09-12（九）
- 類型：修正
- 影響檔案：js/dex.js, js/main.js, js/ui.js, js/share.js, js/i18n.js,
  tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：待辦 B 組三項一次做完。
  - **B-1 英文副標改成功能描述**。`Pokémon Trade Post` 配
    `Pokémon GO trade list maker` 一句裡有兩次 Pokémon，副標改成
    `Build and share your trade list`。中文與日文一個字都沒動，
    它們的站名與副標書寫不同，本來就不刺眼。
  - **B-2 顯示的筆數一律只算畫得出來的**。抽成 `dex.js` 的 `knownItems()`，
    五個地方共用：資訊列、右欄摘要、分頁標籤、兩顆分享鈕的灰掉判斷，
    以及分享圖原本自己寫的那份。
  - **B-3 刪掉沒人用的 `filterAll`**。篩選面板改版後就沒有引用，
    三語各 107 → 106。
- 原因：使用者指定處理 B 組。
- 決定：
  - **抽成單一出處而不是各自補**。這個 bug 的根因就是同一條規則散在三處
    各寫一份，所以第四、第五處漏了沒人發現。抽出來之後不會再漏。
  - **只影響顯示，不影響儲存**。畫不出來的紀錄照樣留在 localStorage，
    條目回來就自己接上。`MAX_ITEMS` 那類管儲存的判斷仍然數原始長度。
  - **英文副標不保留遊戲名**。分享圖底部只印副標不印站名，但圖面本身
    滿是寶可夢，識別不靠那行字。使用者選的。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增四條回歸測試。
    **逐條確認過改動前會失敗**：把 `js/` 暫時還原只留新測試，
    分頁數字報 4 應為 1、右欄摘要數了畫不出來的、分享鈕沒有變灰，三條紅。
  - 起 server 用 DevTools Protocol 實跑 1440：在網站腳本執行前種一份
    want 有 3 筆的清單，其中 `d9999` 與 `d493` 圖鑑查不到。
    資訊列、分頁、欄標題、右欄摘要四個數字全部是 1，畫面上一個格子，
    localStorage 裡仍然是 3 筆。改動前資訊列會寫 3、分頁會寫 4。
  - 分享圖實際產過：1320×656 的 PNG，66 KB。
    只剩畫不出來的條目時 `buildShareImage` 回傳 null，而現在鈕已經是灰的，
    走不到那條路，不會再看到失敗訊息。
  - 三語切換看過，英文頂部列沒有溢出。
- 待辦/已知問題：（無）
- 補記：**分頁標題固定中文，使用者決定不處理**。`document.title` 全專案
  沒有人設定，`index.html` 那行是寫死的「寶可夢交換所 · Pokémon GO
  交換清單製作」，切到日文或英文都不會變。三語副標語義不平行同樣可以接受。
  這不是待辦，不要再提出來排工作。

---

## 2026-09-12（八）
- 類型：修正
- 影響檔案：tools/bg-report.md
- 摘要：忽略快取重跑背卡腳本，確認 30 週年四張仍然沒有寶可夢清單。
  `js/bgdata.js` 一個位元組都沒變，逐張比對前後版本，沒有任何一張卡的
  清單筆數改變。沒有清單的仍是那 11 張。
  唯一變動是報告開頭的手工覆蓋張數 17 → 21，那是上次改 `js/bgevents.js`
  之後沒重跑報告留下的陳舊數字，這次順帶對上。
- 原因：30 週年最早的吉隆坡場是今天（9/12），想看上游有沒有跟上。
- 結論：**還不能接**。Dittobase 與 Serebii 那四頁本身是空的，
  `SEREBII_MANUAL` 的人工對照有生效，對到的頁沒有內容。
  馬來西亞場是今明兩天，上游通常等辦完才整理。而且那批的 PokéXciting
  皮卡丘裝扮至今沒進 game master，圖鑑收不到，清單出現了也還得等裝扮。
  另外三場在十月、十一月與明年一月，時候到了再跑一次就好。
- 驗證：腳本離開碼 0，四個來源都重抓（圖檔 240、Dittobase 234 頁、
  Serebii 249 筆、Bulbapedia 78 張）。`node tools/check.mjs` 全部通過。
- 待辦/已知問題：（無）

---

## 2026-09-12（七）
- 類型：修正
- 影響檔案：js/i18n.js, index.html, CLAUDE.md
- 摘要：站名從「交換表」改成「寶可夢交換所」。
  日文 ポケモン交換所、英文 Pokémon Trade Post。副標三語都沒動。
- 原因：站名跟三個檢視裡的「交換表」是同一個詞，左上角與正中間撞字。
  使用者本來提的是「交換器」，那在台灣第一個意思是網路交換器，沒有採用。
  先落在「交換所」，使用者再指定前面加上寶可夢，講清楚是什麼的交換所。
- 決定：**英文不直譯清單而用 Trade Post**，像一個地點，跟 PokéStop 同一個語感。
- 驗證：`node tools/check.mjs` 全部通過。起 server 實跑三語切換，
  站名依序是 寶可夢交換所／ポケモン交換所／Pokémon Trade Post，分頁標題也換了。
  頂部列量過 1440 / 905 / 390：標題變長後檢視與語言都沒有重疊，
  也沒有橫向捲動。手機本來就不顯示副標，不受影響。
- 上線：2026-09-12 推上 main，線上版本 ec82922。
  線上抓 `index.html` 與 `js/i18n.js` 對過，分頁標題、頂部列站名與英文的
  Pokémon Trade Post 都是新版。
- 待辦/已知問題：
  - 英文站名與副標各有一次 Pokémon，讀起來重複。副標是分享圖唯一印出去的
    文字，要不要改另外談。

---

## 2026-09-12（六）
- 類型：修正
- 影響檔案：js/main.js, js/ui.js, CLAUDE.md, project-index.md
- 摘要：圖鑑檢視不再在右欄擺交換表摘要。圖鑑就是圖鑑。
  - `railHasContent()` 的條件從「不是交換表」改成「是背卡」，
    `drawDetail()` 的最後一支也跟著改。只動這兩個判斷，摘要本身一行沒改。
  - 圖鑑沒點開東西時 `body.rail-off`，整欄收起，格子牆從 12 欄變 14 欄。
- 原因：使用者說圖鑑旁邊不需要掛半張交換表。
- 決定：**背卡保留摘要**，使用者選的。在背卡挑卡時順便看得到排了幾隻，
  而且背卡沒有自己的清單可看，跟圖鑑的情況不同。
- 驗證：
  - `node tools/check.mjs` 全部通過。
  - 起 server 用 DevTools Protocol 實跑 1440：圖鑑進站右欄寬 0、內容右緣 1440；
    點妙蛙花詳情右欄回到 340；關掉又收起；交換表仍然沒有右欄；
    背卡摘要還在；來回切檢視都對。
  - 1000 與 390 量過：兩個寬度的右欄本來就是點了才滑出來的彈出，
    `rail-off` 只寫在 1200 以上那段查詢裡，窄螢幕完全沒受影響。
- 上線：2026-09-12 推上 main，線上版本 3b2acbb。
  線上抓 `js/main.js` 確認過兩處判斷都是 `state.view === "bg"`，沒有舊條件殘留。
- 待辦/已知問題：（無）

---

## 2026-09-12（五）
- 類型：修正
- 影響檔案：tools/build-dex.mjs, js/godex.js, tools/check.mjs,
  CLAUDE.md, project-index.md
- 摘要：原始回歸與阿爾宙斯不再出現在網站上。條目 1484 → 1464。
  - **原始回歸併進既有的暫時狀態排除**。它跟超級進化、極巨化同一類，
    是戰鬥中的形態不是可交換的個體，當初漏掉了。順帶解掉「沒有 CP」那兩筆。
  - **阿爾宙斯用新的 `HIDDEN_DEX` 暫時隱藏**。GO 還沒實裝，上游只有圖，
    18 個屬性型態的基礎數值一模一樣，型態名多半沒有官方譯名，
    畫面上是一整排「Bug-Type form」。實裝之後刪掉編號重跑就回來，id 不變。
  - 兩者分開寫是因為性質不同：一個是永遠不該收，一個是現在還不該收。
- 原因：使用者指定。原始回歸是前一筆做 CP 時發現的漏網之魚。
- 決定：**在產生時排除，不在畫面上過濾**。跟超級進化同一條路徑，
  資料層乾淨，篩選的計數也不必特別處理。
- 驗證：
  - `node tools/check.mjs` 全部通過。沒有 CP 的名單從 4 筆改成 2 筆
    （只剩洗翠黏美兒與黏美龍）。背卡引用的 481 個條目沒有一個受影響。
  - 起 server 實際看過：總數 1464、搜尋「阿爾宙斯」與「原始回歸」都沒有結果、
    蓋歐卡只剩本體一筆。
  - **試過舊紀錄裡已經存了被隱藏的條目**：在網站腳本執行前種一份含 `d493`
    與 `d382.fPRIMAL` 的清單，那兩格畫不出來，但 localStorage 裡三筆都還在，
    產生分享圖也不會炸。資料不會被吃掉。
- 上線：2026-09-12 推上 main，線上版本 cc3c81b（跟前一筆一起上）。
- 待辦/已知問題：
  - **資訊列的筆數會跟畫出來的格子數對不上**。上面那個情況資訊列寫「想要 3」，
    畫面上只有一格。任何「id 存在於紀錄但不存在於圖鑑」的情況都會這樣，
    不是這次才有的，但這次讓它變得可能發生。要不要讓計數只算畫得出來的，
    還沒決定。

---

## 2026-09-12（四）
- 類型：新增
- 影響檔案：tools/build-dex.mjs, js/godex.js, js/extra.js, js/ui.js,
  css/style.css, js/i18n.js, tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：圖鑑詳情面板加 IV100 的 CP，三個等級。舊站有這個功能，這次用算的。
  - **`godex.js` 每筆多 `cp20` `cp25` `cp50`**，個體值全滿在該等級的 CP。
    20 是團體戰捕捉、25 是天氣加成、50 是練滿。
  - **在 build 時算，不在瀏覽器算**。公式是
    `(攻+15) × √(防+15) × √(耐+15) × 倍率² ÷ 10` 無條件捨去，
    倍率取自 game master 的 `PLAYER_LEVEL_SETTINGS`，不自己抄一份。
  - **CP 另外建一份基礎數值的索引**，不共用查屬性那個。那個查不到會退回本體，
    CP 不能這樣退：原始回歸與洗翠黏美龍在 game master 裡沒有自己的數值，
    退回本體會得到一個看起來很像真的、其實是別隻的 CP。
    1484 筆裡 1480 筆有 CP，那 4 筆刻意留空，面板上整列不畫。
  - **型態代碼兩種寫法都收**。上游有時把物種寫進 form（`UNOWN_A`），
    有時不寫（`ORIGIN`），只認一種會讓未知圖騰整批對不到。
  - **裝扮用本體的數值**，裝扮不改數值。`extra.js` 補的 27 筆裝扮直接抄本體，
    捷拉奧拉與纏紅鶴沒有本體可抄，數字寫死在那個檔裡。
  - **詳情面板在條件上面多一列三格**，凹下去那一階當底，等寬數字。
    放最上面是因為團體戰要查的就是這個，不該捲。
  - i18n 加 `cpSection` 與 `cpLevel`（`LV %s` 模板，三個等級共用），
    三語各 105 → 107。
- 原因：使用者要把舊站的 IV100 CP 功能接過來，範圍是全部寶可夢，不只神獸。
- 決定：
  - **範圍是全部 1484 筆**，不只傳說。團體戰不是只有神獸，而且算起來不用錢。
  - **存算好的三個數字**，不存基礎數值。之後要加別的等級就重跑腳本。
  - **那 4 筆整列不畫**，不放三個破折號。破折號只是佔位子，
    還會讓人以為是載入失敗。
  - **重跑腳本用快取不加 `--force`**。這次只多三個欄位，
    逐筆比對過 1455 筆，除了 CP 以外沒有任何欄位變動。
- 驗證：
  - 舊站那 79 筆手寫的 CP 拿來對答案，74 筆完全吻合。
    不吻合的五筆（露奈雅拉、毒貝比、四顎針龍、熊徒弟兩筆）用三個來源交叉比對：
    本機快取、PokeMiners 線上最新、pogoapi.net，三邊的基礎數值一致，
    所以是舊站的數字錯了，不是公式錯。已記進 CLAUDE.md。
  - `node tools/check.mjs` 全部通過。新增四條：超夢 2387 / 2984 / 4724 定值、
    三個等級遞增、要有就三個都有、沒有 CP 的剛好是預期那 4 筆。
  - 起 server 用 DevTools Protocol 實際看過：超夢三個數字、盔甲超夢與本體不同
    （1821 對 2387）、劍之王與蒼響不同、騎拉帝納兩個型態不同、
    裝扮皮卡丘等於本體、原始回歸整列不畫、三語的區塊標題。console 無錯誤。
  - 桌機 1440 與手機 390 都看過，深色也看過。手機三格等寬 114px，沒有橫向捲動。
- 上線：2026-09-12 推上 main，線上版本 cc3c81b。
  線上抽驗過：1435 筆、1433 筆有 CP、超夢 2387 / 2984 / 4724。
- 待辦/已知問題：
  - **洗翠黏美兒與黏美龍的屬性也是錯的**。圖鑑寫龍，實際是鋼與龍。
    這兩筆是上游只有圖、沒有遊戲資料時補出來的，屬性跟著本體抄。
    沒有 CP 是同一個原因。要不要處理另外談。
  - 原始回歸被收成可交換條目。超級進化與極巨化都排除了，它是漏網的同類。

---

## 2026-09-12（三）
- 類型：修正
- 影響檔案：css/style.css
- 摘要：手機上語言與齒輪縮回標題旁邊，改成永遠貼右緣。
  `.top-right` 加 `margin-left: auto`，自己推自己，不靠別人撐開。
- 原因：同一天前一筆把標題從 `flex: 1` 改成 `flex: 0 1 auto`，
  好讓三個檢視貼在標題右邊。但手機上那三個是 fixed 的，離開流排之後
  頂部列沒有任何東西會長大，靠右那一段就跟著縮回來了。
  桌機看不出問題，因為檢視還在流排裡撐著。
- 驗證：1440 / 1000 / 905 / 768 / 390 / 360 六個寬度量過齒輪右緣，
  都貼著內距邊界，誤差 2px 內。
- 上線：2026-09-12 推上 main。

---

## 2026-09-12（二）
- 類型：重構
- 影響檔案：index.html, css/style.css, js/ui.js, js/main.js, js/i18n.js,
  tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：左側欄整個拆掉。裡面三樣東西各自搬家，功能一項沒少。
  - **三個檢視搬到頂部列**，緊接在標題右邊，圖示加文字。
    900 以下同一段 DOM 變成貼底的 bar，圖示在上、小字在下，三等分。
    每個檢視有自己的形狀：圖鑑是格子牆、交換表是兩個對向箭頭、背卡是一張圖。
    光有文字的底部 bar 認不出來，所以圖示是必要的不是裝飾。
  - **顯示與資料兩區搬進右上角的齒輪**。深淺色也收進去，
    跟大圖示、顯示名稱排在同一區：它也是「畫面要長什麼樣」，
    不是一個獨立功能。頂部列因此少了那顆半黑半白的圓。
  - **警語只放設定面板裡**，跟匯出鈕放在一起。使用者指定的位置。
    我提過埋進設定等於拿掉，他確認要這樣，已把 CLAUDE.md 那條
    「介面上的警語不要拿掉」改成記錄它現在住哪，免得下一次又被搬回來。
  - **`state.fopen` 改成 `state.pop`**，值是 `null`／`"filter"`／`"settings"`。
    兩個面板共用一套開合與「點外面關掉」的邏輯，天生擋掉兩片同時打開。
    篩選那一顆昨天才做，這次順手收成同一套。
  - **`.pop` 是兩個面板共用的殼**：桌機貼著自己那顆鈕浮著，
    900 以下 fixed 貼底變抽屜。位置與寬度各自寫在 `.fpanel` 與 `.settings`。
  - **齒輪的路徑是自己算的**，六齒、齒頂 9.5、齒根 6.5，中間一個 r3 的圓。
    不抄圖示庫，省掉授權與出處的問題。
  - `setSidebar()`、`.sidebar`、`.scrim`、漢堡鈕、`--side-w` 全部刪掉。
  - i18n **沒有新增 key**。字典裡本來就有 `settings` 與 `theme` 兩個沒人用的，
    文字剛好就是要的，直接接上，順手把重複宣告與孤兒位置收乾淨。三語各 105 個。
- 原因：使用者要一個右上角的設定齒輪收起深淺色、顯示與資料，
  三個檢視改成手機底部的圖示 bar。
- 決定：
  - **底部 bar 只有手機**，桌機排頂部列。使用者選的，理由是桌機的滑鼠
    不該跑到螢幕最下面點導覽。兩個寬度的導覽位置不同，樣式因此兩套。
  - **語言三顆留在頂部列**，沒有收進齒輪。切語言一點就到。
  - **選中的檢視在底部 bar 不填底色**，改用金色加上緣一條 2px。
    三格填一整格會變成一塊大色塊，比內容還搶眼。桌機仍是填 `--sunk`。
  - **標題不再吃掉剩餘空間**（`flex: 0 1 auto`），檢視才貼得住它右邊。
    原本 `flex: 1` 會把三個檢視推到最右邊擠著語言鈕，離標題很遠。
- 驗證：
  - `node tools/check.mjs` 全部通過，三語各 105 個 key。
    DOM stub 拿掉 `sidebar` 與 `scrim`，補上 `gearBtn` 與 `settings`。
  - 起 server 用 DevTools Protocol 實跑：齒輪開合、在面板裡切深色與大圖示
    （面板不關、開關狀態跟著變）、資料三顆鈕還接得上、篩選與設定互斥、
    點外面、Esc、三個檢視來回切、手機底部 bar 切檢視。console 沒有錯誤或警告。
  - 量過設定面板沒有超出畫面右緣、蓋在內容上面、警語在面板裡而且看得到。
  - 頂部列量過 1440 / 1000 / 905 三個寬度，檢視與語言都沒有重疊、沒有橫向捲動。
  - 手機 390 與 360：bar 滿寬貼底、三格等寬各 130px、捲到底最後一排格子
    不會被 bar 蓋住（實際捲到底量了 bottom 與 bar 的 top）。
  - 深淺色都看過桌機與手機。
- 上線：2026-09-12 推上 main，線上版本 adc72e9。
  html 沒有 sidebar 了、齒輪在、四個 js 與 css 都確認過是新版。
- 待辦/已知問題：
  - 設定面板在桌機會蓋到右欄的清單摘要。它是浮出來的面板，這是預期行為。
  - 手機抽屜沒有遮罩，跟右欄詳情與篩選抽屜一致。

---

## 2026-09-12（一）
- 類型：重構
- 影響檔案：index.html, css/style.css, js/ui.js, js/main.js, js/i18n.js,
  tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：篩選的三排 chip 收進搜尋框旁邊的漏斗。條件與計數邏輯一個沒動。
  - **常駐三排太佔畫面**。36 個選項加間距吃掉格子牆上方約 60px，
    而且手機上三排都要橫捲，第一屏幾乎看不到寶可夢。
  - **已選條件留在漏斗外面**。這是昨天把面板改成常駐 chip 的唯一理由：
    收起來之後畫面上只剩一個數字，看不出篩掉了什麼。現在漏斗帶條件數，
    右邊排出已選的那幾顆，各自帶 ✕ 只移除那一個。沒篩選時整排不長東西，
    第一行只有搜尋框跟漏斗。
  - **「清除全部」不重複做一顆**，用頂部資訊列那顆現成的。
  - **面板五組各一個小標題**（種類、稀有度、其他、屬性、世代），
    組內攤開不橫捲。`grpKind` 那五個標題 key 本來就在字典裡，沒新增。
  - **面板貼在搜尋列下方浮著**，桌機 absolute、900 以下 fixed 貼底變抽屜，
    同一段 DOM 兩種形態，跟右欄同一個手法。
  - **選了條件面板不關**，要能連選幾個。關閉走四種：再點漏斗、
    點面板外面、Esc、面板底部的完成鈕（那顆主要是給手機抽屜用的）。
  - 新增 `filterBtn` 與 `filterDone` 兩個 key，三語各 103 → 105。
    昨天移除的 `filterBtn` 回來了，`filterHits` 沒有回來，
    那個數字現在由漏斗上的條件數與資訊列的筆數各自表達。
- 原因：使用者說三排 chip 太佔畫面，要一顆漏斗把條件收起來。
- 決定：
  - **`.fpicked` 空的時候 `display: none`**，不然沒篩選時搜尋框右邊會
    多留一段 gap 的空白。
  - **`.searchbar` 改成可以換行**，已選那排給 `flex: 1 1 260px`。
    窄螢幕排不下就整排掉到第二行，不跟搜尋框互相擠成兩個都不好用的寬度。
    實測 360 與 390 都是這樣掉行，搜尋框保住 255px。
  - **陰影做成 `--shadow-ink` token**。全站只有這個面板要陰影，
    但深色底上同一個濃度看不見，做成 token 讓 `body.dark` 調，
    不另外寫一條深色規則。淺色 0.11、深色 0.55。
  - 桌機面板寬 `min(560px, 100%)`、最高 70vh；手機抽屜最高 80vh。
- 驗證：
  - `node tools/check.mjs` 全部通過，三語各 105 個 key。
    `renderChips` 的兩條測試換成 `renderFilterBar` 與 `renderFilterPanel`
    各兩條，並多驗了「沒選條件就不該有已選那排」「已選幾顆對得上」
    「面板五組都在」。原本那條「計數要扣掉自己那一組」保留。
  - 起 server 用 DevTools Protocol 實際操作：開關面板、連選三個條件、
    完成鈕、點面板外面、Esc、點 ✕ 移除單項（15 → 273 筆）、
    資訊列清除全部、三個檢視來回切、深色。console 沒有錯誤或警告。
  - 量過面板浮在格子牆上面沒被擋（`elementFromPoint` 打到面板本身），
    以及面板打開時搜尋框沒有被推窄。
  - 寬度看過 1440、1100、360、390。手機這次用 `Emulation.setDeviceMetricsOverride`
    量，`clientWidth` 回報 360 就是真的 360，不必再繞 iframe。
    四個寬度都沒有橫向捲動，手機仍是五欄。
  - 選十個條件時已選那排自己橫捲，整頁不捲。
- 上線：2026-09-12 推上 main，線上版本 f530e98。
  推上去約 40 秒後 Pages 才換檔，html 與 css／js 四個檔都確認過是新版。
- 待辦/已知問題：
  - 手機抽屜沒有遮罩。現有的 `#scrim` 是側欄專用的，右欄詳情在手機上
    也沒有遮罩，這裡跟著一致。
  - 面板開著時按選單，抽屜會蓋掉面板（面板會先被「點外面」那一段關掉），
    跟昨天記的那個老問題方向相反，這一邊是對的。

---

## 2026-09-11（六）
- 類型：重構
- 影響檔案：index.html, css/style.css, js/ui.js, js/main.js, js/share.js,
  js/i18n.js, tools/check.mjs
- 摘要：介面重做。功能一項沒少，資料形狀與 id 完全沒動。
  三個真實產品當錨：Bandcamp Discover 出主幹、Letterboxd 與 Spotify 出深色、
  Scryfall 出頂部資訊列。
  - **根因是全站只有一種容器。** 白底、10px 圓角、1px 邊線，
    圖鑑 1484 個格子、側欄每一顆鈕、23 個收納夾長得一模一樣，
    看不出誰是導覽、誰是資料、誰是破壞性動作。
  - **格子不畫框了**（圖鑑、交換表、背卡三處）。圖直接站在畫布上，
    hover 才浮出一塊底色。中性階從兩層（bg／card）拆成四層，
    多了 sunk 與 hair，層級靠底色差不靠邊框。
  - **篩選從彈出面板改成常駐 chip 三排**。原本關起來之後畫面上只剩一個數字，
    看不出篩掉了什麼。第一排種類與稀有度共九個，屬性 18 個與世代 9 個
    各自一排橫向捲。36 個全部攤平會吃掉半個畫面，全收進下拉又看不到狀態。
  - **詳情面板在桌機變成常駐右欄**，1200 以上不蓋畫面，以下維持彈出。
    同一段 DOM 兩種形態，繪製函式不必知道自己在哪。
    右欄沒東西可顯示時放目前清單摘要，在圖鑑挑寶可夢時看得到自己排了幾隻。
    交換表例外，那個檢視本身就是清單，整欄收起來。
  - **背卡收合時不再只剩一行字**，改成橫向捲的一排真卡面。
    原本 23 條純文字長條佔滿一屏卻一張圖都沒有，而這個檢視的內容就是圖。
  - **屬性色進圖鑑**。types.js 那 18 個代表色原本一個都沒用到，
    現在是格子圖下面那條 3px 色帶，雙屬性兩段。關掉名稱之後它是
    唯一還認得出屬性的東西。
  - **頂部加資訊列**，抄 Scryfall 的 context bar：現在在看哪一批、幾筆、
    篩過就給一顆清除。原本這行資訊是內容區裡一行 15px 的小標題，沒有地位。
  - **深色重做不是反色**。原本 #16150f 偏暖褐，跟寶可夢圖的彩度打架，
    改成中性近黑 #101010。分享圖的兩套顏色跟著對齊。
  - **圖示改 inline SVG**。☰ 與 ◐ 是文字符號，跨平台字重與基線會飄。
- 原因：使用者說「太有 AI 味」。查下去不是配色的問題，
  是沒有層級系統，只有一個元件重複一千次。
- 順帶修掉的無障礙問題：
  - **金色當文字不合格**。`#b8860b` 對白底只有 3.25:1，低於 AA 的 4.5:1，
    而側欄選中項與「顯示名稱」都是金字白底或白字金底。
    拆成 `--gold-ink #8a6508`（5.3:1）給文字，`--gold` 只做填色。
  - **`--dim` 也不合格**，`#7d7870` 對畫布 4.0:1，改成 `#6f6a62`（4.8:1）。
  - **破壞性動作借用了異色的粉紅**。同一個色既表示「異色」又表示
    「會刪掉東西」，新增 `--danger`，「清空這份清單」平常就是紅字，
    不是等 hover 才變色。
- 決定：
  - **加第二個斷點 1200px**。原本只有 900，但右欄在 900 到 1200 之間
    沒地方站。900–1200 右欄收起，點了才滑出來。
  - **全站只留一顆實心按鈕**（產生分享圖）。看到實心就知道那是主要動作，
    其餘一律描邊或純文字。
- 驗證：
  - `node tools/check.mjs` 全部通過。三個被刪掉的繪製函式測試換成
    renderChips、renderInfoBar、renderRailSummary，另外加一條
    「chip 計數要扣掉自己那一組」。
  - 起 server 用 DevTools Protocol 實際操作過：從圖鑑加 15 筆到兩欄、
    開詳情、關詳情回到摘要、切三個檢視、產生分享圖（存成 PNG 看過顏色）。
  - 寬度看過 1440（右欄常駐）、1100（右欄收起）、500（手機版面）。
    真手機寬度用 iframe 量，360 與 390 都是五欄、沒有橫向捲動——
    headless 的視埠下限是 500px，直接開 390 看到的是裁切不是版面。
  - 深淺色都看過三個檢視。
  - 背卡改成一開始就顯示圖之後量過載圖：`loading="lazy"` 有生效，
    240 張裡實際只請求 39 張，捲動後 49 張，畫面上的每一張都載得起來。
- 上線：2026-09-11 推上 main，線上版本 3b750a4。
  這次只有一個 commit，不像上一批是五個一起上。
- 待辦/已知問題：
  - **`filterBtn` 與 `filterHits` 兩個 i18n key 已移除**（篩選面板沒了），
    三語各 105 個 key 變成 103 個。
  - `filterAll` 這個 key 在這次改動之前就沒有人用，這次沒動它。
  - 手機上開著詳情面板時按選單，抽屜會被面板蓋住。這是改動前就有的行為，
    這次沒改。

---

## 2026-09-11（五）
- 類型：修正 / 新增
- 影響檔案：tools/build-bg.mjs, js/bgdata.js, js/bgevents.js, js/extra.js,
  tools/bg-report.md, CLAUDE.md, project-index.md
- 摘要：背卡清單的比對接不上與接錯，一起修掉。收集格 1452 → 1579。
  - **根因是兩邊對同一張卡的叫法不同**，不是資料不存在。
    上游寫球場（`lc_2026_NPB_kyocera`），Dittobase 寫球隊
    （`lc-nbp-orix-buffaloes`）；上游叫都內（`lc_TokMun_koto`），
    它叫蓋章拉力賽。還有上游把 wimpole 拼成 whimpole、
    Dittobase 把 npb 拼成 nbp。快取裡有 41 筆從來沒被任何一張卡用到。
  - **加 `DB_MANUAL` 人工對照 32 筆**，`SEREBII_MANUAL` 4 筆。
    每一筆都把兩邊的卡面縮成 32×32 逐像素比對確認，雙向都指向對方，
    次近的差一個數量級。靠名字推會錯：巴黎兩張的編號是交叉的，
    上游 01 對 Dittobase 的 `-2`；`lc_CR_2026_001` 其實是紐約時代廣場。
  - **修掉三張掛著別張卡清單的**。`matchByKey` 只防「一張卡對到多筆」，
    不防「多張卡對到同一筆」，而 `norm` 連數字與 `lc_`／`sb_` 前綴都去掉。
    `sb_GoFest2025_Eternatus` 拿了 `sb_GoFest2025` 的 6 隻，
    實際該是 Dark Skies 的 74 隻；`sb_GOWA_fukuoka` 拿了 lc 那張的；
    `lc_MLB_tampaBayRays2` 拿了第一張的。
  - **`resolveMatches` 取代直接呼叫 `matchByKey`**，兩張卡搶同一筆就
    兩張一起退回沒有資料，要哪一張得人工指名。Serebii 也套同一套，
    因此又揪出 GO Fest 2024 蟲洞三張共用一筆。
  - **補圖鑑的纏紅鶴**（`js/extra.js`）。上游 `pm973.icon.png` 是 404，
    跟捷拉奧拉一樣。嘉年華那兩張背卡的清單只有牠，圖鑑沒收就等於空的。
  - **30 週年那四張補名稱與日期**（`js/bgevents.js`）。Serebii 拼錯了兩處
    （Phippines、Octboer），照抄會直接顯示在畫面上。
- 原因：25 張背卡沒有寶可夢清單。原以為要上網逐張找，查下去發現
  資料本來就在快取裡，是比對規則接不上。
- 決定：人工對照放 `tools/build-bg.mjs` 不放 `js/bgevents.js`。
  這是腳本抓得到、只是配不上的資料，寫進手工層等於把可自動更新的
  東西凍結成手工，下次出新卡又要再補一次。
- 驗證：
  - 兩邊 474 張圖全部下載做雙向逐像素比對，不是只看名字。
    同一張差 0～0.4，不同張差 25 以上，中間沒有灰區。
  - `vfx: true` 的 14 張圖片對不上，與 CLAUDE.md 記載的特效層一致；
    唯一 `vfx: false` 卻對不上的正是誤接那張，反過來佐證方法有效。
  - `lc_2026_ppk_001` 從 Dittobase 接到三隻神鳥，跟既有手工資料
    逐筆相同，等於多一組對照。
  - `node tools/check.mjs` 全部通過，收集格 1579。
  - 起 server 用 DevTools Protocol 實際看過：日本職棒 13 張現在都有
    收集格，東京都內三張 5/6/6，科隆嘉年華點開是纏紅鶴。
- 待辦/已知問題：
  - **還有 11 張沒有清單**。Dittobase、Serebii、Bulbapedia、官方公告
    都查過沒有。其中 30 週年四張是活動還沒辦（最早的吉隆坡場是 9/12），
    辦完重跑腳本就會自己接上。其餘七張是 `lc_2026_BR_001`、
    `lc_busanFireworksFestival_2025`、`lc_MLB_tampaBayRays2`、
    `sb_MidAutumn`、`sb_PatternWild`、`sb_WorldSpecial_Blue`、
    `sb_s24_sep_2026`。
  - **Serebii 對不到的從 47 張變成 50 張**，是防護生效退回了借來的，
    不是變差。那幾張顯示的是檔名推的暫名。
  - 30 週年四張的 PokéXciting 皮卡丘裝扮還沒進 game master，
    圖鑑收不到，所以清單留空而不是填皮卡丘。

---

## 2026-09-11（四）
- 類型：新增 / 重構
- 影響檔案：js/store.js, js/main.js, js/ui.js, js/share.js, js/i18n.js,
  css/style.css, tools/check.mjs
- 摘要：三份交換清單、交換表的加號，以及「訓練家代碼」改叫友情碼。
  - **儲存結構升到 v2**。key 沒換，`{ v:2, active, lists:[三份] }`，
    每一份是 `{ name, want, have }`。順便把 `name` 從 `{want,have}`
    收成一個字串，那個 have 從來沒被用過。
    v1 是單獨一份，讀到那種形狀就整包成第一份，另外兩份留空。
    **只有真的改動才會寫回**，光是打開網站不會升級。
  - **三份用分頁切換**，標籤是那一份的名稱，沒取名顯示「清單 1」，
    右邊帶項目數。一次只顯示一份，分享圖也只印目前這一份。
  - **匯出三份包成一個檔**。匯入時整包就整包換掉；舊版匯出的單份
    只蓋掉目前在看的那一份，不能讓一個舊檔案把另外兩份清掉。
  - **「清空全部」改成「清空這份清單」**，只清目前這份，確認視窗寫明是哪一份。
  - **兩欄的格子牆最後各有一格加號**，空的時候也在。點了彈出選寶可夢面板
    （搜尋加格子牆），點一隻就換成那隻的詳情，條件與背卡照舊，
    按下加入就回到交換表。面板上有返回鈕回去選別隻。
    那個面板不套用圖鑑檢視的篩選，否則在圖鑑篩過再從交換表按加號會看到空白。
  - **「訓練家代碼」三語全部改成友情碼**，key 也一併改名，
    並給清單名稱與友情碼兩個欄位補上看得見的標籤。
- 原因：使用者要能同時維護多份清單，並且在交換表當場加寶可夢，
  不必先切到圖鑑。另外「訓練家代碼」的說法對不上玩家平常的叫法。
- 決定：三份是寫死的上限不是設定值，分頁一排三個剛好，多了手機擠不下。
- 驗證：
  - `node tools/check.mjs` 全部通過。儲存往返那一段整段改寫，
    新增 v1 相容、三份各自獨立、壞掉的 active、整包與單份匯入的分辨，
    以及分頁、加號、選寶可夢面板、返回鈕的繪製測試。
  - 起 server 用 headless Chrome 實際跑過：在頁面腳本執行前種一份 v1 資料，
    確認畫面照常顯示且沒有立刻改寫，做一次操作才升級成 v2，名稱與項目都接得上；
    匯出一個檔、整包匯入換掉三份、舊版單份只蓋目前那份；
    清空只清目前這份；加號到加入的整段流程；分享圖仍然正常。
    桌機 1440 與手機 390 兩個寬度加深色模式都看過。
- 待辦/已知問題：
  - **v2 是單向的**。上線之後若要回退到舊版程式，使用者的清單會看起來是空的
    （資料還在，只是舊版讀不懂）。上線前建議自己先匯出一份。
  - 選寶可夢面板一次最多畫 150 筆，超過要靠打字縮小範圍。
  - 修過一個版面坑：固定欄數寫 `repeat(3, 1fr)` 會被長名稱撐開捲出面板，
    要寫 `minmax(0, 1fr)`。已記進 CLAUDE.md 的地雷清單。

---

## 2026-09-11（三）
- 類型：新增
- 影響檔案：js/ui.js, js/main.js, js/i18n.js, css/style.css, tools/check.mjs
- 摘要：圖鑑詳情面板改成「先選條件再加入」，同一隻可以配不同背卡各收一筆。
  - **條件與背卡移到加入按鈕上方**。異色／XXL／XXS 三顆與整排背卡
    現在是加入前就能選的草稿，按下加入才寫進清單，關掉面板就丟。
    原本 XXL／XXS 要先加入才看得到，操作順序是反的。
  - **背卡列可以點**。原本只是展示，現在點一下選起來，再點同一張取消，
    另外多一列「不指定」。已經在清單裡的那幾筆仍然用下拉改，
    每一筆都攤成一整排圖會把面板撐得太長。
  - **同一隻可以配不同背卡各收一筆**。加入鈕不再是開關，
    「再按一次移除」在可以收多筆的情況下沒有意義，移除改走每一筆自己的鈕。
  - **四個條件完全一樣的那一筆不重複新增**，改成閃一下指出它。
    那種情況是手滑按兩次，不是真的想要兩格一模一樣的。
  - **交換表的格子帶自己的索引**，從交換表點進詳情會閃一下指出是哪一筆，
    不然同一隻有三筆會不知道在看哪一個。
- 原因：使用者要能從圖鑑直接指定背卡加入交換表，
  而且 XXL／XXS 應該在按下加入之前就看得到。
- 決定：去重看四個條件全等，不是只看「同一隻＋同一張背卡」。
  提案時寫的是後者，但那會擋掉「同一張背卡，一筆要異色一筆不要」這種合法需求，
  而防手滑要擋的本來就是「兩格一模一樣」。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增兩條測試：
    同一隻兩筆各自帶索引、草稿的條件與背卡有標起來且不會冒出編輯區。
  - 起 server 用 headless Chrome 走 DevTools Protocol 實際點過一遍：
    加兩筆、重複加被擋、刪一筆、從交換表點回詳情，
    桌機 1440 與手機 390 兩個寬度、深色模式都看過，沒有橫向捲動與爆版。
- 待辦/已知問題：
  - 儲存結構沒有變，既有紀錄照常讀，不需要資料遷移。
  - 一欄上限 200 筆。同一隻可以收多筆之後會更快吃到，暫時不動。

---

## 2026-09-11（二）
- 類型：重構
- 影響檔案：js/bgevents.js, js/bgdata.js, js/backgrounds.js, tools/build-bg.mjs
- 摘要：兩批背卡的 id 慣例統一，新背卡的 id 形狀也趁還沒發布時改好。
  - **手工那批的六筆註記換成型態層級 id**。原本是 `d128` 加註記
    「帕底亞的樣子・鬥」，現在直接寫 `d128.fPALDEA_COMBAT`，
    跟自動那批一致。名稱改由圖鑑提供，不必自己維護註記，
    格子也會顯示正確的裝扮圖而不是本體。
    圖鑑的譯名比原本的註記還清楚（肯泰羅（鬥戰種）、拉普拉斯（2023 春季 藍隊）），
    所以六筆註記全部拿掉。
  - **id 規則加一條：年份前面要斷開**。上游把年份黏在活動名後面，
    不斷開會產生 `citysafari2024` 這種讀不出來的東西。
    另外少數檔名年份寫了兩次，重複的去掉。
    `lc_CitySafari2023_barcelona_2023` 從 `city-safari2023-barcelona-2023`
    變成 `city-safari-2023-barcelona`。
  - 240 個 id 有 87 個變動，全部不重複，最長 37 字元（上限 40），
    撞名的那組仍靠保留 `lc`／`sb` 前綴處理。手工那 17 個 id 不受影響。
- 原因：兩批寫法不一致，同一個裝扮在不同卡片上長得不一樣；
  id 是使用者紀錄的鍵，發布之後就不能再改，現在改不影響任何人。
- 驗證：`node tools/check.mjs` 全部通過。id 不重複、不超過 40 字、
  既有 17 個 id 都還在、引用的條目都存在。
- 上線：2026-09-11 10:25 推上 main，Pages 部署完成，線上版本 8f0b61d。
  這一批五個 commit 一起上線（背卡擴充、寶可夢清單、五個裝扮、
  圖鏡像與去重、id 統一）。
- 待辦/已知問題：
  - **背卡 id 在這次之前指定過的會對不回來**。id 全部換過，
    例如 `citysafari2024-tainan` 變成 `city-safari-2024-tainan`。
    上線前指定過背卡的格子會變成沒有指定，清單本身與異色、XXL、XXS 不受影響。
    手工那 17 張的 id 沒動，GO Fest 與 GO Tour 那批不受影響。
    從現在起 id 不能再改了。
  - 25 張背卡仍然沒有寶可夢清單，三個來源都沒有。清單在 `tools/bg-report.md`。
  - 三語名稱只有手工那 17 張是完整的，其餘 223 張只有英文，
    要補就改 `js/bgevents.js`，它會逐欄覆蓋骨架。
  - 之後轉私人 repo 時要同時搬到 Cloudflare Pages。

---

## 2026-09-11（一）
- 類型：修正
- 影響檔案：img/extra/（新增 5 張）, js/extra.js, tools/check.mjs,
  CLAUDE.md, project-index.md, README.md, AGENTS.md
- 摘要：那五張裝扮圖收進 repo，並把 extra.js 的去重真的接起來。
  - **圖鏡像進 `img/extra/`**，合計 116 KB。原因是來源沒有 CORS 標頭，
    畫面上的 `<img>` 沒事，但分享圖是 canvas，載不到就退回官方立繪，
    等於看不出穿了什麼。收進來之後同源，兩邊都正常，也不再欠第三方。
  - **`ALIAS` 從此有人用**。它本來只是 export 出去，註解寫的去重沒有實作。
    現在 `extraEntries()` 會擋掉 godex 已經有的裝扮，比對規則是
    代碼轉小寫、底線換連字號、去掉 `_NOEVOLVE`，命名不同的走 `ALIAS`。
  - 另外加了 `supersededEntries()`，列出哪幾筆已經被 godex 收錄可以刪。
- 原因：上游哪天補上已經在 extra 裡的裝扮，圖鑑會出現兩張一樣的卡，
  而且是兩個不同的 id，使用者會兩個都收。
- 驗證：
  - `node tools/check.mjs` 全部通過，新增兩條測試：補充條目沒有跟圖鑑重複、
    `ALIAS` 指到的代碼都還在。
  - 去重不是空跑。在暫存複本塞了四筆一定重複的（c 前綴、f 前綴、
    走 ALIAS 的、走 NOEVOLVE 的），四筆全部被擋掉，輸出仍是 28 筆。
- 待辦/已知問題：
  - 自動與手工兩批的背卡 id 慣例仍不一致，待決定。
  - 新背卡的 id 形狀有點怪（`city-safari2023-barcelona-2023`），
    還沒 push，現在改不影響任何人。

---

## 2026-09-10（日）
- 類型：新增
- 影響檔案：js/extra.js, js/bgdata.js, CLAUDE.md, project-index.md
- 摘要：補上五個只有 Dittobase 有圖的裝扮，背卡清單不再有缺口。
  - 2026 世界錦標賽皮卡丘、玩偶裝皮卡丘、兩隻戴帽子的百變怪、
    2025 曠野地帶的穿著熊。條目 1478 → 1483，裝扮 298 → 303。
  - **PokeMiners 與 Choggor 都沒有這五個**。即時查過上游 3522 個檔案，
    沒有任何 2026 的裝扮；Choggor 的世界賽系列只到 2025，也沒有 PxP。
  - `DB_EXTRA` 的物種名稱與屬性不寫死，從 godex 抓本體那筆，避免打錯。
  - 譯名自己取。世界賽與曠野地帶照 godex 既有的年份加活動名，
    看不出活動的看圖命名。
- 原因：使用者指出 2026 世界賽皮卡丘的背卡確實存在，先前被當成上游還沒出圖而丟掉。
- 決定：圖連 Dittobase，官方立繪當備援。
- 驗證：
  - `node tools/check.mjs` 全部通過，五筆都建得起來，屬性與名稱正確。
  - 重跑 `tools/build-bg.mjs`，對不回條目 id 的從 5 種降到 0。
    世界賽舊金山那張從 37 格變 39 格，Pokopia 那張 3 格變 5 格。
- 待辦/已知問題：
  - **Dittobase 的 CDN 沒有 CORS 標頭**。畫面上看得到裝扮，
    但分享圖是 canvas，會載入失敗而退回官方立繪，那五筆在分享圖上
    看不出穿了什麼。要修就得把那五張圖鏡像進 repo，合計 106 KB。
  - `js/extra.js` 的 `ALIAS` 沒有任何地方 import，它註解寫的去重沒有實作。
    上游哪天補上已經在 extra 裡的裝扮，圖鑑會出現兩張一樣的卡。

---

## 2026-09-10（六）
- 類型：新增
- 影響檔案：js/bgdata.js, tools/build-bg.mjs, tools/bg-report.md,
  CLAUDE.md, project-index.md, README.md
- 摘要：背卡補上寶可夢清單，215 張有清單，1452 個收集格。
  - **主來源是 Dittobase**。它的頁面裡嵌了結構化 JSON，帶物種編號、
    型態代號，以及異色、暗影、超級進化、極巨化的旗標，比解 DOM 穩。
    抓 234 頁，一秒一頁，只存抽出來的結果不存原始 HTML。
  - **交叉比對兩個來源**。Serebii 193 張、Bulbapedia 45 張。
    差異 154 筆裡，119 筆是 Serebii 漏了進化取得的（City Safari 那批
    它只列伊布，另外兩邊都列了八個進化型），19 筆是它有而 Dittobase 沒有。
  - **Serebii 只當墊底**。Dittobase 完全沒有那張卡的資料時才用，
    12 張走這條，其餘 202 張走 Dittobase。兩邊的清單不混在一起，
    混了會冒出型態不明的重複條目。
  - **對照條目 id 踩了四個坑**：重音（Flabébé）、`_NOEVOLVE` 後綴、
    只有型態沒有本體的（酋雷姆用 fNORMAL 當本體）、
    以及叫法不同的裝扮（人工補別名）。修完剩 5 種對不回。
  - 那 5 種是上游還沒出圖的新裝扮（WCS 2026 皮卡丘、戴帽子的百變怪等），
    `assets.json` 裡查不到，圖鑑本來就沒收，不是漏掉。
  - 超級進化與極巨化整批排除，暗影併回本體。
- 原因：使用者要把全部背卡的對應關係納入，並要求另找來源交叉比對。
- 決定：手工那 17 張仍然優先，沒有被自動資料覆蓋。差異列在報告裡。
- 驗證：
  - `node tools/check.mjs` 全部通過，1452 個收集格引用的條目都存在。
  - 交叉比對的結果寫進 `tools/bg-report.md`，逐張列出有出入的。
- 待辦/已知問題：
  - **自動與手工兩批的 id 慣例不一致**。自動那批用型態層級
    （`d128.fPALDEA_COMBAT`），手工那 17 張用「合併成本體加註記」。
    要不要把手工那批也改成型態層級，待決定。
  - 25 張還是沒有清單，Dittobase 與 Serebii 都沒有。
  - 「有背卡可拿」的條目從 118 變成 443，篩選面板的數字會跟著跳。

---

## 2026-09-10（五）
- 類型：新增
- 影響檔案：js/bgdata.js, js/bgevents.js, js/bgseries.js, js/backgrounds.js,
  js/imgchain.js, js/dex.js, js/ui.js, js/main.js, js/share.js, js/i18n.js,
  css/style.css, tools/build-bg.mjs, tools/check.mjs
- 摘要：背卡從 17 張擴到 240 張，圖改連上游，照活動系列收進收納夾。
  - **背卡其實有資料源**，先前寫「百分之百手工維護」是錯的。
    game master 有 `LC_` 樣板 246 筆，pogo_assets 的 `Images/LocationCards/`
    有 240 張圖，`sb_` 是特殊背景、`lc_` 是地點卡。
  - **圖不再進 repo**，一律連上游，`img/bg/` 那 17 張留著當備援。
    跟寶可夢圖示同一個原則，新活動只要填檔名就有圖。
  - **備援鏈抽成 `js/imgchain.js`**。dex 與 backgrounds 互相有依賴，
    共用的東西放在它們底下才不會繞成一圈。
  - **資料分三層**，照 godex／extra／dex 那一套：`bgdata.js` 自動產生的
    骨架 223 張，`bgevents.js` 手工那 17 張，`backgrounds.js` 合併並當入口。
    合併以 asset 對應，手工逐欄覆蓋，重跑腳本不會洗掉手填的東西。
  - **收納夾 23 個**，人孔蓋 41、英國國民信託 27、GO Fest 21、GO Tour 19、
    City Safari 19、寶可夢中心 18、美職 15、日職 13，其餘較零散。
    預設全部收合，搜尋有輸入時只顯示命中的夾並自動展開。
  - **新 id 規則**：上游檔名去掉 `lc_`／`sb_` 前綴，駝峰與底線轉連字號。
    240 張最長 37 字，沒有超過 store 的 40 字上限。既有 17 個 id 原封不動。
  - **上游的圖不一定是卡面**。標了 `vfxAddress` 的 31 張（全部是 `sb_`）
    上游只有底層，實際卡面還疊了一層特效。逐張比過本地那 17 張，
    6 張 `lc_` 與上游完全相同，11 張 `sb_` 都不同。
    所以改成有本地圖就本地優先，上游當備援。剩下 20 張有特效層又沒有
    本地圖的，只能顯示底層。
  - 順手修掉一個舊 bug：`hidden` 屬性只有瀏覽器預設樣式在管，任何一條
    `display` 規則都蓋得過它。`.searchbar` 的 `display: flex` 就蓋掉了，
    所以圖鑑的搜尋列在交換表與背卡檢視也一直看得到。收納夾的
    `.bg-grid` 也踩到同一個坑，收合了格子還在。改成在基礎樣式
    一次講死 `[hidden] { display: none !important }`。
- 原因：使用者要把全部背卡納入，不只手工收的那十七張。
- 決定：
  - 寶可夢清單這一輪不做。上游沒有這份資料，Serebii 只到物種層級，
    型態要另一輪從 Dittobase 補，所以骨架的 pokemon 一律留空。
  - 交換表的背卡選單維持下拉，只改成照收納夾分 optgroup。
    原本打算改成 sheet 選擇器，但下拉列的是「這隻能帶的背卡」不是全部，
    數量有限，換掉是多餘的複雜度。
- 驗證：
  - `node tools/check.mjs` 全部通過，三語各 98 個 key。新增六條背卡測試：
    每張都有上游檔名、id 不重複、id 不超過 40 字、既有 17 個 id 還在、
    收納夾都有名稱、收納夾不是空的。第四條是回歸測試，防的是
    腳本重跑把使用者的鍵洗掉。
  - `node tools/check.mjs --net` 通過，240 張背卡圖片網址全部 200。
  - headless Chrome 實際看過收納夾、展開、搜尋、背卡詳情與交換表。
- 待辦/已知問題：
  - 223 張骨架還沒有寶可夢清單，詳情面板會顯示「還沒收錄」。
  - Serebii 對不到 47 張，那些沒有日期，名稱是檔名推的暫名。
    上游有兩個拼字錯誤（gibslide、whimpole），對不上是正常的。
  - 缺正式代號 12 張，game master 沒有對應樣板，可能還沒上線或已下架。
  - 三語名稱只有手工那 17 張是完整的，其餘只有英文。
  - 之後轉私人 repo 時要同時搬到 Cloudflare Pages。

---

## 2026-09-10（四）
- 類型：新增
- 影響檔案：js/dex.js, js/ui.js, js/main.js, js/i18n.js, css/style.css,
  index.html, tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：圖鑑加多維篩選，五個群組，組間 AND、組內 OR。
  - **群組**：種類（一般／型態／裝扮／地區型）、屬性 18 種、
    世代 9 個地區、稀有度（傳說／神話／究極異獸）、
    其他（有異色／有背卡可拿）。
  - **世代靠圖鑑編號區間判斷**，game master 沒這個欄位。標籤用地區名，
    「第四代」不如「神奧」好認。
  - **有背卡可拿 118 筆**。背卡資料本來只有背卡檢視在用，
    現在圖鑑也篩得到了。`dex.js` 因此多匯入 `backgrounds.js` 與 `types.js`，
    兩條都是往資料層，沒有循環。
  - **介面**：搜尋列旁邊一顆篩選鈕，點了從下方升起面板，
    跟條目詳情與背卡詳情共用同一個 sheet。改動即時生效，沒有套用鈕。
    側欄原本的單選篩選列整段移除。
  - **計數扣掉自己那一組**。算「火」有幾筆時其他組照套、屬性組放掉，
    否則同一組加起來不會等於這組全不選的結果。`applyFilter` 的
    第三個參數就是幹這個的。
  - 會變成零筆的選項直接停用，點了只會得到空畫面。
  - 篩選鈕在有條件時變色並顯示個數。面板關起來後這是唯一的提示，
    沒有它就會出現「我的寶可夢怎麼不見了」。
- 原因：使用者要圖鑑能依屬性與世代篩選，並說不限於這兩項。
- 決定：篩選不寫進偏好。重新整理回到全部，避免下次打開只剩十幾筆
  卻不知道為什麼。
- 驗證：
  - `node tools/check.mjs` 全部通過，三語各 91 個 key 一致。
    新增 12 條篩選測試：組內 OR、組間 AND、九個世代加起來等於全部、
    skip 參數、髒資料、有背卡的條目對得上背卡資料。
  - headless Chrome 實際操作過：開面板、點火加水加神奧得到 31 筆，
    背後圖鑑同步變 31 項，其他組的計數跟著更新，零筆的選項變灰。
    英文介面另外確認。
- 待辦/已知問題：
  - 種類裡的地區型是型態變化的子集，兩個都選等於只選型態變化。
    不會出錯但也沒意義。
  - 之後轉私人 repo 時要同時搬到 Cloudflare Pages。

---

## 2026-09-10（三）
- 類型：修正
- 影響檔案：css/style.css, js/share.js
- 摘要：交換表的格子不再畫框，寶可夢直接站在面板上。分享圖同步。
  - 指定了背卡的格子例外，那張底圖本身就是框，仍然裁圓角。
  - 沒有框就沒有邊框可以在 hover 時變色，改用淡底表示指到了。
- 原因：使用者覺得每一隻都框起來沒必要。一欄最多兩百隻，
  每一隻都有線條的話整面都是框。
- 驗證：`node tools/check.mjs` 全部通過。headless Chrome 實際看過
  交換表的淺色、深色、關掉名稱三種，分享圖的淺色帶名稱與
  深色關名稱兩版。

---

## 2026-09-10（二）
- 類型：新增
- 影響檔案：css/style.css, js/ui.js, js/main.js, js/share.js, js/i18n.js,
  js/store.js, index.html, tools/check.mjs, CLAUDE.md, project-index.md
- 摘要：交換表與圖鑑改成參考圖那種高密度方格牆，手機一排五隻，
  另外加了圖示大小與名稱兩個顯示開關，以及分享圖底部的訓練家代碼。
  - **版面**：格子縮小，區塊標題改成圓點加標題，格子牆包一層圓角面板。
    異色從膠囊標籤改成格子左上角的星星，XXL 與 XXS 移到格子下方。
    畫面與分享圖同一套規則，維持兩邊一致。
  - **格子與名稱分離**：`want-cell` 裡多包一層 `want-tile`，背卡底圖只鋪滿
    方塊，名稱在方塊外面。順手拿掉了名稱底下那塊實色底，
    那原本是為了讓名稱壓在背卡上還看得清楚才加的，現在不需要了。
  - **顯示開關**：側欄新增「顯示」區塊，兩顆開關。走 body 的 class 切換，
    不是傳旗標給每個繪製函式。存 `poke-change/pref`。
  - **訓練家代碼**：只印在分享圖底部，畫面上只有輸入框。存偏好不存清單，
    因為它是使用者的身分不是清單的屬性。`cleanCode` 與 `formatCode`
    放在 `store.js`，讓 `ui.js` 與 `share.js` 共用同一套規則，
    不要在繪製層之間拉橫向依賴。
  - **分享圖**：四欄改五欄，格子從 148 縮到 120，寬度 660。
    名稱關掉時整張圖明顯變短。
  - 順手拿掉 `markNote`，上一輪移除備註時漏掉的死 key。
- 原因：使用者拿了另一個工具的分享圖當參考，要手機上的交換頁面長那樣，
  另外要求圖鑑一排至少五隻。
- 決定與取捨：
  - 顏色沿用現有深淺色 token，沒有照抄參考圖的深藍。使用者選的，
    所以照抄的是版面與密度不是配色。
  - **手機的欄數講死不推算**。`minmax` 推出來的欄數會跟著螢幕寬度飄，
    實測 390px 時交換表只排得下四欄，差 0.4px；360px 更糟。
    而且圖鑑沒有面板內距、交換表有，同樣寬度會算出不同結果。
    900px 以下直接指定欄數就沒有這些問題。
- 驗證：
  - `node tools/check.mjs` 全部通過，三語各 73 個 key 一致。
    新增訓練家代碼的五條規則測試與兩個繪製情境。
  - 320 / 360 / 390 / 430px 四種寬度、三個檢視實測，全部五欄，
    `scrollWidth` 等於視埠寬，沒有水平溢出。
  - headless Chrome 實際看過：交換表的小圖示、大圖示、關掉名稱三種組合，
    圖鑑小圖示，深色模式與英文介面。
  - 分享圖實際產出並檢查，顯示名稱與關掉名稱兩版都看過。
- 待辦/已知問題：
  - 之後轉私人 repo 時要同時搬到 Cloudflare Pages。

---

## 2026-09-10
- 類型：重構
- 影響檔案：js/store.js, js/i18n.js, tools/check.mjs,
  CLAUDE.md, project-index.md
- 摘要：把 `note` 欄位從儲存結構移除，這個懸而未決的待辦收掉。
  - `js/store.js`：`newItem` 不再產生 `note`，`cleanItem` 也不再保留。
    舊資料帶著 `note` 進來會被 `normalize` 直接洗掉。
  - `js/i18n.js`：三語的 `noteHint` 一併刪除，本來就沒有任何地方引用。
    每語 70 個 key 變 69 個。
  - `tools/check.mjs`：儲存往返測試改測 `bg` 的長度截斷，
    另外加一條「舊版的備註欄位會被洗掉」確認向後相容行為。
    逸出測試的對象從備註改成清單名稱，那是現在唯一的使用者自由輸入。
  - `js/backgrounds.js` 的 `note_zh` / `note_ja` / `note_en` 是背卡註記，
    跟這件事無關，沒有動。
- 原因：備註只有自己看得到。對方收到的是分享圖，讀不到任何文字欄位，
  所以寫了也傳達不出去。要讓對方知道的條件必須畫得出來，
  也就是異色、XXL、XXS、背卡這四個維度。
- 資料風險：無。線上版從來沒有出現過備註輸入框，
  它在 09-09 的方格牆重構就已從介面拿掉，而站台是同一天才上線。
- 驗證：`node tools/check.mjs` 全部通過（i18n 三語各 69 key 一致、
  1478 筆詳情逐一繪製）。本機 server 起來，主要檔案全部 200。
  headless Chrome 用注入的測試清單實際看過交換表與詳情面板，
  五個格子、XXL / XXS / ✦ 符號、背卡下拉都正常，沒有殘留空白區塊，
  也沒有出現裸露的 i18n key 名。
- 待辦/已知問題：
  - 之後轉私人 repo 時要同時搬到 Cloudflare Pages。

---

## 2026-09-09（四）
- 類型：修正
- 影響檔案：js/costumes.js, tools/build-dex.mjs, js/godex.js,
  project-index.md, CLAUDE.md
- 摘要：把五個推測來的譯名換成看圖命名的版本，並推上 GitHub Pages。
  - 下載這幾個條目的圖檔跟本尊並排比對後命名：
    `PI` 是一頂咖啡色鴨舌帽（皮卡丘、雷丘、呆呆獸都有）→「鴨舌帽」
    `2020` / `2021` / `2022` 是印著年份的新年造型眼鏡 →「20XX 新年眼鏡」
    `S` 不是換色也不是裝扮，是姿勢不同。洛奇亞與鳳王展翅、
    拉帝斯兄妹前傾 →「特別姿勢」
  - 命名原則改成「看得出是哪一個」，不再嘗試還原代碼原意。
    官方本來就沒給裝扮命名，猜代碼含意沒有意義也驗證不了。
- 原因：使用者表示名詞可以自己取，好區分就好。
- 驗證：`node tools/check.mjs` 全部通過；線上 13 個檔案全部 200，
  headless Chrome 實際跑過線上版確認圖鑑載入正常。
- 待辦/已知問題：
  - `note` 欄位還在儲存結構裡但介面不顯示，要留要拿掉未定案。
  - 之後轉私人 repo 時要同時搬到 Cloudflare Pages。

---

## 2026-09-09（三）
- 類型：重構
- 影響檔案：js/ui.js, js/share.js, js/main.js, css/style.css
- 摘要：交換表改回方格牆，跟舊專案的設計一致。
  - 原本每一列都是「名稱 + 三顆標籤 + 背卡下拉選單 + 備註輸入框」，
    整頁都是文字與表單，看不出這是一份寶可夢清單。
  - 改成跟圖鑑一樣的格子：背卡圖疊在寶可夢後方，狀態用角落的
    小符號表示（XXL / XXS / ✦），刪除鈕在右上角。
  - 所有條件編輯搬進詳情面板，點格子就能改。交換表本身零表單。
  - 分享圖同步改成方格牆，兩區上下排列。左右並排會太寬，
    傳進 LINE 會被縮到看不清楚。
  - 背卡底圖上的名稱加了實色底。背卡有亮有暗，只靠文字陰影不夠穩。
- 原因：使用者指出交換表文字太多，要跟原本的設計一樣。
- 驗證：`node tools/check.mjs` 全部通過；headless Chrome 實際渲染
  交換表與分享圖確認版面。
- 待辦/已知問題：
  - `note` 欄位還留在儲存結構裡但介面已經不顯示。
    要留要拿掉待確認。

---

## 2026-09-09（二）
- 類型：新增
- 影響檔案：index.html, css/style.css, js/dex.js, js/store.js, js/i18n.js,
  js/ui.js, js/main.js, js/share.js, js/extra.js, js/backgrounds.js,
  tools/check.mjs, README.md, project-index.md
- 摘要：完成整個介面層，站台可以實際使用了。
  - `js/dex.js`：合併 godex 與 extra 成 1478 個條目，統一名稱組合、
    搜尋、篩選與圖片備援鏈。畫面層只跟這個檔說話。
  - `js/store.js`：localStorage 儲存，加匯出匯入。讀進來的資料一律過
    `normalize`，因為 localStorage 使用者可以手動改。
  - `js/i18n.js`：重寫成交換工具的字典，三語各 70 個 key。
  - `js/ui.js` + `js/main.js`：三個檢視，圖鑑、交換表、背卡。
  - `js/share.js`：雙欄分享圖，左邊想要右邊可以給。
  - `js/backgrounds.js`：helper 改寫成用條目 id，`cardsFor` 改成通用查詢。
  - `tools/check.mjs`：自我檢查，涵蓋 i18n、條目、背卡、儲存往返、
    全部繪製函式與 HTML 逸出。
- 原因：資料層完成後接介面，讓交換表能實際產出分享圖。
- 驗證：
  - `node tools/check.mjs` 全部通過，含 1478 筆詳情與 17 張背卡逐一繪製。
  - 本機 server 實測，13 個檔案全部 200。
  - headless Chrome 實際渲染並截圖確認四個畫面：圖鑑、交換表、
    背卡、條目詳情，深色模式與英文介面另外確認。
  - 分享圖實際產出並檢查版面，修掉備註與標籤重疊、
    以及只有名稱的列文字靠上而圖示置中的對齊問題。
  - 手機版用 360px iframe 量測，`scrollWidth` 等於視埠寬，沒有水平溢出。
    先前 430px 截圖看起來溢出是 headless Chrome 視埠下限 500px 造成的裁切，
    不是版面問題。
- 待辦/已知問題：
  - 尚未推上 GitHub，也還沒設定 Pages。
  - 裝扮譯名有 4 個代碼是推測的，`js/costumes.js` 標了 ※。
  - `S` 型態譯成「特別」是暫定，待確認實際是什麼。

---

## 2026-09-09
- 類型：新增
- 影響檔案：tools/build-dex.mjs, js/godex.js, js/costumes.js, .gitignore
- 摘要：建立專案並完成資料層。
  - `tools/build-dex.mjs` 離線解析 PokeMiners game master 與圖檔清單，
    產出 `js/godex.js`，1458 個條目，含地區型、型態變化、裝扮。
  - 條目清單以圖檔為準而非 game master，因為 game master 收了很多
    沒實裝的型態，而畫得出來的一定有圖檔。
  - 異色是否實裝改用「有沒有異色圖檔」判斷。game master 沒有這個旗標，
    實測熊徒弟、武道熊師、眷戀雲確實沒有異色圖，與已知情況相符。
  - `js/costumes.js` 手動維護裝扮譯名。裝扮在遊戲內沒有官方名稱，
    語言檔查不到，只能自己取。共 121 個代碼。
  - 排除超級進化與極巨化 60 筆。那是戰鬥中的暫時狀態，不是可交換的個體。
- 原因：專案從「傳說收集圖鑑」轉向「交換表格製作」，資料庫要從 79 隻
  擴到全圖鑑，且以活動限定的裝扮與背卡為差異化重點。
- 驗證：全欄位完整、id 無重複、屬性零缺漏；三語名稱齊全；
  隨機抽樣 12 個條目共 24 個圖片網址實測全部 200。
- 待辦/已知問題：
  - 裝扮譯名有 4 個代碼含意是從檔名推測的，標了 ※ 待核對。
  - 尚未接介面。下一步是儲存層改 localStorage 與雙欄交換表。
